---
title: "HCLSM: Hierarchical Causal Latent State Machines for Object-Centric World Modeling"
authors: "Jaber Jaber, Osama Jaber"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29090v1"
tags: ["query:ai-3d"]
score: 10.0
evidence: 以物体为中心的世界建模与连续物理
tldr: HCLSM是一种利用分层潜状态进行以物体为中心的因果和物理预测的世界模型架构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29090v1/fig-001.webp\", \"caption\": \"Figure 2: Spatial broadcast decoder output. Top row: per-slot alpha heatmaps (ownership probability). Bottom row: slot overlays on input frame. Left: segmentation map (argmax over slots). Different colors indicate different slot assignments, showing emerging spatial decomposition.\", \"page\": 7, \"index\": 1, \"width\": 826, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2603.29090v1/fig-002.webp\", \"caption\": \"Figure 3: Event detection across four episodes. Blue = event probability over time. Red dashed = detected event boundaries. The model learns to fire at moments of state transition (2–3 per episode).\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 702}, {\"url\": \"assets/figures/arxiv/2603.29090v1/fig-003.webp\", \"caption\": \"Figure 1: HCLSM architecture. Five layers process video into structured world states. The spatial broadcast decoder (SBD, dashed) provides the reconstruction signal that drives slot specialization during Stage 1 training.\", \"page\": 4, \"index\": 3, \"width\": 531, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2603.29090v1/fig-004.webp\", \"caption\": \"Figure 4: Slot state trajectories projected to 2D via PCA. Circles = start, squares = end. Different colors = different slots. Trajectories show structured temporal dynamics with slot-specific evolution paths.\", \"page\": 9, \"index\": 4, \"width\": 582, \"height\": 467}]"
motivation: 以物体为中心的世界建模与连续物理。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
从视频预测未来状态的世界模型目前仍受限于扁平的潜表示，这些表示往往纠缠了对象、忽略了因果结构，并将时间动力学压缩到单一尺度。我们提出了 HCLSM，这是一种基于三个相互关联原则运行的世界模型架构：首先，通过带有空间广播解码的槽注意力（slot attention）进行以对象为中心的分解；其次，通过三层引擎实现分层时间动力学，该引擎结合了用于连续物理的选择性状态空间模型（SSM）、用于离散事件的稀疏 Transformer 以及用于抽象目标的压缩 Transformer；最后，通过图神经网络交互模式进行因果结构学习。HCLSM 引入了一种两阶段训练协议，其中空间重建在动力学预测开始前强制槽位专门化。我们在 Open X-Embodiment 数据集的 PushT 机器人操作基准上训练了一个拥有 6800 万参数的模型，实现了 0.008 MSE 的下一状态预测损失，并展现出涌现的空间分解（SBD 损失为 0.0075）和学习到的事件边界。用于 SSM 扫描的自定义 Triton 内核相比顺序 PyTorch 实现了 38 倍的加速。整个系统包含分布在 51 个模块中的 8,478 行 Python 代码，并配有 171 个单元测试。代码：https://github.com/rightnow-ai/hclsm

## 速览
**TLDR**：针对现有世界模型在物体解耦、因果结构和多尺度动力学方面的局限，本文提出HCLSM架构。该模型通过Slot Attention实现物体中心化分解，并构建了结合选择性状态空间模型（SSM）、稀疏Transformer和压缩Transformer的三层分层动力学引擎，同时利用GNN学习因果交互。在PushT机器人任务中，该68M参数模型实现了高精度预测与显著的推理加速。 \
**Motivation**：传统世界模型采用扁平潜空间，难以有效处理物体间的解耦、因果关系及跨尺度的复杂时间动态。 \
**Method**：提出一种集成槽位注意力、三层分层动力学引擎及图神经网络因果学习的物体中心化世界模型架构。 \
**Result**：在PushT基准测试中达到0.008的预测均方误差，并利用自定义Triton内核实现了38倍的SSM扫描加速。 \
**Conclusion**：HCLSM证明了通过分层因果结构和物体中心化分解，能够更精确且高效地建模复杂的机器人操作环境。

---

## Abstract
World models that predict future states from video remain limited by flat latent representations that entangle objects, ignore causal structure, and collapse temporal dynamics into a single scale. We present HCLSM, a world model architecture that operates on three interconnected principles: object-centric decomposition via slot attention with spatial broadcast decoding, hierarchical temporal dynamics through a three-level engine combining selective state space models for continuous physics, sparse transformers for discrete events, and compressed transformers for abstract goals, and causal structure learning through graph neural network interaction patterns. HCLSM introduces a two-stage training protocol where spatial reconstruction forces slot specialization before dynamics prediction begins. We train a 68M-parameter model on the PushT robotic manipulation benchmark from the Open X-Embodiment dataset, achieving 0.008 MSE next-state prediction loss with emerging spatial decomposition (SBD loss: 0.0075) and learned event boundaries. A custom Triton kernel for the SSM scan delivers 38x speedup over sequential PyTorch. The full system spans 8,478 lines of Python across 51 modules with 171 unit tests. Code: https://github.com/rightnow-ai/hclsm

---

## 论文详细总结（自动生成）

以下是对论文《HCLSM: Hierarchical Causal Latent State Machines for Object-Centric World Modeling》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频预测世界模型（如 V-JEPA）通常使用“扁平”的潜表示，将场景中的物体、因果结构和不同尺度的时间动力学纠缠在一起，导致模型缺乏对物理世界的结构化理解，难以进行复杂的规划和反事实推理。
*   **研究动机**：人类感知世界是结构化的（识别物体、跟踪运动、推断因果）。作者旨在构建一个能够模拟这种结构的世界模型，解决三个维度的问题：**物体解耦**（独立表示实体）、**时间分层**（处理连续物理、离散事件和长期目标）以及**因果推理**（明确物体间的相互影响）。

### 2. 方法论：核心思想与技术细节
HCLSM 架构由五个互联层组成，并采用独特的两阶段训练协议：
*   **感知与物体分解**：
    *   使用 **Slot Attention（槽注意力）** 将 ViT 编码的图像特征分解为多个“槽位”（Slots）。
    *   引入 **空间广播解码器（SBD）**，以冻结的 ViT 特征为重建目标，强制每个槽位竞争并负责特定的空间区域。
*   **三层分层动力学引擎**：
    *   **L0 层（连续物理）**：使用**选择性状态空间模型（SSM/Mamba）**处理每个物体的帧间连续运动。
    *   **L1 层（离散事件）**：通过事件检测器识别状态突变（如碰撞），并使用**稀疏 Transformer** 处理这些关键时间点。
    *   **L2 层（抽象目标）**：使用**压缩 Transformer** 将事件序列总结为高层状态，用于目标规划。
*   **因果结构学习**：利用图神经网络（GNN）处理物体间的交互，通过学习边缘权重来揭示因果关系，并引入 NOTEARS 约束来保证因果图的有向无环性（DAG）。
*   **两阶段训练协议**：
    *   **阶段 1**：仅进行空间重建（SBD 损失），强制槽位实现空间专门化（即“认出物体”）。
    *   **阶段 2**：激活 JEPA 风格的动力学预测损失，在已解耦的物体基础上学习运动规律。

### 3. 实验设计
*   **数据集与场景**：主要在 **PushT** 机器人操作基准上进行测试（来源于 Open X-Embodiment 生态系统）。该任务包含机器人推 T 型块到达目标的 206 个视频片段。
*   **对比方法**：
    *   对比了 HCLSM 在“有无 SBD 两阶段协议”下的表现。
    *   在架构设计上参考并对比了 V-JEPA 2、DreamerV3、SlotFormer 和 DINOSAUR 等模型的功能差异（见论文表 1）。
*   **评估指标**：下一状态预测均方误差（MSE）、SBD 重建损失、槽位多样性损失、事件检测准确性及推理速度。

### 4. 资源与算力
*   **硬件使用**：使用了 **NVIDIA H100 80GB GPU**。
*   **训练时长**：68M 参数的小型模型（HCLSM Small）在单卡上训练 50K 步约需 **6 小时**。
*   **工程优化**：开发了自定义 **Triton 内核**用于 SSM 扫描，相比原生 PyTorch 实现了 **38 倍**的加速。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了在 PushT 任务上的定量结果和定性可视化（空间分解图、事件检测曲线、PCA 轨迹投影）。
*   **充分性评价**：
    *   **初步验证有效**：证明了两阶段训练对物体分解的必要性。
    *   **样本量较小**：仅在一个机器人任务（PushT）上进行了深入实验，缺乏多任务、多环境的大规模验证。
    *   **稳定性问题**：作者坦言实验存在种子敏感性，4 次运行中仅 2 次成功收敛，其余因 bf16 精度下的梯度爆炸（NaN）而失败。

### 6. 主要结论与发现
*   **结构先于预测**：如果一开始就同时训练重建和预测，模型会倾向于使用分布式编码而非物体解耦。先训练空间分解是实现物体中心化建模的关键。
*   **分层动力学有效**：模型能够自动学习到事件边界（如机器人接触物体的瞬间），并能通过 SSM 高效处理物体轨迹。
*   **性能提升**：在 PushT 任务上达到了 0.008 的预测 MSE，且自定义内核显著降低了计算瓶颈。

### 7. 优点与亮点
*   **架构创新**：首次将槽位注意力、三层分层动力学（SSM + Transformer）和因果 GNN 统一在一个可微架构中。
*   **工程质量高**：提供了 8,478 行高质量代码、171 个单元测试以及高性能 Triton 内核，具有很强的开源贡献。
*   **生物启发**：两阶段训练协议模拟了视觉皮层先发育物体识别、后发育运动预测的生物观察。

### 8. 不足与局限
*   **槽位管理失效**：模型目前无法自动“杀死”多余槽位，导致 3 个物体的场景使用了全部 32 个槽位，物体被过度分割。
*   **因果发现不理想**：显式因果邻接矩阵在实验中未能学习到有效边缘（权重趋于零），因果学习部分仍处于初步阶段。
*   **数值稳定性差**：在 bf16 精度下极易出现 NaN 梯度，限制了模型向更大规模（如 3B 参数）扩展。
*   **泛化性待验证**：尚未在更复杂、多物体的真实场景（如 ALOHA 双臂操作）中证明其优越性。

（完）
