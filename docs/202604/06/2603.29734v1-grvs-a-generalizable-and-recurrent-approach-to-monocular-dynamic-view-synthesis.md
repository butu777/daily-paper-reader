---
title: "GRVS: a Generalizable and Recurrent Approach to Monocular Dynamic View Synthesis"
title_zh: GRVS：一种用于单目动态视图合成的可泛化循环方法
authors: "Thomas Tanay, Mohammed Brahimi, Michal Nazarczuk, Qingwen Zhang, Sibi Catley-Chandar, Arthur Moreau, Zhensong Zhang, Eduardo Pérez-Pellitero"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29734v1"
tags: ["query:3d"]
score: 9.0
evidence: 单目动态视图合成与4D表示
tldr: 针对单目动态视频新视角合成中几何不一致和计算开销大的问题，本文提出GRVS模型。该方法通过循环神经网络实现输入与目标视频间的异步映射，并利用动态平面扫描技术解耦相机与场景运动，从而实现精确的6自由度相机控制。实验表明，该通用模型在处理复杂动态场景的几何细节上优于传统的场景特定优化方法和生成式扩散模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29734v1/fig-001.webp\", \"caption\": \"Figure 1. Scene-specific approaches optimized under motion priors typically reconstruct static regions well but struggle with dynamic elements (e.g. 4DGS [75]). Diffusion-based approaches conditioned on projected point-clouds typically struggle with finegrained geometries (e.g. Gen3C [51]). Our proposed method reconstructs static and dynamic elements with high accuracy.\", \"page\": 1, \"index\": 1, \"width\": 503, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-002.webp\", \"caption\": \"Figure 4. Qualitative evaluation on UCSD. On the left, we show the first and last frames for 2 input sequences. We then show the predictions of the three baselines and our method for a mid-sequence frame on those sequences. The ground-truths are shown on the right, with the dynamic elements highlighted.\", \"page\": 6, \"index\": 2, \"width\": 1036, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-003.webp\", \"caption\": \"Figure 5. Qualitative evaluation on Kubric-4D-dyn. At the top, we show every 10th frames for one input sequence, and a visualization of the relative positions of the input trajectory (in green) and three target cameras placed at increasing distances from the input trajectory (in red). Below, we show the predictions of the five baselines and our method for the three target views. The ground-truths are shown on the right, with the dynamic elements highlighted (yellow car, white and blue box, green shoes).\", \"page\": 6, \"index\": 3, \"width\": 1039, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-004.webp\", \"caption\": \"Table 1. Quantitative evaluation on UCSD. The metrics are computed on full images (left) and on dynamic regions only (right). Dynamic regions are determined using the dynamic masks provided in [38]. The performances for the starred methods are reproduced from [34]. For each metric, 1st, and 2nd best-performing methods are highlighted in red and orange respectively.\", \"page\": 7, \"index\": 4, \"width\": 496, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-005.webp\", \"caption\": \"Table 2. Quantitative evaluation on Kubric-4D-dyn. The metrics are computed on full images (left) and on dynamic regions only (right), for three targets at increasing distances from the input trajectory. Full images are center-cropped to match the rectangular dimensions of GCD predictions (341×512). Dynamic regions are determined by segmenting out dynamic objects. For each metric, 1st, and 2nd best-performing methods are highlighted in red and orange respectively.\", \"page\": 7, \"index\": 5, \"width\": 496, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-006.webp\", \"caption\": \"Figure 2. GRVS. For a target view Ji at time ti and with camera parameters Qi , our Generalizable Recurrent View Synthesizer consists of 5 stages. 1) The selection of V input views Ii uniformly sampled around the time ti , with corresponding camera parameters Pi. 2) The projection of Ii into a dynamic plane sweep volume Xi using the homographies HPi→Qi . 3) The patchification and reshaping of Xi into a downsampled tensor Yi. 4) The latent rendering of Yi into a hidden state Zi using the recurrent hidden state Zi−1 projected using the homographies HQi−1→Qi . 5) The decoding of Zi into the predicted output J̃i.\", \"page\": 3, \"index\": 6, \"width\": 924, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-007.webp\", \"caption\": \"Table 3. Influence of various hyper-parameters. Each model is trained on Kubric-4D-dyn and evaluated on target 1. The frame rate (FPS) and the number of parameters are also reported. Performance increases with the number of depth planes D and the number of input views V , and decreases with the patch size F .\", \"page\": 8, \"index\": 7, \"width\": 496, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2603.29734v1/fig-008.webp\", \"caption\": \"Figure 3. Dynamic Plane Sweep Volume. Projecting V input images onto D fronto-parallel planes facing the target camera produces a 5D plane sweep volume tensor of shape D×V ×3×H×W , illustrated here on an example scene by averaging over the V dimension and plotting the D = 6 planes in a row (the depth increases left-to-right). When the scene is static (top), all the elements of the scene appear in focus at their respective depths. When the scene is dynamic (bottom), static elements are in focus and dynamic elements are not. Our model takes plane sweep volumes as input, computed over dynamic monocular sequences.\", \"page\": 4, \"index\": 8, \"width\": 1036, \"height\": 243}]"
motivation: 现有的场景特定优化方法在高度动态区域易失效，而扩散模型则难以保持几何一致性且计算资源消耗巨大。
method: 采用循环结构处理长序列映射，并结合动态平面扫描机制来解耦相机运动与场景运动，实现高效的通用视角合成。
result: 在UCSD和新推出的Kubric-4D-dyn数据集上，该模型在重建静态与动态区域的几何细节方面均优于四种高斯泼溅方法和两种扩散模型。
conclusion: GRVS证明了结合循环机制与运动解耦的通用模型在单目动态视角合成任务中具有更强的几何保真度和泛化能力。
---

## 摘要
从动态场景的单目视频中合成新视图仍然是一个具有挑战性的问题。针对特定场景的方法通过显式运动先验优化 4D 表示，但在难以利用多视图信息的高动态区域往往会失效。将相机控制集成到大型预训练模型中的扩散方法可以生成视觉上合理的视频，但在静态和动态区域经常会出现几何不一致的问题。这两类方法都需要大量的计算资源。借鉴静态新视图合成中可泛化模型的成功经验，我们将该框架应用于动态输入，并提出了一个包含两个关键组件的新模型：(1) 一个循环回路，能够实现输入视频与目标视频之间无界且异步的映射；(2) 在动态输入上高效利用平面扫描（plane sweeps）来解耦相机运动与场景运动，并实现细粒度的六自由度相机控制。我们在 UCSD 数据集和 Kubric-4D-dyn 上训练并评估了我们的模型。Kubric-4D-dyn 是一个新的单目动态数据集，与现有替代方案相比，它具有更长、更高分辨率的序列以及更复杂的场景动态。我们的模型在重建静态和动态区域的细粒度几何细节方面，优于四种基于高斯泼溅（Gaussian Splatting）的特定场景方法以及两种基于扩散的方法。

## Abstract
Synthesizing novel views from monocular videos of dynamic scenes remains a challenging problem. Scene-specific methods that optimize 4D representations with explicit motion priors often break down in highly dynamic regions where multi-view information is hard to exploit. Diffusion-based approaches that integrate camera control into large pre-trained models can produce visually plausible videos but frequently suffer from geometric inconsistencies across both static and dynamic areas. Both families of methods also require substantial computational resources. Building on the success of generalizable models for static novel view synthesis, we adapt the framework to dynamic inputs and propose a new model with two key components: (1) a recurrent loop that enables unbounded and asynchronous mapping between input and target videos and (2) an efficient use of plane sweeps over dynamic inputs to disentangle camera and scene motion, and achieve fine-grained, six-degrees-of-freedom camera controls. We train and evaluate our model on the UCSD dataset and on Kubric-4D-dyn, a new monocular dynamic dataset featuring longer, higher resolution sequences with more complex scene dynamics than existing alternatives. Our model outperforms four Gaussian Splatting-based scene-specific approaches, as well as two diffusion-based approaches in reconstructing fine-grained geometric details across both static and dynamic regions.

---

## 论文详细总结（自动生成）

### 论文总结：GRVS —— 一种用于单目动态视图合成的可泛化循环方法

#### 1. 核心问题与研究动机
论文针对的是**从单目动态视频中合成新视角（Novel View Synthesis, NVS）**这一极具挑战性的任务。
*   **研究背景**：传统的 NVS 假设场景是静态的。在动态场景下，相机运动与场景中物体的运动相互交织，导致多视图几何约束失效。
*   **现有方法局限性**：
    *   **场景特定优化类（如 4DGS）**：需要为每个视频单独训练（耗时 20-40 分钟），且在高度动态区域容易崩溃。
    *   **基于扩散模型类（如 Gen3C）**：虽然视觉效果好，但往往缺乏几何一致性，且依赖昂贵的预训练模型和不准确的深度先验。
*   **核心问题**：如何构建一个既能泛化到未知场景、又能保持几何一致性、且计算高效的单目动态 NVS 模型？

#### 2. 方法论
GRVS 将动态 NVS 视为输入视频到目标视频的**异步映射**，核心思想包括：
*   **循环潜空间渲染（Recurrent Latent Rendering）**：引入循环回路（Recurrent Loop），通过隐藏状态 $Z_i$ 传递时空信息。这不仅保证了视频生成的时序一致性，还允许模型通过多次迭代（由粗到精）累积全局和局部上下文。
*   **动态平面扫描体（Dynamic Plane Sweep Volume, PSV）**：将输入帧投影到目标相机视角的多个深度平面上。PSV 的妙处在于它能自然地**解耦相机运动与场景运动**：静态背景在对应深度平面上是“对焦”的，而动态物体则呈现出特定的伪影，供网络学习识别。
*   **算法流程**：
    1.  **输入选择**：在目标时间点 $t_i$ 附近均匀采样 $V$ 帧输入。
    2.  **构建动态 PSV**：利用单应性变换将输入帧投影至目标视角。
    3.  **分块处理（Patchify）**：下采样以提升计算效率。
    4.  **循环渲染**：结合当前 PSV 和经视角变换后的前一帧隐藏状态，通过 3D Unet 预测当前帧潜变量。
    5.  **解码输出**：将潜变量还原为 RGB 图像。

#### 3. 实验设计
*   **数据集**：
    *   **UCSD 数据集**：真实的动态场景，用于验证模型在真实数据上的表现。
    *   **Kubric-4D-dyn**：论文新提出的合成数据集，包含 5000 个训练场景，分辨率达 512x512，具有更复杂的物体运动和更长的序列。
*   **Benchmark 与对比方法**：
    *   **场景特定方法**：D-3DGS、SC-GS、4DGS、MoSca。
    *   **扩散模型方法**：GCD、Gen3C。
*   **评估指标**：PSNR、SSIM、LPIPS（分别针对全图和仅动态区域进行评估）。

#### 4. 资源与算力
*   **训练配置**：使用了 **4 张 32GB 显存的 GPU**（具体型号未指明，推测为 V100 或 A100 级别）。
*   **训练时长**：总计训练 100k 步，耗时约 **4 天**。
*   **推理效率**：模型参数量仅为 **40M**，运行速度约为 **3 FPS**。相比之下，对比方法 Gen3C 拥有 7B 参数且速度仅 0.3 FPS。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了详尽的定量对比，并提供了大量的定性可视化结果。
*   **消融实验**：针对循环机制（Recurrence）、采样步长（Dilation factor）、深度平面数量（D）、输入视图数（V）以及分块大小（F）等关键超参数进行了系统的消融研究。
*   **客观性**：实验涵盖了从近距离到远距离的不同目标视角轨迹，并专门开辟了“仅动态区域”的评估维度，证明了模型在处理运动物体方面的真实能力，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **性能领先**：GRVS 在重建静态和动态区域的几何细节方面均显著优于现有的高斯泼溅（GS）方法和扩散模型。
*   **循环机制的必要性**：循环结构能有效结合全局（大采样步长）和局部（小采样步长）的时间上下文，显著提升 PSNR（约 1dB）。
*   **解耦优势**：动态 PSV 能够有效处理单目视频中的运动歧义，使模型在无需显式运动先验的情况下准确重建动态几何。

#### 7. 优点（亮点）
*   **高效且轻量**：相比扩散模型，参数量减少了两个数量级，推理速度快 10 倍，且无需针对新场景进行优化。
*   **异步映射能力**：支持“子弹时间”等自由视角渲染，不受输入视频帧率或同步限制。
*   **端到端学习**：不需要光流、深度图或运动掩码等外部预训练模块的辅助，仅靠 RGB 和相机参数即可训练。

#### 8. 不足与局限
*   **依赖相机参数**：模型性能高度依赖于输入相机位姿的准确性，在位姿估计误差较大的场景下可能失效。
*   **深度范围限制**：受限于平面扫描（PSV）的离散深度平面数量，对于极近或极远物体的重建精度可能受限。
*   **生成能力边界**：由于不是纯生成式模型，在处理大面积遮挡（Disocclusion）时，其补全能力可能弱于基于大规模预训练的扩散模型。

（完）
