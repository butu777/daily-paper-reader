---
title: "LightHarmony3D: Harmonizing Illumination and Shadows for Object Insertion in 3D Gaussian Splatting"
title_zh: LightHarmony3D：3D 高斯泼溅中物体插入的光照与阴影协调
authors: "Tianyu Huang, Zhenyang Ren, Zhenchen Wan, Jiyang Zheng, Wenjie Wang, Runnan Chen, Mingming Gong, Tongliang Liu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29209v1"
tags: ["query:3d"]
score: 9.0
evidence: 3DGS场景中网格插入的物理一致光照与阴影
tldr: 本文提出LightHarmony3D框架，旨在解决在3D高斯泼溅（3DGS）场景中插入网格对象时光照与阴影不一致的问题。该方法通过生成式模块单次前向传播即可预测插入位置的360° HDR环境贴图，避免了复杂的迭代优化。此外，研究者还推出了首个针对3DGS网格插入的专用基准测试。实验证明，该方法在保持多视角一致性的同时，实现了卓越的真实感和物理光影效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29209v1/fig-001.webp\", \"caption\": \"Fig. 3: Qualitative results on our LH3D synthetic dataset and real scenes from MipNeRF360. The inserted object is highlighted with a bounding box. As Mip-NeRF360 provides no insertion ground truth, we additionally show the original background for reference.\", \"page\": 10, \"index\": 1, \"width\": 718, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2603.29209v1/fig-002.webp\", \"caption\": \"Fig. 2: Overview of LightHarmony3D. Given multi-view input images, we reconstruct a hybrid 3DGS–mesh scene and render a base 360◦ panorama. To capture dominant light emitters, a fine-tuned diffusion model predicts bracketed underexposures, which are fused to reconstruct a HDR environment map via radiometric truncation. To illuminate the enclosed scene, we employ a ray-decoupled visibility formulation that renders the proxy mesh transparent to camera rays while preserving its physical shadow-catching properties. Finally, we perform physically based rendering of the inserted mesh and linearly composite the resulting physically grounded shading and cast shadows over the original 3DGS background to produce seamless, illuminationconsistent results.\", \"page\": 5, \"index\": 2, \"width\": 719, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2603.29209v1/fig-003.webp\", \"caption\": \"Fig. 4: Visual results of the ablation study on LH3D-Ku. Details are zoomed in within the bounding boxes.\", \"page\": 12, \"index\": 3, \"width\": 678, \"height\": 230}]"
motivation: 在3DGS场景中插入外部网格对象时，难以实现与原场景物理一致的光照和阴影效果。
method: 提出一种生成式模块，通过单次前向传播预测插入位置的360° HDR环境贴图，从而实现高效的物理着色。
result: 在多个真实世界数据集上达到了最先进的真实感水平，并保持了出色的多视角一致性。
conclusion: LightHarmony3D为3DGS场景中的物体插入提供了高效的光照协调方案，并通过新基准推动了该领域的发展。
---

## 摘要
3D 高斯泼溅 (3DGS) 能够实现场景几何和外观的高保真重建。基于这一能力，在重建的 3DGS 场景中插入外部网格物体，可为 AR/VR、虚拟置景和数字内容创作等沉浸式应用实现交互式编辑和内容增强。然而，为插入的网格实现物理一致的光照和阴影仍具挑战性，因为这需要准确的场景光照估计和多视图一致的渲染。为应对这一挑战，我们提出了 LightHarmony3D，这是一个用于 3DGS 场景中光照一致性网格插入的新型框架。我们方法的核心是所提出的生成式模块，它通过单次前向传递预测插入位置的完整 360° HDR 环境贴图。通过利用生成式先验而非迭代优化，我们的方法能够高效捕捉场景的主导光照，并为插入的网格实现基于物理的着色和阴影，同时保持多视图一致性。此外，我们引入了首个专门针对 3DGS 网格插入的基准测试，为评估光照一致性和照片级真实感提供了标准化的评估框架。在多个真实世界重建数据集上的广泛实验表明，LightHarmony3D 实现了最先进的真实感和多视图一致性。

## Abstract
3D Gaussian Splatting (3DGS) enables high-fidelity reconstruction of scene geometry and appearance. Building on this capability, inserting external mesh objects into reconstructed 3DGS scenes enables interactive editing and content augmentation for immersive applications such as AR/VR, virtual staging, and digital content creation. However, achieving physically consistent lighting and shadows for mesh insertion remains challenging, as it requires accurate scene illumination estimation and multi-view consistent rendering. To address this challenge, we present LightHarmony3D, a novel framework for illumination-consistent mesh insertion in 3DGS scenes. Central to our approach is our proposed generative module that predicts a full 360° HDR environment map at the insertion location via a single forward pass. By leveraging generative priors instead of iterative optimization, our method efficiently captures dominant scene illumination and enables physically grounded shading and shadows for inserted meshes while maintaining multi-view coherence. Furthermore, we introduce the first dedicated benchmark for mesh insertion in 3DGS, providing a standardized evaluation framework for assessing lighting consistency and photorealism. Extensive experiments across multiple real-world reconstruction datasets demonstrate that LightHarmony3D achieves state-of-the-art realism and multi-view consistency.

---

## 论文详细总结（自动生成）

以下是对论文《LightHarmony3D: Harmonizing Illumination and Shadows for Object Insertion in 3D Gaussian Splatting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在 3D 高斯泼溅（3DGS）重建的场景中，无缝且物理一致地插入外部 3D 网格（Mesh）物体。
*   **研究背景**：3DGS 在实时高保真渲染方面表现卓越，但在场景编辑（如插入新物体）时面临两大挑战：
    1.  **光照解耦难**：3DGS 将光照与材质属性隐式纠缠在视图相关的颜色中，难以提取准确的场景光照。
    2.  **物理交互缺失**：传统的 3DGS 缺乏显式几何，导致插入物体时难以产生准确的阴影投射和环境遮挡。
*   **研究动机**：现有的逆渲染方法计算昂贵且依赖精确几何，而基于 2D 生成的方法往往缺乏多视角一致性。本文旨在通过生成式先验与物理渲染的结合，实现高效、一致且真实的物体插入。

### 2. 方法论：核心思想与关键技术
LightHarmony3D 采用了一种结合“生成式光照估计”与“物理引导合成”的混合框架：
*   **混合场景重建**：利用 MILo 框架同时重建 3DGS 和一个简化的代理网格（Proxy Mesh），为后续的物理光线追踪提供几何基础。
*   **生成式环境光照估计 (GenEnvLighting)**：
    *   **核心思想**：将光照估计转化为“辐射截断”任务。
    *   **技术细节**：使用微调的扩散模型（基于 Flux.1 Kontext，配合 LoRA 和 DreamBooth 训练），输入 3DGS 渲染的普通曝光全景图（EV 0），预测其在极低曝光（如 EV -3）下的表现。这种方法能自动识别并分离出场景中的高能光源。
    *   **曝光融合**：通过多级曝光预测并进行亮度迭代合并，构建出 32 位浮点格式的 HDR 环境贴图。
*   **射线解耦可见性 (Ray-Decoupled Visibility)**：
    *   **解决痛点**：室内场景的代理网格通常是封闭的，会阻挡外部环境光。
    *   **算法流程**：在 PBR 引擎中自定义着色器，根据射线类型进行分类处理。**相机射线、透射射线和镜面射线**视网格为透明（允许光线进入）；而**阴影射线和漫反射射线**视网格为不透明（允许产生阴影和间接光交互）。
*   **PBR 引导的阴影合成**：
    *   在 PBR 引擎中分别渲染有无插入物体的场景，计算线性空间下的**阴影比例图 (Shadow Ratio Map)**。
    *   将该比例图应用于原始 3DGS 背景，实现物理真实的阴影叠加，避免了“双重阴影”伪影。

### 3. 实验设计
*   **数据集与基准 (Benchmark)**：
    *   **LH3D-Bench (自建)**：包含两个子集。**LH3D-Ku**（4个 Kujiale 室内场景）和 **LH3D-Blender**（7个场景 × 9个物体，共 315 张测试图）。这是首个专门为 3DGS 物体插入设计的配对基准。
    *   **Mip-NeRF360**：用于验证在真实世界、无标注场景下的泛化能力。
*   **对比方法**：
    *   **GIGS**：基于高斯的逆渲染方法。
    *   **GaussianEditor**：3DGS 场景编辑框架。
    *   **MV-CoLight**：多视角 2D 合成方法。
    *   **GaSLight**：并发研究，采用逐视图 HDR 预测。
*   **评估指标**：PSNR、SSIM、LPIPS（用于合成数据）；**VQAScore**（利用视觉语言模型进行无参考的真实感评估）。

### 4. 资源与算力
*   **数据准备**：使用了 800 多个来自 Poly Haven 的 HDR 全景图进行模型微调。
*   **基准制作成本**：每个场景平均需要约 12 小时的人工设计（物体放置、轨迹设计）和 **16 个 GPU 小时**的配对路径追踪渲染。
*   **训练细节**：文中未明确给出训练 GenEnvLighting 模块的具体 GPU 型号和总训练时长，但提到了使用 LoRA 进行低秩自适应训练，这通常意味着较低的显存占用和较快的训练速度。

### 5. 实验数量与充分性
*   **实验规模**：进行了数百组多视角渲染实验，涵盖了室内、室外、复杂材质（如金属、玻璃）和多种光照条件。
*   **消融实验**：针对“曝光融合”、“阴影比例计算”和“射线解耦着色器”分别进行了消融，验证了每个模块对最终真实感的贡献。
*   **客观性**：通过引入 VQAScore 这种基于对比提示词（正向 vs 负向）的自动化评估方式，减少了主观审美偏差，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：LightHarmony3D 在 PSNR、SSIM 等指标上显著优于现有方法，尤其在多视角一致性上表现突出。
*   **光照准确性**：生成式 HDR 模块能有效捕捉主导光源，产生的阴影方向和强度比逐视图预测方法更稳定。
*   **物理一致性**：射线解耦技术成功解决了 3DGS 场景中“封闭几何”与“外部光照”的矛盾，使得室内物体插入不再出现异常发黑的情况。

### 7. 优点（亮点）
*   **高效性**：通过单次前向传播预测 HDR 光照，避免了耗时的逐场景逆渲染优化。
*   **创新性**：射线解耦可见性公式是一个非常实用的创新，解决了神经渲染场景与传统 PBR 结合时的拓扑闭合问题。
*   **贡献度**：发布了 LH3D-Bench，填补了该领域缺乏高质量配对评估数据的空白。

### 8. 不足与局限
*   **几何依赖**：光照和阴影的质量高度依赖于 MILo 重建的代理网格质量。如果网格存在空洞或结构错误，会导致阴影断裂或光照泄露。
*   **间接光限制**：虽然考虑了漫反射交互，但对于极其复杂的多次反弹间接光（如强烈的颜色溢出效应），目前的处理仍显简化。
*   **计算开销**：尽管比逆渲染快，但引入 PBR 路径追踪环节使得其渲染速度仍慢于纯 2D 图像合成方法，难以达到完全的实时交互。

（完）
