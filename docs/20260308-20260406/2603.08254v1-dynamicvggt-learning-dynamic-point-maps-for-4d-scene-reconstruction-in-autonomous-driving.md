---
title: "DynamicVGGT: Learning Dynamic Point Maps for 4D Scene Reconstruction in Autonomous Driving"
title_zh: DynamicVGGT：面向自动驾驶 4D 场景重建的学习型动态点图
authors: "Zhuolin He, Jing Li, Guanghao Li, Xiaolei Chen, Jiacheng Tang, Siyang Zhang, Zhounan Jin, Feipeng Cai, Bin Li, Jian Pu, Jia Cai, Xiangyang Xue"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08254v1"
tags: ["query:3d"]
score: 10.0
evidence: 自动驾驶中的动态 4D 场景重建
tldr: 针对自动驾驶中动态场景重建的挑战，本文提出DynamicVGGT框架，将VGGT从静态3D感知扩展到动态4D重建。该方法通过在共享参考系下联合预测当前与未来点图来学习动态表示，并引入运动感知时间注意力（MTA）模块与动态3D高斯泼溅头，利用场景流监督预测高斯速度，实现了复杂驾驶场景下高效、鲁棒的前馈式4D动态重建。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08254v1/fig-001.webp\", \"caption\": \"Figure 5. Point map reconstruction. DynamicVGGT reconstructs denser, smoother, and more geometrically consistent point maps than VGGT, maintaining temporal coherence even under large viewpoint or scene changes. Zoom in for better view.\", \"page\": 7, \"index\": 1, \"width\": 483, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2603.08254v1/fig-002.webp\", \"caption\": \"Figure 2. Proposed DynamicVGGT training framework. Given a sequence of multi-view images {V1, V2, V3}, the model first encodes them using a pretrained DINOv2 backbone to extract patch tokens and camera tokens for each view, while motion tokens are initialized as learnable parameters that encode temporal priors. The patch and camera tokens are processed by the Alternating-Attention (AA) blocks to model intra-frame spatial geometry, whereas the Motion-aware Temporal Attention (MTA) blocks operate in parallel to model inter-frame temporal dependencies using the motion tokens. The resulting temporal features TA are then fed into a Dynamic 3D Gaussian Head (DGSHead) for dynamic 3DGS reconstruction and a Future Point Head for future point prediction.\", \"page\": 3, \"index\": 2, \"width\": 948, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2603.08254v1/fig-003.webp\", \"caption\": \"Figure 6. Scene Reconstruction and Novel View Synthesis. Given input frames 0, 2, and 4, DynamicVGGT reconstructs the corresponding scenes and synthesizes novel views for the next frame. For both KITTI and Waymo, multi-view inputs are used; for Waymo, we visualize the front-camera results due to limited view overlap. The model achieves high-quality reconstruction and realistic novel view generation across dynamic driving scenes.\", \"page\": 8, \"index\": 3, \"width\": 1002, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2603.08254v1/fig-004.webp\", \"caption\": \"Figure 3. Dynamic task formulation. We formulate dynamic point maps by designing two complementary tasks that model point-wise motion over time. The Future Point Head learns implicit motion through inter-frame point consistency, while the Dynamic 3D Gaussian Splatting Head provides explicit motion supervision via scene flow to refine dynamic geometry.\", \"page\": 4, \"index\": 4, \"width\": 404, \"height\": 394}]"
motivation: 现有的前馈3D模型在处理自动驾驶中显著的时间变化和物体运动时，难以捕捉复杂的动态场景几何。
method: 通过联合预测点图、引入运动感知时间注意力模块以及设计基于场景流监督的动态3D高斯泼溅头来建模点运动。
result: 在自动驾驶数据集上的实验证明，DynamicVGGT在重建精度上显著优于现有方法，展现了强大的动态场景建模能力。
conclusion: DynamicVGGT成功实现了从静态到动态前馈重建的跨越，为自动驾驶中的4D场景理解提供了高效且准确的解决方案。
---

## 摘要
由于显著的时间变化、移动物体以及复杂的场景动力学，自动驾驶中的动态场景重建仍然是一项基础性挑战。现有的前馈 3D 模型在静态重建中表现出强大的性能，但在捕捉动态运动方面仍面临困难。为了解决这些局限性，我们提出了 DynamicVGGT，这是一个将 VGGT 从静态 3D 感知扩展到动态 4D 重建的统一前馈框架。我们的目标是以动态且时间相干的方式在前馈 3D 模型中建模点运动。为此，我们在共享参考坐标系内联合预测当前和未来的点图，使模型能够通过时间对应关系隐式学习动态点表示。为了高效捕捉时间依赖性，我们引入了运动感知时间注意力（MTA）模块来学习运动连续性。此外，我们设计了一个动态 3D 高斯泼溅（Gaussian Splatting）头，通过在场景流监督下使用可学习的运动令牌预测高斯速度，从而显式建模点运动。它通过连续的 3D 高斯优化来细化动态几何。在自动驾驶数据集上的广泛实验表明，DynamicVGGT 在重建精度上显著优于现有方法，在复杂驾驶场景下实现了鲁棒的前馈 4D 动态场景重建。

## Abstract
Dynamic scene reconstruction in autonomous driving remains a fundamental challenge due to significant temporal variations, moving objects, and complex scene dynamics. Existing feed-forward 3D models have demonstrated strong performance in static reconstruction but still struggle to capture dynamic motion. To address these limitations, we propose DynamicVGGT, a unified feed-forward framework that extends VGGT from static 3D perception to dynamic 4D reconstruction. Our goal is to model point motion within feed-forward 3D models in a dynamic and temporally coherent manner. To this end, we jointly predict the current and future point maps within a shared reference coordinate system, allowing the model to implicitly learn dynamic point representations through temporal correspondence. To efficiently capture temporal dependencies, we introduce a Motion-aware Temporal Attention (MTA) module that learns motion continuity. Furthermore, we design a Dynamic 3D Gaussian Splatting Head that explicitly models point motion by predicting Gaussian velocities using learnable motion tokens under scene flow supervision. It refines dynamic geometry through continuous 3D Gaussian optimization. Extensive experiments on autonomous driving datasets demonstrate that DynamicVGGT significantly outperforms existing methods in reconstruction accuracy, achieving robust feed-forward 4D dynamic scene reconstruction under complex driving scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：DynamicVGGT - 面向自动驾驶的动态 4D 场景重建

#### 1. 核心问题与研究动机
在自动驾驶领域，动态场景的 4D 重建（3D 几何 + 时间维度）至关重要，但面临巨大挑战：场景中存在大量移动物体、显著的时间变化以及复杂的长程依赖。现有的前馈（Feed-forward）3D 模型（如 VGGT、DUSt3R）虽然在静态场景重建中表现优异，但在处理动态运动时往往难以保持几何准确性和时间一致性。此外，现有的 3D 基础模型在处理高噪声、稀疏深度的真实驾驶数据时容易出现性能退化。

**DynamicVGGT** 的研究动机在于：构建一个统一的前馈框架，将静态 3D 感知扩展到动态 4D 重建，通过联合建模几何与运动，实现高效、鲁棒的动态场景恢复。

#### 2. 方法论核心思想与技术细节
DynamicVGGT 基于 VGGT 架构，引入了动态点图（Dynamic Point Map, DPM）的概念，核心技术包括：

*   **运动感知时间注意力（MTA）模块**：
    *   在原有的空间注意力分支旁并行引入 MTA。
    *   使用**可学习的运动令牌（Motion Tokens）**来动态编码帧间运动信息。
    *   通过时间注意力机制捕捉运动连续性，而不破坏预训练模型的空间几何先验。
*   **未来点预测头（Future Point Head, FPH）**：
    *   模型在共享参考坐标系下同时预测当前帧和未来帧的点图。
    *   引入时间一致性正则化损失（$L_{temp}$），通过约束预测位移与真实位移的一致性，让模型隐式学习点级别的运动。
*   **动态 3D 高斯泼溅头（DGSHead）**：
    *   将几何特征与图像外观特征融合，初始化 3D 高斯基元。
    *   预测每个高斯基元的速度向量（Velocity），并在**场景流（Scene Flow）**的显式监督下进行优化。
    *   支持连续的 3D 高斯优化，提升动态几何的精细度。
*   **阶段式训练策略**：
    *   **第一阶段**：在合成数据集（Virtual KITTI, MVS-Synth）上训练，学习鲁棒的几何先验和时间一致性。
    *   **第二阶段**：在真实数据集（Waymo）上微调，引入深度蒸馏策略（以第一阶段预测的深度为教师信号），解决真实 LiDAR 数据稀疏导致的高斯优化不稳定问题。

#### 3. 实验设计
*   **数据集**：
    *   训练：Waymo Open Dataset、Virtual KITTI、MVS-Synth。
    *   评估：Waymo 验证集、KITTI（用于点图和深度评估）、NYU-v2（用于跨领域泛化评估）。
*   **Benchmark 与对比方法**：
    *   **点图重建**：对比 VGGT、StreamVGGT。
    *   **4D 场景重建**：对比 3DGS、DeformableGS（优化类方法）以及 GS-LRM、STORM（前馈类方法）。
    *   **深度估计**：对比 DUSt3R、MASt3R、MonST3R、VGGT 等。
*   **评估指标**：准确度（Acc.）、完整度（Comp.）、法线一致性（NC）、PSNR、SSIM、Abs Rel 等。

#### 4. 资源与算力
*   **模型规模**：总参数量约为 14 亿（1.4B），其中 MTA 模块包含 12 层。
*   **训练细节**：
    *   初始化自预训练的 VGGT 权重，微调约 8 亿（800M）参数。
    *   第一阶段训练 10 个 epoch，第二阶段训练 50 个 epoch。
    *   采用动态 Batch Size 策略（每批次处理 18 张图像）。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长，但从参数量和数据集规模推断，该模型需要大规模 GPU 集群支持。

#### 5. 实验数量与充分性
*   **实验覆盖面**：论文涵盖了点图重建、4D 场景重建、单目/多视图深度估计等多个任务，并在室外（KITTI/Waymo）和室内（NYU-v2）场景均进行了验证。
*   **消融实验**：针对 MTA 模块、未来点预测头（FPH）和动态高斯头（DGSHead）分别进行了消融分析，验证了各组件对准确度和完整度的提升作用。
*   **客观性**：对比了当前最先进的多种前馈模型和基于优化的模型，实验设计较为全面，通过多维度指标展示了模型在动态区域重建上的优势。

#### 6. 主要结论与发现
*   **性能领先**：DynamicVGGT 在 Waymo 和 KITTI 数据集上的点图重建精度显著优于 VGGT 和 StreamVGGT。
*   **动态建模能力**：通过显式（场景流）和隐式（未来点预测）的联合监督，模型能有效捕捉物体的运动轨迹，生成的点图更稠密、更平滑。
*   **泛化性**：尽管主要针对自动驾驶设计，但模型在室内数据集（NYU-v2）上也表现出极强的深度估计泛化能力。
*   **前馈效率**：相比需要逐场景优化的方法（如 DeformableGS），DynamicVGGT 作为前馈模型，在推理速度和通用性上具有明显优势。

#### 7. 优点与亮点
*   **统一框架**：成功将静态 3D 基础模型扩展到 4D，实现了几何、运动、外观的联合学习。
*   **运动令牌设计**：MTA 模块通过运动令牌解耦了空间和时间特征，既保留了强大的空间先验，又增强了时间相干性。
*   **深度蒸馏策略**：巧妙地解决了真实自动驾驶场景中 LiDAR 监督信号过于稀疏的问题，提升了高斯渲染的质量。

#### 8. 不足与局限
*   **算力需求高**：1.4B 的参数量对于实时部署（尤其是车载端）仍有较大挑战。
*   **依赖场景流监督**：动态高斯头的训练依赖于场景流（Scene Flow）标注，而高质量的场景流数据在现实中获取成本较高。
*   **视角重叠限制**：在 Waymo 等视角重叠较小的场景下，多视图融合的效果仍受限于输入图像的覆盖范围。
*   **长程预测风险**：目前主要关注短时（相邻帧）的动态重建，对于极长序列或剧烈运动场景的鲁棒性尚待进一步验证。

（完）
