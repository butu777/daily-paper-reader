---
title: Landmark Guided 4D Facial Expression Generation
title_zh: 关键点引导的 4D 面部表情生成
authors: "Xin Lu, Zhengda Lu, Yiqun Wang, Jun Xiao"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10337v1"
tags: ["query:3d"]
score: 9.0
evidence: 4D面部表情生成与合成
tldr: 本研究提出了一种名为LM-4DGAN的生成模型，旨在通过中性面部关键点引导合成4D面部表情。针对现有方法在处理不同身份时鲁棒性不足的问题，该模型在WGAN基础上引入了身份判别器、关键点自编码器以及交叉注意力机制。这一方法不仅增强了身份一致性，还实现了更具针对性的表情位移生成，为高质量4D表情合成提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.10337v1/fig-001.webp\", \"caption\": \"Figure 2: Qualitative results. The first line is the ground truth of the expression mouth-extreme with different identities. The second line is the generating results of Motion3D while the third line is the generating results of our network.\", \"page\": 2, \"index\": 1, \"width\": 506, \"height\": 252}]"
motivation: 现有的4D表情生成方法在面对不同身份特征时缺乏鲁棒性，难以生成与特定身份高度契合的表情序列。
method: 提出了LM-4DGAN模型，结合中性关键点引导、身份判别器、关键点自编码器以及带有交叉注意力机制的位移解码器。
result: 该方法能够利用中性关键点有效引导表情生成，并在保持身份特征一致性的同时提升了生成质量。
conclusion: 通过引入关键点引导和身份保持机制，LM-4DGAN显著提高了4D面部表情合成的身份鲁棒性和表现力。
---

## 摘要
在本文中，我们提出了一种生成模型，该模型学习利用中性关键点合成 4D 面部表情。现有工作主要集中在由表情标签、语音等引导的序列生成，但它们对不同身份的变化缺乏鲁棒性。我们的 LM-4DGAN 利用中性关键点引导面部表情生成，同时在基础 WGAN 中加入了身份判别器和关键点自编码器，以实现更好的身份鲁棒性。此外，我们在现有的位移解码器中加入了交叉注意力机制，使其适用于给定的身份。

## Abstract
In this paper, we proposed a generative model that learns to synthesize the 4D facial expression with the neutral landmark. Existing works mainly focus on the generation of sequences guided by expression labels, speech, etc, while they are not robust to the change of different identities. Our LM-4DGAN utilizes neutral landmarks to guide the facial expression generation while adding an identity discriminator and a landmark autoencoder to the basic WGAN for achieving better identity robustness. Furthermore, we add a cross-attention mechanism to the existing displacement decoder which is suitable for the given identity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Landmark Guided 4D Facial Expression Generation》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何根据给定的中性面部关键点（Neutral Landmark）生成逼真且具有身份鲁棒性的 4D（3D 序列）面部表情动画。
*   **研究动机**：
    *   **身份鲁棒性不足**：现有方法（如基于表情标签或语音引导）在处理不同面部身份时表现不佳，生成的位移往往无法完美契合特定个体的面部特征。
    *   **数据获取困难**：高质量 4D 面部扫描数据稀缺，导致学习动态细节具有挑战性。
    *   **灵活性限制**：现有模型（如 Motion3DGAN）通常只能生成固定长度的序列，难以适应多变的动画需求。

### 2. 方法论
该研究提出了一种名为 **LM-4DGAN** 的框架，其核心流程如下：
*   **粗到精的生成架构**：借鉴 GANimator 的思想，采用一系列 LM-4DGAN 模块。第一层以中性关键点和随机噪声为输入，后续层以前一层的输出为基础，从而支持生成变长的表情序列。
*   **关键点自编码器（Landmark AE）**：由于 3D 空间中面部关键点具有稀疏性，直接学习其变形较为困难。模型通过自编码器对关键点进行编码，在隐空间中学习变形。
*   **双判别器设计**：
    *   **身份判别器 ($D_{iden}$)**：确保生成的表情序列保持输入个体的身份特征。
    *   **时间一致性判别器 ($D_{coh}$)**：通过计算相邻帧之间的变形差异（diff），确保动作的平滑性和连贯性。
*   **带交叉注意力的位移解码器**：在将关键点位移（Sparse）转换为稠密网格顶点位移（Dense）的过程中，引入了**交叉注意力机制（Cross-attention）**，将中性关键点信息与位移特征结合，使解码出的面部细节更符合特定身份。

### 3. 实验设计
*   **数据集**：主要在 **CoMA (Cognitive Model of the Anthropomorphic Shape)** 数据集上进行训练和评估。
*   **基准测试（Benchmark）**：使用每顶点重建误差（Per-vertex reconstruction error，单位：0.1mm）作为量化指标。
*   **对比方法**：主要与 **Motion3D** (Otberdout et al. 2022) 进行对比。
*   **消融实验**：分别验证了时间一致性损失 ($L_{coh}$)、身份损失 ($L_{iden}$)、自编码器 (AE) 以及交叉注意力机制 (Atten) 对最终结果的影响。

### 4. 资源与算力
*   **文中说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。考虑到 4D 数据的处理量，通常需要高性能显卡支持，但由于该论文为 SIGGRAPH Asia Poster 简短论文（2页），技术细节有所简化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了定性对比（视觉效果图）。
    *   进行了定量对比（重建误差表）。
    *   进行了 5 组消融实验，涵盖了模型的所有核心组件。
*   **充分性评价**：实验设计较为标准，涵盖了从关键点到稠密网格的完整链路。但由于仅在 CoMA 单一数据集上进行测试，且对比方法较少（仅 Motion3D），其实验的广泛适用性仍有待在更多样化的数据集（如 BFM 或其他大规模 4D 库）上进一步验证。

### 6. 主要结论与发现
*   **精度提升**：LM-4DGAN 在关键点生成和网格顶点生成上的误差均低于 Motion3D（关键点误差从 0.750 降至 0.562，网格误差从 5.288 降至 4.324）。
*   **身份保持**：引入身份判别器和交叉注意力机制显著增强了模型对不同面部形状的适应能力。
*   **变长生成**：证明了粗到精的架构可以有效处理不同长度的动画生成任务。

### 7. 优点（亮点）
*   **身份感知**：通过中性关键点引导和交叉注意力，解决了表情生成中“千人一面”的违和感。
*   **时序稳定性**：专门的时间一致性判别器有效减少了 4D 序列中的闪烁或抖动现象。
*   **架构灵活**：支持变长序列生成，比固定长度的 GAN 模型更具实用价值。

### 8. 不足与局限
*   **数据依赖性**：目前仅在 CoMA 数据集验证，该数据集样本量相对有限，可能存在过拟合风险。
*   **细节表现**：虽然比前人有进步，但在极端表情下的局部皮肤褶皱等精细解剖学细节仍有提升空间。
*   **评估维度**：缺乏用户主观评价实验（User Study），仅靠几何重建误差难以完全衡量表情的“生动性”。

（完）
