---
title: "UniDA3D: A Unified Domain-Adaptive Framework for Multi-View 3D Object Detection"
title_zh: UniDA3D：一种用于多视图3D目标检测的统一域自适应框架
authors: "Hongjing Wu, Cheng Chi, Jinlin Wu, Yanzhao Su, Zhen Lei, Wenqi Ren"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27995v1"
tags: ["query:adverseweather"]
score: 9.0
evidence: 针对夜间、雨天和雾天场景的领域自适应3D目标检测
tldr: 针对多视图3D目标检测在夜间、雨天和雾天等恶劣天气下性能大幅下降的问题，本文提出UniDA3D统一领域自适应框架。该框架通过查询引导领域差异缓解模块（QDDM）和教师-学生训练机制，在单一训练过程中实现多目标领域的特征对齐与一致性学习，显著提升了全天候环境下的3D感知鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27995v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON WITH STATE-OF-THE-ART METHODS ON THE NUSCENES-NIGHT VAL SET. MODELS ARE GROUPED BY BACKBONE (RESNET AND V2-99) FOR A FAIR COMPARATIVE ANALYSIS. BOLD INDICATES THE BEST RESULT.\", \"page\": 8, \"index\": 1, \"width\": 1062, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2603.27995v1/fig-002.webp\", \"caption\": \"Fig. 2. Details of the query-guided domain discrepancy mitigation (QDDM) module. QDDM leverages 3D object queries to align source and target features at the object level. It combines adversarial training and contrastive learning with dynamically updated global representation, promoting robust and consistent cross-domain 3D object detection.\", \"page\": 6, \"index\": 2, \"width\": 1062, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2603.27995v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of the proposed UniDA3D. UniDA3D first synthesizes nighttime, rainy, and foggy multi-view images to assist training. A domain-adaptive teacher–student self-training pipeline is employed to transfer knowledge from the source domain to the target domains. UniDA3D is further equipped with a query-guided domain discrepancy mitigation (QDDM) module to align object-level features across domains at both batch and global scales.\", \"page\": 4, \"index\": 3, \"width\": 1062, \"height\": 558}]"
motivation: 现有的多视图3D检测器在复杂天气下表现不佳，且缺乏能同时处理多种恶劣环境的统一领域自适应方案。
method: 提出QDDM模块进行批次和全局层面的特征对齐，并结合指数移动平均教师模型与动态伪标签技术增强一致性学习。
result: 在nuScenes合成的夜间、雨天和雾天基准测试中，UniDA3D在mAP和NDS指标上均显著优于现有的SOTA纯视觉检测器。
conclusion: UniDA3D通过统一的训练流程实现了高效且鲁棒的全天候3D目标检测，有效解决了跨领域环境下的感知退化问题。
---

## 摘要
纯摄像头3D目标检测对于自动驾驶至关重要，它为基于激光雷达（LiDAR）的方法提供了一种具有成本效益的替代方案。特别是，多视图3D目标检测因其在性能和成本之间的良好权衡而成为一个极具前景的方向。然而，现有方法在夜间、雾天和雨天等复杂环境条件下往往会出现显著的性能下降，这主要是因为它们依赖于大多在理想条件下采集的训练数据。为了应对这一挑战，我们提出了 UniDA3D，这是一种统一的域自适应多视图3D目标检测器，旨在实现多种不利条件下的鲁棒感知。UniDA3D 将夜间、雨天和雾天场景建模为一个统一的多目标域自适应问题，并利用一种新颖的查询引导域差异缓解（QDDM）模块，通过以查询为中心的对抗学习和对比学习，在批次和全局水平上对齐源域与目标域之间的目标特征。此外，我们引入了一种域自适应教师-学生训练流水线，采用指数移动平均（EMA）教师和动态更新的高质量伪标签，以增强一致性学习并抑制无标签目标域中的背景噪声。与以往需要针对每种条件进行单独训练的方法不同，UniDA3D 在多个域上执行单一的统一训练过程，从而实现了鲁棒的全天候3D感知。在通过从 nuScenes 生成夜间、雨天和雾天对应版本（nuScenes-Night、nuScenes-Rain 和 nuScenes-Haze）构建的合成多视图3D基准测试中，UniDA3D 在极端条件下始终优于最先进的纯摄像头多视图3D检测器，在保持实时推理效率的同时，在 mAP 和 NDS 指标上取得了显著提升。

## Abstract
Camera-only 3D object detection is critical for autonomous driving, offering a cost-effective alternative to LiDAR based methods. In particular, multi-view 3D object detection has emerged as a promising direction due to its balanced trade-off between performance and cost. However, existing methods often suffer significant performance degradation under complex environmental conditions such as nighttime, fog, and rain, primarily due to their reliance on training data collected mostly in ideal conditions. To address this challenge, we propose UniDA3D, a unified domain-adaptive multi-view 3D object detector designed for robust perception under diverse adverse conditions. UniDA3D formulates nighttime, rainy, and foggy scenes as a unified multi target domain adaptation problem and leverages a novel query guided domain discrepancy mitigation (QDDM) module to align object features between source and target domains at both batch and global levels via query-centric adversarial and contrastive learning. Furthermore, we introduce a domain-adaptive teacher student training pipeline with an exponential-moving-average teacher and dynamically updated high-quality pseudo labels to enhance consistency learning and suppress background noise in unlabeled target domains. In contrast to prior approaches that require separate training for each condition, UniDA3D performs a single unified training process across multiple domains, enabling robust all-weather 3D perception. On a synthesized multi-view 3D benchmark constructed by generating nighttime, rainy, and foggy counterparts from nuScenes (nuScenes-Night, nuScenes-Rain, and nuScenes-Haze), UniDA3D consistently outperforms state of-the-art camera-only multi-view 3D detectors under extreme conditions, achieving substantial gains in mAP and NDS while maintaining real-time inference efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《UniDA3D: A Unified Domain-Adaptive Framework for Multi-View 3D Object Detection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的纯摄像头（Camera-only）多视图3D目标检测器在理想天气下表现优异，但在**夜间、雨天、雾天**等恶劣环境下性能会剧烈下降。
*   **研究动机**：
    *   **环境鲁棒性缺失**：现有模型过度依赖理想光照和天气下的训练数据。
    *   **域偏移复杂性**：不同恶劣天气带来的干扰不同（如夜间低光、雾天对比度低、雨天遮挡），单一的域自适应策略难以同时处理。
    *   **标注成本高昂**：在极端天气下收集并精确标注多视图3D数据极其困难。
*   **整体含义**：提出一个统一的框架 UniDA3D，通过单一的训练过程（而非为每种天气单独建模），实现全天候、鲁棒的多视图3D感知。

### 2. 论文提出的方法论
UniDA3D 结合了数据合成、教师-学生自学习和查询引导的特征对齐技术：
*   **跨域多视图图像合成**：
    *   使用 **UNIT** 生成夜间图像。
    *   基于 **Koschmieder 定律**和深度估计（Depth Anything）合成物理真实的雾天图像。
    *   通过噪声生成、运动模糊和图像融合三阶段流水线合成雨天图像。
*   **域自适应教师-学生流水线**：
    *   **学生模型**：同时学习带标签的源域（晴天）数据和带伪标签的目标域（恶劣天气）数据。
    *   **教师模型**：通过指数移动平均（EMA）从学生模型更新，为目标域生成动态、高置信度的伪标签。
*   **查询引导领域差异缓解模块（QDDM）**：
    *   **核心思想**：利用 3D Object Queries 作为语义锚点，在目标级别而非全图级别进行对齐，从而抑制背景噪声。
    *   **批次级对抗学习**：通过梯度反转层（GRL）和域判别器，使源域和目标域的类中心特征在表示空间上不可区分。
    *   **全局对比学习**：维护一个全局类别内存库，通过动态权重更新（缓解长尾类别偏差），强制要求当前批次的特征与全局语义特征保持一致。

### 3. 实验设计
*   **数据集与场景**：基于 **nuScenes** 原始数据集，合成了三个子集：**nuScenes-Night**、**nuScenes-Rain** 和 **nuScenes-Haze**。
*   **Benchmark**：采用 nuScenes 官方评估协议，核心指标为 **mAP**（平均精度）和 **NDS**（nuScenes 检测得分），以及五个 TP 误差指标（平移、缩放、方向、速度、属性）。
*   **对比方法**：涵盖了主流的 BEV 架构（如 BEVDet, BEVFormer, BEVDepth, BEVStereo）和 Query 架构（如 PETR, StreamPETR, SOLOFusion）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   使用 ResNet-50 主干网络时训练 **60 epoch**。
    *   使用 V2-99 主干网络时训练 **24 epoch**。
*   **软件框架**：基于 PyTorch 1.11.0 和 StreamPETR 框架开发。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三种恶劣天气（夜、雨、雾）下分别进行了大规模对比实验。
    *   使用了两种不同规模的主干网络（ResNet-50, V2-99）验证泛化性。
    *   **消融实验**：针对 EMA 比例、伪标签过滤阈值、QDDM 模块各组件的有效性进行了详细测试。
    *   **可视化实验**：提供了 t-SNE 特征分布可视化，直观展示了域对齐效果。
*   **客观性与公平性**：所有基线模型均在相同的数据组合和训练协议下重新训练，确保了对比的公正性。实验涵盖了从感知精度到推理效率（FPS）的全方位评估。

### 6. 主要结论与发现
*   **性能领先**：UniDA3D 在所有恶劣天气下的表现均显著优于现有 SOTA 方法。例如在夜间场景下，相比 StreamPETR，mAP 提升了约 18% 以上。
*   **统一训练的优势**：通过单一模型同时处理多种天气，比针对每种天气单独训练模型具有更好的参数效率和正则化效果。
*   **Query 对齐的有效性**：实验证明在 3D 查询层面进行对齐比在 2D 图像特征图层面对齐更能有效处理 3D 空间中的几何偏移。
*   **实时性**：由于 QDDM 和教师模型仅在训练阶段使用，UniDA3D 在推理时保持了与基准模型相同的实时速度。

### 7. 优点（亮点）
*   **Query-centric 视角**：巧妙地利用 Transformer 架构中的 Object Queries 作为对齐目标，避开了复杂的背景干扰。
*   **全局语义一致性**：引入全局类别表示和动态频率更新，有效解决了跨域场景下常见的长尾类别识别难问题。
*   **实用性强**：无需额外的传感器（如雷达或热成像），仅靠摄像头即可提升鲁棒性，且不增加推理开销。

### 8. 不足与局限
*   **Sim-to-Real 差距**：虽然使用了物理建模合成数据，但合成图像与真实的极端天气传感器伪影（如镜头水渍、传感器噪声）之间仍存在差距。
*   **复合环境挑战**：论文主要针对单一恶劣天气（如单纯的雾或雨），对于“深夜+暴雨”等复合极端条件的感知能力尚待进一步验证。
*   **依赖伪标签质量**：尽管有阈值过滤，但在极度恶劣的情况下，教师模型若产生持续性偏差，可能会导致学生模型陷入确认偏差（Confirmation Bias）。

（完）
