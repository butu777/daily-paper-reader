---
title: Unleashing Guidance Without Classifiers for Human-Object Interaction Animation
title_zh: 释放无分类器引导在人物-物体交互动画中的潜力
authors: "Ziyin Wang, Sirui Xu, Chuan Guo, Bing Zhou, Jiangshan Gong, Jian Wang, Yu-Xiong Wang, Liang-Yan Gui"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25734v1"
tags: ["query:wm"]
score: 9.0
evidence: 为交互动画建模动态人体动作和物体几何
tldr: 针对人机交互（HOI）动画生成中过度依赖手工设计接触先验的问题，本文提出了 LIGHT 框架。该方法基于扩散强制（diffusion forcing），通过为不同模态分配异步去噪进度，使较清晰的组件通过交叉注意力自发引导较模糊的组件，从而实现无需辅助分类器的接触感知引导。结合合成几何增强训练，该方法在提升接触保真度的同时，显著增强了对未知物体和任务的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25734v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of LIGHT. Left: Training. We form different modalities, e.g., body, hand, and object, each diffused with its own noise level. After adding modal-wise and frame-wise rotary positional encodings, the tokens are processed by a shared Transformer decoder and an MLP head to predict clean motion. Right: Inference. We compare a uniform schedule that denoises all modalities synchronously with a staged schedule that keeps one modality cleaner from the uniform run.\", \"page\": 4, \"index\": 1, \"width\": 826, \"height\": 490}]"
motivation: 现有人机交互生成方法通常依赖复杂的手工约束或先验，难以应对多样的物体几何形状和动态交互。
method: 提出一种基于去噪进度的异步引导机制，通过不同模态间的噪声水平差异实现数据驱动的自发引导。
result: 实验证明该方法在接触保真度、动画真实感以及对新物体的泛化性能上均优于传统的无分类器引导方法。
conclusion: 这种基于去噪进度的引导范式为实现高质量、强泛化的人机交互动画生成提供了一种有效的数据驱动替代方案。
---

## 摘要
生成逼真的人物-物体交互（HOI）动画仍然具有挑战性，因为它需要对动态人体动作和多样的物体几何形状进行联合建模。先前的基于扩散的方法通常依赖于手工设计的接触先验或人为施加的运动学约束，以提高接触质量。我们提出了 LIGHT，这是一种数据驱动的替代方案，其中引导源自去噪步调（denoising pace）本身，从而减少了对手工设计先验的依赖。基于扩散强迫（diffusion forcing），我们将表示分解为特定模态的组件，并为不同组件分配具有异步去噪计划的个性化噪声水平。在这种范式中，较清晰的组件通过交叉注意力引导噪声较大的组件，从而在无需辅助分类器的情况下产生引导。我们发现这种数据驱动的引导本质上具有接触感知能力，并且当训练通过广泛的合成物体几何形状进行增强时，这种引导可以得到进一步加强，从而促进接触语义对几何多样性的不变性。大量实验表明，与传统的无分类器引导（classifier-free guidance）相比，步调诱导的引导能更有效地体现接触先验的优势，同时实现更高的接触保真度、更逼真的 HOI 生成，以及对未知物体和任务更强的泛化能力。

## Abstract
Generating realistic human-object interaction (HOI) animations remains challenging because it requires jointly modeling dynamic human actions and diverse object geometries. Prior diffusion-based approaches often rely on hand-crafted contact priors or human-imposed kinematic constraints to improve contact quality. We propose LIGHT, a data-driven alternative in which guidance emerges from the denoising pace itself, reducing dependence on manually designed priors. Building on diffusion forcing, we factor the representation into modality-specific components and assign individualized noise levels with asynchronous denoising schedules. In this paradigm, cleaner components guide noisier ones through cross-attention, yielding guidance without auxiliary classifiers. We find that this data-driven guidance is inherently contact-aware, and can be enhanced when training is augmented with a broad spectrum of synthetic object geometries, encouraging invariance of contact semantics to geometric diversity. Extensive experiments show that pace-induced guidance more effectively mirrors the benefits of contact priors than conventional classifier-free guidance, while achieving higher contact fidelity, more realistic HOI generation, and stronger generalization to unseen objects and tasks.

---

## 论文详细总结（自动生成）

### 论文总结：Unleashing Guidance Without Classifiers for Human-Object Interaction Animation (LIGHT)

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖手工设计的物理约束（如反向动力学 IK）或外部预训练分类器（如接触预测器）的情况下，生成逼真且具有高接触保真度的人物-物体交互（HOI）动画。
*   **研究背景**：现有的扩散模型在生成 HOI 动画时，常出现手部悬空、物体穿模或滑动等不自然现象。传统的“无分类器引导”（CFG）虽能提升全局一致性，但对精细的局部接触控制力不足；而“分类器引导”则需要额外训练复杂的辅助模型，泛化性差且设计繁琐。

#### 2. 方法论：LIGHT 框架
*   **核心思想**：利用“去噪步调”（Denoising Pace）的差异产生隐式引导。通过为人体、手部、物体等不同模态分配不同的噪声水平，让“更清晰”的模态在去噪过程中通过交叉注意力机制自然地引导“噪声更大”的模态。
*   **关键技术细节**：
    *   **模态分解（Token Separation）**：将交互序列分解为人体躯干、手部和物体三个独立的 Token 组。
    *   **异步去噪调度（Asynchronous Schedules）**：在推理时运行两个分支：一个是所有模态同步去噪的“均匀分支”；另一个是某一模态（如人体）保持领先、另一模态（如物体）相对滞后的“阶段性分支”。
    *   **步调诱导引导（Pace-induced Guidance）**：通过对比上述两个分支的预测差异，形成一种类似于 CFG 但作用于模态间协调的“软引导”信号。
    *   **接触感知形状谱增强（Shape-Spectrum Augmentation）**：利用优化算法将原始动作重定向到具有相似语义但几何形状不同的新物体上，训练模型识别“接触语义”在几何变化下的不变性。

#### 3. 实验设计
*   **数据集**：主要在 **InterAct**（大规模全方位交互数据集）上进行，并消融分析了其子集 **BEHAVE** 和 **OMOMO**。
*   **Benchmark 与对比方法**：
    *   对比了四种最先进的基准：**HOI-Diff**（基于分类器引导）、**CHOIS**、**InterDiff**（基于物理信息扩散）和 **Text2HOI**。
    *   评估指标包括：R-Precision（文本对齐）、FID（生成质量）、多样性、脚部滑动率（FSR）、穿模率（Pene）以及接触准确率（Contact Ratio/F1）。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU**。
*   **训练时长**：在单台 A100 上，模型约在 **24 小时**内收敛。
*   **推理效率**：对于 64 个样本（每样本 300 帧）的批处理，带引导的推理耗时约 72 秒。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量对比（表 1、D、E）和定性可视化。
*   **消融实验**：涵盖了模态分离策略、引导权重 $\omega_2$、去噪偏移量 $\delta$、数据增强效果以及不同模态组合（如人体引导物体 vs 物体引导人体）的影响。
*   **泛化性测试**：专门针对“未见过的物体类别”进行了跨类别泛化实验。
*   **主观评价**：组织了 15 人的用户研究（User Study），对比了基准方法和引导效果。
*   **评价**：实验设计非常充分且客观，通过多维度指标和跨数据集验证，证明了该数据驱动方法的鲁棒性。

#### 6. 主要结论与发现
*   **引导本质**：LIGHT 产生的引导方向与“减少穿模”的梯度方向高度相关，证明了异步去噪能自动捕捉物理接触约束。
*   **性能领先**：在接触保真度和文本对齐度上显著优于依赖手工先验的方法。
*   **增强价值**：形状谱增强显著提升了模型对新物体的处理能力，使其能够处理训练集中未出现的几何结构。

#### 7. 优点与亮点
*   **无需显式先验**：摆脱了对 IK 约束或复杂物理模拟器的依赖，实现纯数据驱动的物理感知。
*   **优雅的理论扩展**：将“扩散强制”（Diffusion Forcing）理论巧妙地转化为一种新型的引导范式。
*   **精细化控制**：通过手部与身体的分离建模，显著改善了抓取等精细动作的质量。

#### 8. 不足与局限
*   **计算开销**：由于推理时需要运行多个分支并进行多次前向计算，推理速度慢于不带引导的基础扩散模型（约慢 4-5 倍）。
*   **场景限制**：目前仅针对“单人-单物”交互，尚未扩展到复杂的多物体交互或静态环境约束（如墙壁、地板障碍物）。
*   **物理极限**：虽然减少了穿模，但在极端动态交互下，仍可能无法完全替代严格的物理引擎模拟。

（完）
