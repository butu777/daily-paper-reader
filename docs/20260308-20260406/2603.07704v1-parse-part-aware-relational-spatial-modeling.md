---
title: "PARSE: Part-Aware Relational Spatial Modeling"
title_zh: PARSE：部件感知的关系空间建模
authors: "Yinuo Bai, Peijun Xu, Kuixiang Shao, Yuyang Jiao, Jingxuan Zhang, Kaixin Yao, Jiayuan Gu, Jingyi Yu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07704v1"
tags: ["query:wm"]
score: 10.0
evidence: 物理接地的场景配置的部件级空间建模
tldr: 针对现有空间建模在物体层级过于粗糙、导致布局歧义和物理不一致的问题，本文提出PARSE框架。该框架通过“部件中心装配图（PAG）”显式建模物体部件间的几何关系，并利用空间配置求解器生成无碰撞且物理合理的3D场景。此外，研究者构建了包含1万个场景的PARSE-10K数据集。实验表明，PARSE显著提升了多模态大模型的空间推理能力及3D场景生成的物理真实性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07704v1/fig-001.webp\", \"caption\": \"Figure 4. Gallery of PARSE-10K\", \"page\": 6, \"index\": 1, \"width\": 1020, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2603.07704v1/fig-002.webp\", \"caption\": \"Figure 2. An illustrative example of our Part-centric Assembly Graph (PAG). The left panel shows a PARSE-generated scene, where several regions governed by spatial relation constraints are highlighted. The middle panel presents the corresponding PAG: object nodes are visualized as full circular nodes, while part nodes are shown as partially clipped nodes attached to their parent object; the sub-PAGs corresponding to the highlighted regions are also emphasized. The right panel zooms into one such sub-PAG, where we annotate, for each part node, the oriented face used in defining its relational constraint.\", \"page\": 3, \"index\": 2, \"width\": 1015, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2603.07704v1/fig-003.webp\", \"caption\": \"Figure 3. Controllable Scene Synthesis via Part-Aware Spatial Configuration Solver. (a) Coarse Localization: The solver first prunes the 2D support surface of occupied regions (red), then further contracts the feasible space using object-level spatial relations (orange). (b) Part-Level Alignment: Precise geometric alignment is achieved by enforcing constraints (e.g., coplanarity) between specific surfaces identified by the solver. This drastically shrinks the pose space from which a final pose is sampled. (c) Fine-Grained Relational Control: Specifying different part-level geometric relations in the PAG results in distinct and predictable arrangements, showcasing the framework’s fine-grained controllability.\", \"page\": 4, \"index\": 3, \"width\": 998, \"height\": 312}]"
motivation: 现有的物体级场景图缺乏对部件间具体接触和支撑关系的精细描述，导致生成的空间布局存在歧义且不符合物理规律。
method: 提出PARSE框架，核心包含编码部件间几何关系的部件中心装配图（PAG）以及将关系转化为几何约束的物理求解器。
result: 构建了大规模数据集PARSE-10K，并证明该方法能显著增强Qwen3-VL的推理能力及3D生成模型的物理一致性。
conclusion: PARSE通过引入部件级建模，有效推进了具备几何基础的空间推理，为生成复杂且物理合理的3D场景提供了新路径。
---

## 摘要
物体间的关系是空间智能的基础，然而现有的表示方式——如语言介词或物体级场景图——过于粗糙，无法明确哪些区域实际上相互支撑、包含或接触，从而导致布局模糊且物理上不一致。为了解决这些歧义，需要一种部件级的表述；因此，我们提出了 PARSE，这是一个显式建模物体部件如何相互作用以确定可行且具有空间关联的场景配置的框架。PARSE 的核心是部件中心组装图（PAG）和部件感知空间配置求解器。PAG 编码了特定物体部件之间的几何关系，而求解器将这些关系转换为几何约束，以组装出无碰撞且物理有效的场景。利用 PARSE，我们构建了 PARSE-10K 数据集，包含 10,000 个 3D 室内场景。这些场景基于真实图像布局先验和精选的部件标注形状数据库构建，每个场景都具有密集的接触结构和部件级接触图。凭借这种结构化、空间关联的监督，在 PARSE-10K 上微调 Qwen3-VL 能够获得更强的物体级布局推理能力和更准确的部件级关系理解；此外，在 3D 生成模型中利用 PAG 作为结构先验，可以生成物理真实感和结构复杂度显著提升的场景。综上所述，这些结果表明 PARSE 显著推进了基于几何的空间推理，并支持生成物理一致的 3D 场景。

## 速览
**TLDR**：针对现有空间建模在物体层级过于粗糙、导致布局歧义和物理不一致的问题，本文提出PARSE框架。该框架通过“部件中心装配图（PAG）”显式建模物体部件间的几何关系，并利用空间配置求解器生成无碰撞且物理合理的3D场景。此外，研究者构建了包含1万个场景的PARSE-10K数据集。实验表明，PARSE显著提升了多模态大模型的空间推理能力及3D场景生成的物理真实性。 \
**Motivation**：现有的物体级场景图缺乏对部件间具体接触和支撑关系的精细描述，导致生成的空间布局存在歧义且不符合物理规律。 \
**Method**：提出PARSE框架，核心包含编码部件间几何关系的部件中心装配图（PAG）以及将关系转化为几何约束的物理求解器。 \
**Result**：构建了大规模数据集PARSE-10K，并证明该方法能显著增强Qwen3-VL的推理能力及3D生成模型的物理一致性。 \
**Conclusion**：PARSE通过引入部件级建模，有效推进了具备几何基础的空间推理，为生成复杂且物理合理的3D场景提供了新路径。

---

## Abstract
Inter-object relations underpin spatial intelligence, yet existing representations -- linguistic prepositions or object-level scene graphs -- are too coarse to specify which regions actually support, contain, or contact one another, leading to ambiguous and physically inconsistent layouts. To address these ambiguities, a part-level formulation is needed; therefore, we introduce PARSE, a framework that explicitly models how object parts interact to determine feasible and spatially grounded scene configurations. PARSE centers on the Part-centric Assembly Graph (PAG), which encodes geometric relations between specific object parts, and a Part-Aware Spatial Configuration Solver that converts these relations into geometric constraints to assemble collision-free, physically valid scenes. Using PARSE, we build PARSE-10K, a dataset of 10,000 3D indoor scenes constructed from real-image layout priors and a curated part-annotated shape database, each with dense contact structures and a part-level contact graph. With this structured, spatially grounded supervision, fine-tuning Qwen3-VL on PARSE-10K yields stronger object-level layout reasoning and more accurate part-level relation understanding; furthermore, leveraging PAGs as structural priors in 3D generation models leads to scenes with substantially improved physical realism and structural complexity. Together, these results show that PARSE significantly advances geometry-grounded spatial reasoning and supports the generation of physically consistent 3D scenes.

---

## 论文详细总结（自动生成）

这是一份关于论文《PARSE: Part-Aware Relational Spatial Modeling》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的空间关系表示（如“在……之上”等语言介词或物体级场景图）过于粗糙，无法精确描述物体间具体的接触点和支撑区域。这种模糊性导致在3D场景生成或机器人操作中，物体布局往往出现物理不一致（如悬空、穿模）或不符合常理的排列。
*   **研究动机**：作者认为空间智能的下一步在于“部件级”建模。例如，“书在桌上”实际上是书的封面与桌面接触。通过将关系细化到部件层面，可以将模糊的语义转化为精确的几何约束，从而实现物理上更合理、结构上更复杂的空间建模。

### 2. 方法论：PARSE 框架
PARSE 框架由两个核心组件组成：
*   **部件中心装配图 (Part-centric Assembly Graph, PAG)**：
    *   **两层节点结构**：上层为物体节点（VO），代表语义类别；下层为部件节点（VP），代表物体的几何组件（如椅子的腿、座垫）。每个部件定义了六个定向表面（上、下、前、后、左、右）。
    *   **多粒度边**：包括物体级空间边（粗略位置关系，如“在左边”）和部件级几何边（精细接触关系，如“底面接触”）。
    *   **有向无环图 (DAG) 结构**：将场景视为按顺序组装的过程，确保物理支撑关系的逻辑严密性。
*   **部件感知空间配置求解器 (Part-Aware Spatial Configuration Solver)**：
    *   **粗到精的求解流程**：首先根据物体级关系在2D支撑面上确定候选区域（粗定位）；然后根据部件级约束（如共面、对齐）大幅缩小位姿搜索空间（部件级对齐）；最后进行碰撞检测和物理验证。
    *   **物理精炼**：利用 Sapien 物理引擎进行短时间的动力学模拟，确保最终场景的稳定性和真实性。

### 3. 实验设计
*   **数据集构建**：利用 PARSE 框架生成了 **PARSE-10K** 数据集，包含 10,000 个室内场景，涵盖 132 个物体类别和 17 种房间类型，平均每个场景包含约 50 个物体。
*   **任务一：VLM 空间推理**：
    *   **Benchmark**：视觉关系多选题（MCQ）、部件级接触 MCQ、场景图生成（SGG）。
    *   **对比方法**：GPT-5、Gemini-2.5-Pro、Claude-Opus-4、Robobrain 2.0、Qwen3-VL。
*   **任务二：3D 场景生成**：
    *   **对比方法**：InstructScene（基于 3D-FRONT 数据集训练）。
    *   **评估方式**：用户研究（20名参与者），评估复杂度、真实感和接触保真度。

### 4. 资源与算力
*   **文中提及**：论文提到了使用 Sapien 引擎进行物理模拟，并使用 Michelangelo 编码器处理几何特征。
*   **未明确说明**：文中**未详细列出**具体的 GPU 型号、数量、训练总时长或具体的算力消耗数值。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 VLM 推理方面，对比了 5 种最先进的商业和开源大模型。
    *   在场景生成方面，进行了有无 PAG 约束的消融对比，并与 SOTA 方法进行了定性与定量（用户研究）对比。
    *   数据集包含 10,000 个场景，样本量较大。
*   **充分性与公平性**：实验设计较为全面，涵盖了从理解（VLM）到生成（3D Synthesis）的闭环验证。通过引入“接地无关（grounding-agnostic）”指标来区分推理能力和定位能力，体现了实验的客观性。但由于 3D 生成任务缺乏统一的自动化物理指标，主要依赖用户研究，主观性略高。

### 6. 主要结论与发现
*   **提升空间推理**：在 PARSE-10K 上微调后的 Qwen3-VL 在空间关系判断和场景图生成任务上显著优于 GPT-5 等通用大模型，证明了高质量部件级标注的价值。
*   **增强生成质量**：引入 PAG 约束后，3D 生成模型能够处理更复杂的堆叠和接触关系，生成的场景在物理真实感和物体密度上远超基于传统物体级数据集（如 3D-FRONT）的模型。
*   **几何约束的有效性**：部件级建模能有效剪枝合法的位姿空间，使求解器能高效生成无碰撞的复杂布局。

### 7. 优点与亮点
*   **粒度突破**：将场景图从物体级推向部件级，解决了长期存在的语义歧义问题。
*   **物理接地**：通过 DAG 组装顺序和物理模拟，确保了生成结果的物理可行性，而非仅仅是视觉上的“看起来像”。
*   **高质量资源**：PARSE-10K 填补了大规模、高精度部件级接触标注数据集的空白。

### 8. 不足与局限
*   **人工依赖**：PAG 的构建目前仍涉及部分人工定义，且高度依赖物体的标准位姿（Canonical Pose），如果模型库中的物体位姿不统一，求解器可能失效。
*   **几何表示限制**：目前的定向面接触模型（上/下/前/后等）主要处理近乎平行的表面，对于高度倾斜或形状极度复杂的接触面，建模精度可能受限。
*   **泛化风险**：虽然在室内场景表现优异，但尚未验证其在室外或更动态、非结构化环境中的适用性。

（完）
