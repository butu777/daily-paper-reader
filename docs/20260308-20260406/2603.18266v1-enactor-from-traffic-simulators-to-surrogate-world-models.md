---
title: "Enactor: From Traffic Simulators to Surrogate World Models"
authors: "Yash Ranjan, Rahul Sengupta, Anand Rangarajan, Sanjay Ranka"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18266v1"
tags: ["query:wm"]
score: 9.0
evidence: 用于交通仿真和主体交互的代理世界模型
tldr: 开发代理世界模型以提升交通仿真中的物理一致性和主体交互真实感。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18266v1/fig-001.webp\", \"caption\": \"Figure 2: The digital representation of two traffic intersection. (a) is the SUMO representation of the intersection at West Univ Ave @ NW 17th Street in Gainesville, Florida. (b) is the same intersection with the middle lane of the east and west flowing traffic removed.\", \"page\": 7, \"index\": 1, \"width\": 452, \"height\": 756}, {\"url\": \"assets/figures/arxiv/2603.18266v1/fig-002.webp\", \"caption\": \"Figure 1: Transformer-based architecture for learning multi-actor interactions and generating physically consistent trajectories. (a) The SUMO simulator initializes the traffic scene based on a set of control parameters. For each vehicle, neighboring actors are identified and their states are vectorized. (b) A state encoder maps the actor and its neighbors into embeddings, which are processed by a spatial attention module to produce interaction-aware spatial embeddings over the previous H timesteps. (c) The sequence of spatial embeddings for each actor is combined with a learned query embedding and passed through a temporal attention module to obtain a temporally informed representation, which is used to parameterize a Gaussian distribution over the actor’s next state. During inference, this distribution is sampled to generate the next state. (d) The newly generated actor state, together with updated neighbor states, is re-encoded and fed back into the model, enabling recursive trajectory unrolling in closed loop.\", \"page\": 5, \"index\": 2, \"width\": 920, \"height\": 590}]"
motivation: 用于交通仿真和主体交互的代理世界模型。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
交通微观模拟器被广泛用于评估各种“假设”条件下的路网性能。然而，控制交通参与者行为的行为模型过于简单，无法捕捉真实的参与者间交互。基于深度学习的方法已被应用于将车辆和行人建模为响应周围“环境”（包括车道、信号灯和邻近智能体）的“智能体”。尽管这些方法在学习参与者间交互方面行之有效，但它们无法在长时间跨度内生成物理一致的轨迹，且未能明确处理交通路口产生的复杂动力学，而路口是城市网络中的关键位置。受世界模型范式的启发，我们开发了一种基于 Transformer 架构的以参与者为中心的生成模型，该模型能够捕捉参与者间的交互，同时理解交通路口的几何结构，从而基于学习到的行为生成符合物理规律的轨迹。此外，我们在实时的“仿真在环”设置中测试了该模型，其中我们使用 SUMO 生成参与者的初始条件，然后由模型控制参与者的动力学。我们让仿真运行 40000 个时间步（4000 秒），测试模型在长时程上的性能，并根据交通工程相关指标评估轨迹。实验结果表明，所提出的框架有效地捕捉了复杂的参与者间交互，并生成了长时程、物理一致的轨迹，同时所需的训练样本显著少于传统的以智能体为中心的生成方法。我们的模型在交通相关指标以及综合指标上均优于基准模型，其中在 KL 散度上领先基准模型 10 倍以上。

## 速览
**TLDR**：针对传统交通微观仿真器行为模型过于简单以及深度学习方法在长时物理一致性和交叉口处理上的不足，本文提出了Enactor。这是一个基于Transformer的以动作者为中心的世界模型，能够捕捉复杂的动作者交互和道路几何特征。通过与SUMO联动的闭环仿真测试，该模型在长达4000秒的运行中展现了卓越的物理一致性和交通指标表现，在KL散度上比基准模型提升了10倍以上，且训练样本需求更少。 \
**Motivation**：现有的交通仿真器行为模型过于简化，而深度学习方法难以在复杂的交通路口生成长时且物理一致的轨迹。 \
**Method**：提出一种基于Transformer的以动作者为中心的世界模型，结合交通路口几何信息，通过“仿真在环”方式控制动作者动态。 \
**Result**：实验表明该模型在长时仿真中保持了高度的物理一致性，且在交通工程指标和KL散度上显著优于基准模型。 \
**Conclusion**：Enactor成功将世界模型范式应用于交通仿真，证明了其在捕捉复杂交互和生成长时物理一致轨迹方面的卓越能力。

---

## Abstract
Traffic microsimulators are widely used to evaluate road network performance under various ``what-if" conditions. However, the behavior models controlling the actions of the actors are overly simplistic and fails to capture realistic actor-actor interactions. Deep learning-based methods have been applied to model vehicles and pedestrians as ``agents" responding to their surrounding ``environment" (including lanes, signals, and neighboring agents). Although effective in learning actor-actor interaction, these approaches fail to generate physically consistent trajectories over long time periods, and they do not explicitly address the complex dynamics that arise at traffic intersections which is a critical location in urban networks. Inspired by the World Model paradigm, we have developed an actor centric generative model using transformer-based architecture that is able to capture the actor-actor interaction, at the same time understanding the geometry to the traffic intersection to generate physically grounded trajectories that are based on learned behavior. Moreover, we test the model in a live ``simulation-in-the-loop" setting, where we generate the initial conditions of the actors using SUMO and then let the model control the dynamics of the actors. We let the simulation run for 40000 timesteps (4000 seconds), testing the performance of the model on long timerange and evaluating the trajectories on traffic engineering related metrics. Experimental results demonstrate that the proposed framework effectively captures complex actor-actor interactions and generates long-horizon, physically consistent trajectories, while requiring significantly fewer training samples than traditional agent-centric generative approaches. Our model is able to outperform the baseline in traffic related as well as aggregate metrics where our model beats the baseline by more than 10x on the KL-Divergence.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enactor: From Traffic Simulators to Surrogate World Models》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：传统的交通微观模拟器（如 SUMO）依赖简单的规则模型（如 IDM、Krauss），难以捕捉真实世界中复杂的人类驾驶行为和动作者间的交互。而现有的深度学习轨迹预测模型虽然能学习交互，但在长时程（Long-horizon）闭环仿真中容易产生误差累积，导致物理不一致（如碰撞、无故停滞）或偏离真实分布。
*   **研究背景**：交通路口是城市网络中最复杂的冲突区域。论文旨在引入“世界模型（World Model）”范式，构建一个既能理解路口几何约束，又能生成长时程、物理一致轨迹的生成式仿真模型。

### 2. 方法论与核心思想
Enactor 模型采用以动作者为中心（Actor-centric）的 Transformer 架构，核心思想是将交通仿真视为一个隐藏马尔可夫模型（HMM）的状态演化过程。
*   **极坐标表示法**：不同于传统的笛卡尔坐标，模型使用极坐标 $(r, \sin\theta, \cos\theta, s, \sin\alpha, \cos\alpha)$ 来表示动作者状态。这种方式能更好地适应路口的弯曲轨迹和几何特征。
*   **空间注意力（Spatial Attention）**：利用 Transformer 的自注意力机制，将目标动作者的状态作为 Query，邻近动作者、地图多段线（Polylines）和交通信号状态作为 Key 和 Value，捕捉复杂的空间交互。
*   **时间注意力（Temporal Attention）**：结合可学习的运动查询（Motion Queries）和正弦位置编码，通过定向注意力机制处理历史状态序列，预测未来状态的概率分布。
*   **闭环生成**：模型预测未来位移的 Gaussian 分布参数（均值和方差），通过采样获取下一时刻状态，并将其反馈回模型进行递归推理（Recursive Unrolling），实现长达 4000 秒的连续仿真。

### 3. 实验设计与基准对比
*   **实验场景**：选取了佛罗里达州盖恩斯维尔的两个真实信号交叉口进行数字化建模。
    *   场景一：标准交叉口。
    *   场景二：修改几何结构后的交叉口（移除中车道），用于测试模型的泛化能力。
*   **基准模型（Benchmark）**：对比了 **IntTrajSim**（该团队之前的研究成果）。
*   **评估指标**：
    *   **重建指标**：ADE（平均位移误差）、FDE（最终位移误差）、NLL（负对数似然）。
    *   **交通工程指标**：红灯违章数、路口内异常停车、预停止线异常停车、TTC（碰撞时间）事件、不安全减速。
    *   **宏观统计指标**：平均速度分布和平均旅行时间的 KL 散度。

### 4. 资源与算力
*   **数据量**：使用 SUMO 生成了 6 小时（21,600 秒）的仿真数据，采样频率 0.1 秒，总计约 124 万个训练样本。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及训练时长。文中仅提到使用了 Adam 优化器、余弦学习率衰减以及 4000 步的预热（Warmup）阶段。

### 5. 实验数量与充分性
*   **实验规模**：进行了长达 40,000 个时间步（4000 秒）的闭环仿真测试，这在生成式模型中属于极长时程的验证。
*   **消融实验**：针对“邻车尾部位置信息”进行了消融研究，证明了引入物理尺寸感知对减少追尾和红灯违章至关重要。
*   **充分性评价**：实验设计较为客观，通过“仿真在环（Simulation-in-the-loop）”验证了模型在动态环境下的稳定性。但在对比方法上略显单一（主要对比了自家前作），若能对比更多主流的世界模型（如 TrafficBots）会更具说服力。

### 6. 主要结论与发现
*   **长时稳定性**：Enactor 能够维持 4000 秒的稳定仿真而不崩溃，证明了其在处理协变量偏移（Covariate Shift）方面的鲁棒性。
*   **宏观一致性极佳**：在速度分布和旅行时间的 KL 散度指标上，Enactor 比基准模型提升了 **10 倍以上**，说明其生成的交通流在统计特性上高度接近真实（或 SUMO 标定）数据。
*   **几何适应性**：模型在修改后的路口几何结构上依然能保持良好的规则遵守能力，展现了较强的泛化潜力。

### 7. 亮点与优点
*   **物理约束与几何感知**：通过极坐标和向量化地图表示，使模型生成的轨迹天然符合道路几何形状。
*   **少样本高效性**：相比传统的以智能体为中心的生成模型，该框架在更少的训练样本下达到了更高的物理一致性。
*   **交通工程视角**：评估不仅停留在数学误差（ADE/FDE），还深入到了交通安全和效率指标（TTC、违章率等），具有实际工程意义。

### 8. 不足与局限
*   **微观安全指标仍有差距**：尽管宏观指标领先，但在 TTC（碰撞时间）等微观安全指标上，Enactor 仍逊于经过复杂后处理的基准模型，说明其在极近距离的精细避障逻辑上仍需优化。
*   **数据源局限**：训练数据完全来自 SUMO 仿真而非真实传感器数据（如 Waymo Open Dataset），这可能导致模型学到的是 SUMO 的行为模式而非真实人类的驾驶模式。
*   **算力细节缺失**：缺乏训练成本的量化信息，难以评估其在大规模城市仿真中的部署可行性。

（完）
