---
title: Dense Dynamic Scene Reconstruction and Camera Pose Estimation from Multi-View Videos
title_zh: 基于多视图视频的稠密动态场景重建与相机位姿估计
authors: "Shuo Sun, Unal Artan, Malcolm Mielle, Achim J. Lilienthaland, Martin Magnusson"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12064v1"
tags: ["query:3d"]
score: 9.0
evidence: 多视图视频的稠密动态场景重建
tldr: 本研究针对多台自由移动相机捕获的动态场景，提出了一种两阶段优化框架，旨在解决稠密重建与相机位姿估计难题。通过构建时空连接图扩展单目SLAM，并结合前馈模型进行宽基线初始化，随后利用光流优化跨相机一致性。该方法在自研的MultiCamRobolab数据集及合成数据上表现优异，显著提升了追踪鲁棒性与重建精度，且内存占用更低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.12064v1/fig-001.webp\", \"caption\": \"Fig. 5: Qualitative reconstruction results on MultiCamRobolab datasets.\", \"page\": 14, \"index\": 1, \"width\": 720, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2603.12064v1/fig-002.webp\", \"caption\": \"Fig. 2: Method Overview. Given multiple video inputs: Our method first uses a feed-forward model for initialization to achieve a global scale anchor and initialized poses (Step1). Then, we build a spatio-temporal connection graph during tracking to estimate camera poses and maintain a consistent scale (Step2). At last, we leverage the dense optical flow, estimated poses, and achieved connection graph to refine per-pixel depth to get a consistent scene and refined camera poses.\", \"page\": 5, \"index\": 2, \"width\": 725, \"height\": 192}]"
motivation: 现有的动态场景重建方法通常局限于单相机或预校准的固定相机阵列，难以处理多台自由移动相机捕获的复杂场景。
method: 提出一个两阶段框架，首先通过时空连接图和宽基线初始化实现多相机协同追踪，随后利用宽基线光流优化稠密深度与位姿的一致性。
result: 在合成和真实世界基准测试中，该方法在精度上显著超越了现有的前馈模型，同时有效降低了内存消耗。
conclusion: 该研究通过解耦追踪与精细化过程，为多自由移动相机下的动态场景稠密重建提供了一种高效且鲁棒的解决方案。
---

## 摘要
我们解决了利用多个自由移动的相机进行稠密动态场景重建和相机位姿估计这一具有挑战性的问题——这种设置在多个观察者捕捉同一共享事件时自然产生。先前的方法要么仅处理单相机输入，要么需要固定安装且预先校准的相机阵列，这限制了它们的实际应用性。我们提出了一个两阶段优化框架，将任务解耦为鲁棒的相机跟踪和稠密深度细化。在第一阶段，我们通过构建时空连接图，将单相机视觉 SLAM 扩展到多相机设置。该图利用了相机内的时域连续性和相机间的空域重叠，从而实现了尺度一致性和鲁棒跟踪。为了确保在重叠有限的情况下的鲁棒性，我们引入了一种利用前馈重建模型的宽基线初始化策略。在第二阶段，我们利用宽基线光流优化稠密的相机间和相机内一致性，从而细化深度和相机位姿。此外，我们推出了 MultiCamRobolab，这是一个包含由动作捕捉系统提供的真值位姿的新型真实世界数据集。最后，我们证明了我们的方法在合成和真实世界基准测试中均显著优于最先进的前馈模型，同时所需的内存更少。

## Abstract
We address the challenging problem of dense dynamic scene reconstruction and camera pose estimation from multiple freely moving cameras -- a setting that arises naturally when multiple observers capture a shared event. Prior approaches either handle only single-camera input or require rigidly mounted, pre-calibrated camera rigs, limiting their practical applicability. We propose a two-stage optimization framework that decouples the task into robust camera tracking and dense depth refinement. In the first stage, we extend single-camera visual SLAM to the multi-camera setting by constructing a spatiotemporal connection graph that exploits both intra-camera temporal continuity and inter-camera spatial overlap, enabling consistent scale and robust tracking. To ensure robustness under limited overlap, we introduce a wide-baseline initialization strategy using feed-forward reconstruction models. In the second stage, we refine depth and camera poses by optimizing dense inter- and intra-camera consistency using wide-baseline optical flow. Additionally, we introduce MultiCamRobolab, a new real-world dataset with ground-truth poses from a motion capture system. Finally, we demonstrate that our method significantly outperforms state-of-the-art feed-forward models on both synthetic and real-world benchmarks, while requiring less memory.

---

## 论文详细总结（自动生成）

以下是对论文《Dense Dynamic Scene Reconstruction and Camera Pose Estimation from Multi-View Videos》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从多台**自由移动**且**未预先校准**的相机捕获的视频中，实现稠密的动态场景重建和精确的相机位姿估计。
*   **研究动机**：
    *   **现实需求**：多观察者拍摄同一事件（如体育赛事、机器人协作、AR应用）非常普遍，但现有方法多局限于单相机或固定校准的相机阵列。
    *   **技术挑战**：单目重建存在尺度不确定性（Scale Ambiguity）；自由移动相机间重叠区域有限且间歇性；动态物体违反了传统多视图几何的静态世界假设。

### 2. 方法论：两阶段优化框架
该方法将任务解耦为“鲁棒相机追踪”和“稠密深度精细化”两个阶段：
*   **第一阶段：时空多相机追踪（Spatio-temporal Tracking）**
    *   **时空连接图 ($\Omega$)**：构建包含三种连接的图：
        1.  **时间连接 ($\Omega_{temp}$)**：单相机内的连续帧关联。
        2.  **空间连接 ($\Omega_{spat}$)**：同一时刻不同相机间的重叠视图关联。
        3.  **时空连接 ($\Omega_{st}$)**：当前帧与其它相机历史关键帧的关联。
    *   **宽基线初始化**：利用前馈模型（如 VGGT）获取初始的全局一致位姿和深度，解决多相机间初始重叠不足的问题。
    *   **联合束调整（Bundle Adjustment）**：通过最小化加权再投影误差，并引入对齐后的单目深度先验，联合优化位姿和深度。
*   **第二阶段：多视图场景一致性精细化**
    *   **稠密对应估计**：使用宽基线光流模型（UFM）建立更长程、更稠密的关联。
    *   **两步优化**：
        1.  **相位1**：固定位姿，优化每帧的深度缩放和偏移参数。
        2.  **相位2**：交替迭代优化每像素深度值和相机位姿，并加入位姿平滑项约束。

### 3. 实验设计
*   **数据集**：
    *   **MultiCamVideo**：合成数据集，包含10个视角，实验中随机抽取50个片段（每片段3个相机）。
    *   **MultiCamRobolab（自研）**：真实世界数据集，包含24个RGB-D序列（2-3台相机），涵盖机器人狗、机械臂、动态行人等5类场景，使用 Qualisys 动捕系统提供真值位姿。
*   **基准方法（Baselines）**：
    *   传统方法：**COLMAP**（稀疏重建）。
    *   前馈模型：**VGGT**、**FastVGGT**、**Fast3R**。
    *   视频处理模型：**CUT3R**。
*   **评估指标**：ATE（绝对轨迹误差）、RTE/RRE（相对平移/旋转误差）、Abs Rel（绝对相对深度误差）、$\delta < 1.25$（深度准确率）、Md（中值欧氏距离，衡量场景一致性）。

### 4. 资源与算力
*   **硬件使用**：
    *   **NVIDIA RTX 4090**：用于运行本文提出的方法、COLMAP 和 CUT3R。
    *   **NVIDIA A100 (40GB)**：由于内存需求极高，Fast3R 和 FastVGGT 必须在此卡上运行。
*   **内存效率**：论文强调其方法比 VGGT 等前馈模型更节省内存。VGGT 在处理长序列时会发生 OOM（内存溢出），必须进行下采样（间隔8帧），而本文方法可处理全帧率视频。

### 5. 实验数量与充分性
*   **实验规模**：在合成和真实两个大类数据集上进行了测试，涵盖了从强重叠到无重叠（Non-overlap）的极端情况。
*   **消融实验**：针对“宽基线初始化”、“时空连接图”以及“两阶段精细化”分别做了消融，验证了各模块对精度和一致性的贡献。
*   **客观性**：对比了当前最先进的（SOTA）前馈模型，并使用了动捕系统提供的客观真值。实验设计考虑了动态物体干扰和长序列处理能力，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在所有测试场景中，本文方法在位姿估计和深度重建精度上均优于 SOTA 前馈模型。
*   **鲁棒性**：通过时空连接图，即使在相机间重叠较少的情况下，也能维持尺度一致性。
*   **动态适应性**：相比 COLMAP，该方法能更好地处理包含移动物体（人、机器人）的复杂场景。
*   **内存优势**：在保持高精度的同时，显著降低了对高端 GPU 显存的依赖。

### 7. 优点与亮点
*   **首创性**：据作者称，这是第一个专门针对“多台自由移动相机”进行稠密动态重建的框架。
*   **解耦设计**：将追踪与精细化分离，既保证了在线追踪的效率，又通过后期优化实现了极高的离线重建质量。
*   **数据集贡献**：提供了带真值的 MultiCamRobolab 数据集，填补了该领域真实世界基准数据的空白。

### 8. 不足与局限
*   **极端情况失效**：在“完全无重叠”的极端场景下，空间连接失效，系统退化为独立的单相机重建，虽然能靠初始化维持大致尺度，但无法通过跨相机约束进一步提升精度。
*   **计算延迟**：虽然内存占用低，但两阶段优化（尤其是第二阶段的迭代精细化）可能涉及较大的计算量，难以达到实时处理。
*   **依赖初始化**：系统性能在一定程度上受限于第一阶段前馈模型的初始化质量，若初始位姿偏差过大，后续优化可能陷入局部最优。

（完）
