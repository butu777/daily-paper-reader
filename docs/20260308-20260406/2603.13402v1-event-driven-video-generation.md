---
title: Event-Driven Video Generation
title_zh: 事件驱动的视频生成
authors: Chika Maduabuchi
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.13402v1"
tags: ["query:wm"]
score: 9.0
evidence: 在生成式视频模型中建模物理交互与接触
tldr: 针对现有视频生成模型在处理物体交互（如接触、放置）时常出现的物理逻辑错误，本文提出Event-Driven Video Generation (EVD)框架。该框架通过引入轻量化事件头预测事件活动，并结合事件接地损失和门控采样机制，将去噪过程与特定事件挂钩。实验证明，EVD在保持画质的同时，显著提升了视频的物理一致性和交互稳定性，有效减少了交互过程中的视觉幻觉。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.13402v1/fig-001.webp\", \"caption\": \"Table 5: Sensitivity of EVD on EVD-Bench (DiT-4B backbone). Each row varies a single control while holding the rest at the default. Metrics are aggregated over EVD-Bench.\", \"page\": 47, \"index\": 1, \"width\": 725, \"height\": 422}, {\"url\": \"assets/figures/arxiv/2603.13402v1/fig-002.webp\", \"caption\": \"Table 3: EVD ablations on EVD-Bench (DiT-4B backbone). Human Eval reports the percentage of 2AFC votes favoring the full DiT-4B+EVD model over each ablated variant (higher is better for EVD). Auto. Metrics are computed using VBench. All variants use identical sampling settings (solver, NFE, CFG scale) and the same prompt set.\", \"page\": 14, \"index\": 2, \"width\": 725, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2603.13402v1/fig-003.webp\", \"caption\": \"Fig. 4: Qualitative comparison with leading video generation baselines. We compare EVD against Movie Gen, Sora, and DiT-30B on representative prompts involving soft-body deformation, flexible-object dynamics, structured scene interactions, and liquid transfer. Across all examples, baseline models often exhibit incomplete event realization, weak contact-response coupling, or implausible state evolution, whereas EVD produces temporally ordered interactions and more coherent physical outcomes.\", \"page\": 13, \"index\": 3, \"width\": 716, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2603.13402v1/fig-004.webp\", \"caption\": \"Table 2: Comparison of EVD with leading large-scale video generation models on EVD-Bench. Human evaluation reports the percentage of pairwise votes favoring EVD ; automatic metrics are computed using VBench.\", \"page\": 13, \"index\": 4, \"width\": 652, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.13402v1/fig-005.webp\", \"caption\": \"Table 7: Human evaluation protocol statistics (2AFC). Summary of rater pool, assignment, quality control (QC), and agreement used for the human preference results reported in the paper.\", \"page\": 49, \"index\": 5, \"width\": 652, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2603.13402v1/fig-006.webp\", \"caption\": \"Table 1: Comparison of EVD with prior video generation baselines on EVDBench. Human evaluation reports the percentage of pairwise votes favoring EVD ; automatic metrics are computed using VBench.\", \"page\": 12, \"index\": 6, \"width\": 642, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2603.13402v1/fig-007.webp\", \"caption\": \"Fig. 3: Representative text-conditioned video generations from EVD. EVD produces coherent event-driven dynamics across a diverse set of interactions, including target-directed motion, constrained mechanisms, deformation and recovery, gravitymediated closure, multi-agent coordination, and liquid transfer. These examples illustrate that EVD captures causally grounded state transitions beyond simple frame-toframe motion synthesis.\", \"page\": 12, \"index\": 7, \"width\": 716, \"height\": 429}]"
motivation: 现有的视频生成模型采用全帧去噪，缺乏对交互发生时空的显式建模，导致物体漂移和接触逻辑错误。
method: 引入DiT兼容的轻量化事件头，通过事件接地损失耦合状态变化，并利用事件门控采样在交互期间集中更新。
result: 在EVD-Bench上，该方法显著减少了状态持久性、空间精度和接触稳定性方面的失败案例，并提升了人类偏好。
conclusion: 显式的事件接地是减少视频生成中交互幻觉、提升物理真实性的实用且有效的抽象方案。
---

## 摘要
最先进的文本到视频模型在单帧上通常看起来很逼真，但在简单的交互上却表现不佳：例如运动在接触前就开始、动作未能实现、物体在放置后漂移以及支撑关系破裂。我们认为这源于“帧优先”的去噪方式，它在每一步都更新所有位置的潜状态，而没有明确交互在何时何地发生的感知。我们引入了事件驱动的视频生成（EVD），这是一个与 DiT 兼容的极简框架，使采样过程基于事件：一个轻量级的事件头预测与 token 对齐的事件活动，基于事件的损失函数在训练期间将活动与状态变化耦合，而事件门控采样（具有滞后和早期步调度）在抑制伪更新的同时，将更新集中在交互期间。在 EVD-Bench 上，EVD 持续提升了人类偏好和 VBench 动力学指标，在不牺牲外观质量的前提下，大幅减少了状态持久性、空间准确性、支撑关系和接触稳定性方面的失败模式。这些结果表明，显式的事件引导是减少视频生成中交互幻觉的一种实用抽象。

## 速览
**TLDR**：针对现有视频生成模型在处理物体交互（如接触、放置）时常出现的物理逻辑错误，本文提出了一种事件驱动的视频生成框架（EVD）。该框架通过引入轻量化事件头预测事件活动，并结合事件接地损失和门控采样机制，将去噪过程与具体的物理事件耦合。实验证明，EVD在保持画面质量的同时，显著提升了视频的物理一致性和交互准确性。 \
**Motivation**：现有的视频生成模型采用逐帧去噪，缺乏对交互发生时空的显式建模，导致物体漂移和接触逻辑错误。 \
**Method**：提出EVD框架，利用事件头预测Token级别的活动，并通过事件门控采样在交互瞬间集中更新，抑制无关更新。 \
**Result**：在EVD-Bench上，该方法显著减少了状态持久性、空间精度和接触稳定性方面的故障，提升了人类偏好评分。 \
**Conclusion**：显式的事件接地是减少视频生成中交互幻觉、提升物理真实性的有效且实用的抽象方法。

---

## Abstract
State-of-the-art text-to-video models often look realistic frame-by-frame yet fail on simple interactions: motion starts before contact, actions are not realized, objects drift after placement, and support relations break. We argue this stems from frame-first denoising, which updates latent state everywhere at every step without an explicit notion of when and where an interaction is active. We introduce Event-Driven Video Generation (EVD), a minimal DiT-compatible framework that makes sampling event-grounded: a lightweight event head predicts token-aligned event activity, event-grounded losses couple activity to state change during training, and event-gated sampling (with hysteresis and early-step scheduling) suppresses spurious updates while concentrating updates during interactions. On EVD-Bench, EVD consistently improves human preference and VBench dynamics, substantially reducing failure modes in state persistence, spatial accuracy, support relations, and contact stability without sacrificing appearance. These results indicate that explicit event grounding is a practical abstraction for reducing interaction hallucinations in video generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Event-Driven Video Generation》（事件驱动的视频生成）的结构化深度分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视频生成模型（如 Sora、Movie Gen 等）虽然单帧画质极高，但在处理**物理交互**时经常失败。常见幻觉包括：物体在接触前就开始移动（违反因果律）、放置后持续漂移（缺乏状态持久性）、支撑关系凭空出现或消失。
*   **研究动机**：作者认为根源在于现有的“帧优先（Frame-first）”去噪机制。模型在每一步采样中都会更新潜空间的所有位置，缺乏对“交互何时何地发生”的显式建模，导致去噪过程产生了物理上不可信的随机扰动。
*   **整体含义**：提出将视频生成视为由**离散事件驱动的状态转换**，通过显式建模事件活动来约束潜状态的演化。

### 2. 论文提出的方法论：EVD 框架
EVD（Event-Driven Video Generation）是一个与扩散变压器（DiT）兼容的轻量化插件，核心思想是“无事件则无更新”。
*   **轻量化事件头（Event Head）**：在 DiT 的末层特征上添加一个极小的预测头，输出与 Token 对齐的“事件活动图”（Activity Map），识别交互发生的时空位置。
*   **事件接地训练损失（Event-Grounded Losses）**：
    *   **实现损失（Realization Loss）**：惩罚在无事件区域发生的潜状态更新。
    *   **一致性损失（Consistency Loss）**：确保活动事件期间的状态更新在时间上是连续且定向的。
    *   **排序与终止正则化**：强制执行“先接触后运动”和“事件结束即停止”的物理逻辑。
*   **事件门控采样（Event-Gated Sampling）**：
    *   在推理阶段，利用预测的事件图作为“门控”，调制 DiT 输出的更新方向场。
    *   引入**滞后（Hysteresis）机制**防止事件边界闪烁。
    *   采用**早期步调度（Early-step Scheduling）**：在生成初期（确定宏观动力学的关键阶段）强化门控，后期逐渐退火以微调外观细节。

### 3. 实验设计与 Benchmark
*   **数据集/场景**：构建了 **EVD-Bench**，包含 150 个专注于物理交互的提示词，涵盖四类典型失败模式：状态持久性、空间准确性、支撑关系、接触稳定性。
*   **对比方法**：
    *   **开源/半开源模型**：CogVideo (2B/5B), PyramidFlow, Open-Sora, HunyuanVideo, DiT-4B/30B 原型。
    *   **闭源 SOTA 模型**：Kling 3.0 Pro, Runway Gen-4.5, Veo 3.1, Sora 2 Pro, Movie Gen。
*   **评估指标**：VBench（外观与动力学评分）以及人类偏好实验（2AFC，针对文本忠实度、质量、动力学进行投票）。

### 4. 资源与算力
论文明确说明了算力消耗：
*   **DiT-4B + EVD**：在 **32 张 GPU** 上训练，总计 50,000 步，全局 Batch Size 为 64。
*   **DiT-30B + EVD**：在 **256 张 GPU** 上训练（未指明具体型号，推测为 A100/H100 级别），总计 40,000 步，全局 Batch Size 为 192。
*   **开销分析**：EVD 仅增加了极少量的参数（30B 模型中仅增加 0.04% 的参数），推理时间开销仅增加约 2%，训练吞吐量保持在原有的 97% 以上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 30B 不同参数规模的骨干网络，验证了方法的可扩展性。
*   **消融实验**：非常详尽。分别验证了实现损失、一致性损失、训练/推理门控、采样调度等组件的必要性。
*   **客观性**：通过引入“运动掩码（Motion Masking）”作为对照组，证明了 EVD 学习到的是语义层面的交互逻辑，而非简单的像素运动过滤。使用了 120 名独立众包人员进行双盲人类评估，实验设计较为严谨。

### 6. 主要结论与发现
*   **物理一致性大幅提升**：EVD 在不牺牲图像质量（Appearance）的前提下，显著提高了动力学（Dynamics）评分。
*   **减少幻觉**：有效解决了“接触前运动”和“放置后漂移”等顽疾。
*   **即插即用**：证明了在不改变预训练 DiT 核心权重的情况下，通过轻量化微调即可注入物理常识。
*   **人类偏好**：在动力学维度上，DiT-30B+EVD 相比原版获得了超过 97% 的人类胜率。

### 7. 优点与亮点
*   **极简且高效**：不改变求解器（Solver）或解码器，仅通过调制方向场实现，工程实现难度低。
*   **因果律建模**：通过事件门控采样，首次在扩散模型中显式引入了类似于物理引擎的“触发”机制。
*   **自监督学习**：事件标签通过光流和潜空间差异自动提取，无需人工标注，具备大规模扩展潜力。

### 8. 不足与局限
*   **弱信号失效**：当交互物体极小、被遮挡或处于复杂背景中时，事件头可能无法准确捕捉信号。
*   **分辨率限制**：在处理极细微的流体（如细水流）或织物褶皱时，受限于潜空间的压缩率，效果提升有限。
*   **全局运动干扰**：剧烈的相机运动（如快速推拉摇移）可能会干扰事件活动的判定，导致误触发或漏触发。
*   **长程逻辑**：虽然改善了瞬时交互，但对于需要长期因果链条（如复杂的机械联动）的模拟仍有提升空间。

（完）
