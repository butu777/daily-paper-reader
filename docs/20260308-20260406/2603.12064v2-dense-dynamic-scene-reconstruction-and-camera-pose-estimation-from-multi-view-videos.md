---
title: Dense Dynamic Scene Reconstruction and Camera Pose Estimation from Multi-View Videos
title_zh: 基于多视图视频的稠密动态场景重建与相机位姿估计
authors: "Shuo Sun, Unal Artan, Malcolm Mielle, Achim J. Lilienthaland, Martin Magnusson"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12064v2"
tags: ["query:3d"]
score: 9.0
evidence: 基于多视图视频的稠密动态场景重建
tldr: 本研究针对多台自由移动相机捕获的动态场景，提出了一种两阶段优化框架，旨在解决稠密重建与位姿估计难题。通过构建时空连接图扩展多相机SLAM，并结合宽基线初始化与光流一致性优化，实现了高精度的深度细化。此外，研究还推出了包含真值的MultiCamRobolab数据集，实验证明该方法在精度和内存效率上均优于现有模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.12064v2/fig-001.webp\", \"caption\": \"Fig. 5: Qualitative reconstruction results on MultiCamRobolab datasets.\", \"page\": 14, \"index\": 1, \"width\": 720, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2603.12064v2/fig-002.webp\", \"caption\": \"Fig. 2: Method Overview. Given multiple video inputs: Our method first uses a feed-forward model for initialization to achieve a global scale anchor and initialized poses (Step1). Then, we build a spatio-temporal connection graph during tracking to estimate camera poses and maintain a consistent scale (Step2). At last, we leverage the dense optical flow, estimated poses, and achieved connection graph to refine per-pixel depth to get a consistent scene and refined camera poses.\", \"page\": 5, \"index\": 2, \"width\": 725, \"height\": 192}]"
motivation: 现有的动态场景重建方法通常局限于单相机或预校准的固定相机阵列，难以处理多台自由移动相机捕获的复杂场景。
method: 提出一种两阶段框架，首先通过时空连接图和宽基线初始化实现多相机跟踪，随后利用宽基线光流优化稠密一致性。
result: 在合成和真实数据集上均显著超越了最先进的前馈模型，且具有更低的内存消耗。
conclusion: 该方法有效解决了多自由移动相机下的动态场景重建问题，并通过新数据集和优化策略提升了系统的鲁棒性与精度。
---

## 摘要
我们解决了利用多个自由移动的相机进行稠密动态场景重建和相机位姿估计这一具有挑战性的问题——这种设置在多个观察者捕捉同一共享事件时自然产生。先前的方法要么仅处理单相机输入，要么需要固定安装且预先校准的相机装置，限制了它们的实际应用性。我们提出了一个两阶段优化框架，将任务解耦为鲁棒的相机跟踪和稠密深度细化。在第一阶段，我们通过构建时空连接图，将单相机视觉 SLAM 扩展到多相机设置。该图利用了相机内的时域连续性和相机间的空域重叠，从而实现了尺度一致性和鲁棒跟踪。为了确保在重叠有限情况下的鲁棒性，我们引入了一种利用前馈重建模型的宽基线初始化策略。在第二阶段，我们利用宽基线光流优化稠密的相机间和相机内一致性，从而细化深度和相机位姿。此外，我们推出了 MultiCamRobolab，这是一个包含由动作捕捉系统提供的真值位姿的新型真实世界数据集。最后，我们证明了我们的方法在合成和真实世界基准测试中均显著优于最先进的前馈模型，同时所需的内存更少。

## Abstract
We address the challenging problem of dense dynamic scene reconstruction and camera pose estimation from multiple freely moving cameras -- a setting that arises naturally when multiple observers capture a shared event. Prior approaches either handle only single-camera input or require rigidly mounted, pre-calibrated camera rigs, limiting their practical applicability. We propose a two-stage optimization framework that decouples the task into robust camera tracking and dense depth refinement. In the first stage, we extend single-camera visual SLAM to the multi-camera setting by constructing a spatiotemporal connection graph that exploits both intra-camera temporal continuity and inter-camera spatial overlap, enabling consistent scale and robust tracking. To ensure robustness under limited overlap, we introduce a wide-baseline initialization strategy using feed-forward reconstruction models. In the second stage, we refine depth and camera poses by optimizing dense inter- and intra-camera consistency using wide-baseline optical flow. Additionally, we introduce MultiCamRobolab, a new real-world dataset with ground-truth poses from a motion capture system. Finally, we demonstrate that our method significantly outperforms state-of-the-art feed-forward models on both synthetic and real-world benchmarks, while requiring less memory.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dense Dynamic Scene Reconstruction and Camera Pose Estimation from Multi-View Videos》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：如何在多个**自由移动**的相机捕捉到的视频流中，实现高精度的**相机位姿估计**与**稠密动态场景重建**。
*   **研究背景与动机**：
    *   **现实需求**：多观察者拍摄同一事件（如多人用手机录制、机器人协作、体育转播）已非常普遍。
    *   **现有局限**：传统的动态重建方法多局限于单相机，或需要预先校准的固定相机阵列。
    *   **技术挑战**：自由移动相机面临单目深度尺度模糊（Scale Ambiguity）、相机间重叠区域有限且多变（Limited Overlap）、以及动态物体违反静态世界假设（Dynamic Content）等三大难题。

### 2. 方法论
论文提出了一个**两阶段优化框架**，将任务解耦为“鲁棒相机跟踪”和“稠密深度细化”：

*   **第一阶段：时空多相机跟踪（Spatio-temporal Tracking）**
    *   **宽基线初始化**：利用前馈模型（如VGGT）获取初始的全局一致位姿和深度，作为尺度锚点。
    *   **时空连接图（Ω）**：构建包含三种连接的图结构：
        1.  **时间连接（Ωtemp）**：单相机内的连续帧。
        2.  **空间连接（Ωspat）**：同一时刻不同相机间的重叠帧。
        3.  **时空连接（Ωst）**：当前帧与其它相机历史关键帧的重叠。
    *   **捆绑调整（BA）**：通过最小化加权重投影误差（结合光流对应点和单目深度先验）来联合优化位姿和深度。

*   **第二阶段：多视图场景一致性细化（Refinement）**
    *   **稠密对应估计**：使用宽基线光流模型（UFM）提取更高分辨率的帧间对应关系。
    *   **两步优化策略**：
        1.  **尺度对齐**：固定位姿，优化每帧深度的仿射变换参数（尺度和偏移）。
        2.  **迭代细化**：交替优化每像素深度值和相机位姿，并引入位姿平滑项（Lpose）以确保轨迹连续性。

### 3. 实验设计
*   **数据集**：
    1.  **MultiCamVideo**：合成数据集，包含10个视角，文中随机抽取50个片段进行评估。
    2.  **MultiCamRobolab（自建）**：真实世界数据集，包含24个RGB-D序列（2-3个相机），涵盖机器人狗、机械臂、动态行人等5种场景，利用Qualisys动作捕捉系统提供真值位姿。
*   **Benchmark 与对比方法**：
    *   **传统方法**：COLMAP（经典的SfM）。
    *   **前馈模型（SOTA）**：VGGT、Fast3R、FastVGGT、CUT3R。
*   **评估指标**：位姿误差（ATE, RTE, RRE）、深度质量（Abs Rel, Delta）、场景一致性（中值欧氏距离 Md）。

### 4. 资源与算力
*   **硬件环境**：
    *   **高性能测试**：Fast3R 和 FastVGGT 在 **NVIDIA A100 (40GB)** 上运行，以应对其极高的内存需求。
    *   **常规测试**：本方法及其他模型在 **NVIDIA RTX 4090** 上运行。
*   **内存效率**：论文强调其方法比 VGGT 等模型更节省内存，VGGT 在处理长序列时经常出现内存溢出（OOM），而本方法通过滑动窗口和解耦设计规避了此问题。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在合成和真实两个大型数据集上进行了全面对比。
    *   针对 2 相机和 3 相机配置分别进行了验证。
    *   **消融实验**：详细验证了“宽基线初始化”、“时空连接图”以及“两阶段细化”中各步骤对最终结果的贡献。
*   **充分性评价**：实验设计较为客观，通过引入噪声初始化实验验证了系统的鲁棒性。对比方法涵盖了从传统几何到最新深度学习的前沿模型，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：本方法在位姿估计和深度重建精度上均显著优于现有的前馈模型（如 CUT3R, Fast3R）。
*   **鲁棒性强**：在动态物体干扰和相机重叠度极低的情况下，通过时空图约束，依然能保持较好的尺度一致性。
*   **内存优势**：相比于全局注意力机制的前馈模型，本方法能处理更长的视频序列且对显存要求更低。
*   **初始化重要性**：实验证明，在多相机无重叠或弱重叠初期，基于学习的宽基线初始化对建立全局尺度至关重要。

### 7. 优点
*   **解耦架构**：将复杂的端到端重建分解为跟踪与细化，兼顾了运行效率与重建精度。
*   **时空图创新**：有效地利用了跨相机的历史信息，增强了多相机系统的整体约束。
*   **贡献数据集**：提供了带真值的真实世界多相机动态数据集，填补了该领域的空白。

### 8. 不足与局限
*   **极端退化场景**：当多个相机之间**完全没有**视场重叠时，空间约束失效，系统会退化为独立的单相机重建，尺度一致性将依赖于初始化的准确度。
*   **计算耗时**：虽然内存占用优化较好，但两阶段优化（尤其是第二阶段的迭代细化）在处理超长视频时，计算时间成本可能仍然较高。
*   **依赖初始化模型**：系统的最终精度在一定程度上受限于第一阶段所使用的前馈模型（如VGGT）的初始质量。

（完）
