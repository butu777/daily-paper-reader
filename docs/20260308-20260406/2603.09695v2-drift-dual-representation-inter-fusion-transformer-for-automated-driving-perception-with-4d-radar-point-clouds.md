---
title: "DRIFT: Dual-Representation Inter-Fusion Transformer for Automated Driving Perception with 4D Radar Point Clouds"
title_zh: DRIFT：基于 4D 雷达点云的自动驾驶感知双重表示交互融合 Transformer
authors: "Siqi Pei, Andras Palffy, Dariu M. Gavrila"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09695v2"
tags: ["query:adverseweather"]
score: 9.0
evidence: 利用4D雷达在恶劣天气下实现鲁棒的自动驾驶感知
tldr: 针对4D毫米波雷达点云密度低、感知难度大的挑战，本文提出DRIFT模型。该模型采用双路径Transformer架构，通过点路径提取细粒度局部特征，通过柱体路径编码粗粒度全局特征，并利用创新的特征共享层在多阶段进行深度融合。实验证明，DRIFT在目标检测和道路估计任务中表现优异，显著提升了4D雷达的感知精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09695v2/fig-001.webp\", \"caption\": \"Fig. 1: Example scene from the View-of-Delft [1] dataset with radar and annotation data. The two inlets on the left show corresponding LiDAR and radar point clouds around the same pedestrian. Unlike the LiDAR point cloud, it is difficult to detect the pedestrian using only the local region of the radar point cloud due to its sparsity. However, incorporating global information (i.e. the pedestrian’s relative position to the ego-vehicle and the scene, e.g. drivable area marked with blue) with local features such as shape and velocity, the pedestrian’s presence becomes more apparent.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.09695v2/fig-002.webp\", \"caption\": \"TABLE I: Comparison of VoD validation set results. DRIFT uses cross-attention feature sharing. Pre-training is performed by perciv-scenes-2 dataset. The best results of each configuration (with and without pre-training) are bold. Results marked with † are inherited from [6], results marked with * comes from re-implemented methods.\", \"page\": 6, \"index\": 2, \"width\": 868, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2603.09695v2/fig-003.webp\", \"caption\": \"Fig. 4: Visualization results of ground truth, CenterPoint, and DRIFT (proposed) on the VoD validation set. Blue/green/red boxes indicate car, pedestrian, and cyclist detections, respectively. All images are trimmed.\", \"page\": 7, \"index\": 3, \"width\": 1026, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2603.09695v2/fig-004.webp\", \"caption\": \"Fig. 5: Visualization of free-road ground truth and prediction on perciv-scenes-2 validation set on BEV. Yellow segment indicate free-road region.\", \"page\": 7, \"index\": 4, \"width\": 471, \"height\": 537}, {\"url\": \"assets/figures/arxiv/2603.09695v2/fig-005.webp\", \"caption\": \"Fig. 3: Model architecture. The top figure shows the overall architecture of DRIFT, which consists of a point path, a pillar path, and feature sharing blocks. The feature after block i: Mi×Ci in Hi×Wi refers to a sparse pillar representation tensor with shape Mi × Ci, from BEV grid size Hi × Wi, where Mi is the number of non-empty pillars and Ci is the number of channels. (a) Point transformer block. (b) Pillar transformer block. (c1) Feature sharing block with add or concatenation fusion. (c2) Feature sharing block with cross-attention fusion.\", \"page\": 4, \"index\": 5, \"width\": 1037, \"height\": 751}]"
motivation: 4D雷达虽具备全天候优势但点云稀疏，现有方法难以同时有效捕捉局部细节和全局上下文信息。
method: 提出一种双路径架构，结合点表征与柱体表征，并通过多阶段特征共享层实现两种表征的互补融合。
result: "在VoD数据集上，DRIFT的平均精度（mAP）达到52.6%，显著超越了CenterPoint等主流基准模型。"
conclusion: 双路径特征融合机制能有效弥补4D雷达点云稀疏的缺陷，是提升自动驾驶感知性能的有效方案。
---

## 摘要
4D 雷达能够提供 3D 点云数据以及多普勒速度，因其低成本和在恶劣天气条件下的鲁棒性，成为现代自动驾驶系统中有吸引力的组件。然而，其点云密度显著低于激光雷达（LiDAR）传感器。因此，不仅要利用局部上下文信息，还要利用全局场景上下文信息，这一点至关重要。本文提出了 DRIFT，这是一种通过双路径架构有效捕获并融合局部和全局上下文的模型。该模型包含一个用于聚合细粒度局部特征的点路径（point path）和一个用于编码粗粒度全局特征的柱体路径（pillar path）。这两条并行路径通过多个阶段的新型特征共享层相互交织，从而能够充分利用这两种表示。DRIFT 在广泛使用的 View-of-Delft (VoD) 数据集和专有的内部数据集上进行了评估。实验结果表明，它在目标检测和/或空闲道路估计任务上优于基准模型。例如，DRIFT 在 VoD 数据集上实现了 52.6% 的平均精度均值（mAP），而 CenterPoint 的 mAP 为 45.4%。

## Abstract
4D radars, which provide 3D point cloud data along with Doppler velocity, are attractive components of modern automated driving systems due to their low cost and robustness under adverse weather conditions. However, they provide a significantly lower point cloud density than LiDAR sensors. This makes it important to exploit not only local but also global contextual scene information. This paper proposes DRIFT, a model that effectively captures and fuses both local and global contexts through a dual-path architecture. The model incorporates a point path to aggregate fine-grained local features and a pillar path to encode coarse-grained global features. These two parallel paths are intertwined via novel feature-sharing layers at multiple stages, enabling full utilization of both representations. DRIFT is evaluated on the widely used View-of-Delft (VoD) dataset and a proprietary internal dataset. It outperforms the baselines on the tasks of object detection and/or free road estimation. For example, DRIFT achieves a mean average precision (mAP) of 52.6% (compared to, say, 45.4% of CenterPoint) on the VoD dataset.

---

## 论文详细总结（自动生成）

以下是对论文《DRIFT: Dual-Representation Inter-Fusion Transformer for Automated Driving Perception with 4D Radar Point Clouds》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：4D 毫米波雷达虽然具备全天候工作能力、低成本且能提供多普勒速度，但其生成的**点云极其稀疏且噪声大**。
*   **研究动机**：传统的激光雷达（LiDAR）感知模型（如仅依赖局部特征）直接迁移到雷达数据时效果不佳。由于雷达点云稀疏，仅靠局部信息难以准确识别目标（如行人和自行车），必须同时结合**细粒度的局部细节**和**粗粒度的全局上下文信息**。

### 2. 方法论：DRIFT 架构
DRIFT 提出了一种**双路径并行 Transformer 架构**，核心思想是通过两种数据表示的深度交织来弥补雷达点云的缺陷：
*   **点路径 (Point Path)**：直接处理原始点云（$N \times 7$ 维张量，含坐标、RCS、多普勒速度等）。利用 **Point Transformer 块**进行下采样和局部特征聚合，保留细粒度的空间细节。
*   **柱体路径 (Pillar Path)**：将点云体素化为 2D 鸟瞰图（BEV）网格。利用 **Pillar Transformer 块**（结合全局自注意力机制和稀疏卷积）来捕捉长距离的场景上下文。
*   **关键技术：特征共享块 (Feature Sharing Blocks)**：
    *   在骨干网络的多个阶段（Stage）插入共享层，实现双向信息流。
    *   **Pillar-to-Point**：采用类似 PointPainting 的方法，将全局柱体特征映射回每个点。
    *   **Point-to-Pillar**：将点特征重新体素化并融合到柱体表示中。
    *   **融合策略**：探索了相加（Add）、拼接（Concat）和**交叉注意力（Cross-Attention）**三种方式，其中交叉注意力效果最佳。
*   **全稀疏实现**：整个模型基于稀疏数据表示构建，以确保实时性。

### 3. 实验设计
*   **数据集**：
    1.  **View-of-Delft (VoD)**：公开的 4D 雷达数据集，包含 8.7k 帧数据。
    2.  **perciv-scenes-2**：专有的内部大规模数据集，包含 130k 帧数据，涵盖高速公路和城市场景。
*   **任务与 Benchmark**：
    *   **目标检测**：检测汽车、行人和自行车。对比基准包括 PointPillars、CenterPoint、PillarNeXt、RadarPillars、MVFAN、MUFASA 和 SMURF。
    *   **空闲道路估计 (Free-road Segmentation)**：识别可行驶区域。
*   **评价指标**：mAP（全区域及 ROI 区域）、NDS（nuScenes 检测得分）、IoU（针对道路分割）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：在 VoD 数据集上训练 80 个 epoch，在内部数据集上训练 16 个 epoch。
*   **推理性能**：
    *   **延迟**：加法融合模式下为 16.4 ms，交叉注意力模式下为 20.0 ms（具备实时感知能力）。
    *   **显存**：验证时占用约 4.92 GB 至 7.74 GB。
    *   **参数量**：约 6.63 M 至 7.45 M。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的对比实验和消融实验。
*   **消融实验**：
    *   验证了单路径 vs 双路径的差异。
    *   验证了 Transformer 块在不同阶段的作用。
    *   对比了三种不同的特征融合策略（Add/Concat/Attention）。
    *   分析了单向共享 vs 双向共享的影响。
*   **充分性评价**：实验设计非常充分且客观。通过在公开数据集和更大规模的私有数据集上同时验证，证明了模型的泛化能力。每组消融实验均运行三次并取均值/标准差，增强了结果的可信度。

### 6. 主要结论与发现
*   **性能领先**：DRIFT 在 VoD 数据集上达到了 **52.6% mAP**，显著超过了之前的 SOTA 模型（如 SMURF 的 51.0% 和 CenterPoint 的 45.4%）。
*   **小目标优势**：在行人和自行车检测上提升尤为明显，证明了局部与全局特征融合对稀疏目标的识别至关重要。
*   **预训练增益**：在大规模数据集上预训练后，DRIFT 的性能进一步提升，而基准模型 CenterPoint 提升不明显，说明 DRIFT 具有更强的模型容量和学习潜力。

### 7. 优点与亮点
*   **双重表示深度融合**：不同于以往只在末端融合的方法，DRIFT 在骨干网络的多阶段进行双向交互，使特征提取过程更加互补。
*   **Transformer 的巧妙应用**：针对雷达点云的稀疏性，在早期阶段就引入全局自注意力，有效扩大了感受野。
*   **多任务通用性**：模型不仅在目标检测上表现优异，在道路分割任务中也展现了良好的适配性。

### 8. 不足与局限
*   **数据依赖性**：Transformer 架构通常需要大量数据，论文也提到模型在更大规模数据集上表现更好，这可能限制其在小规模数据集上的发挥。
*   **计算复杂度**：虽然达到了实时性要求，但相比于纯卷积或单路径模型，双路径及交叉注意力机制增加了计算开销和实现复杂度。
*   **任务覆盖**：目前仅验证了检测和分割，尚未在点云语义分割或实例分割等更细粒度的任务上进行评估。

（完）
