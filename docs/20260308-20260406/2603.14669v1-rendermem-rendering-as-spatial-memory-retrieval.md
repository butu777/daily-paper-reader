---
title: "RenderMem: Rendering as Spatial Memory Retrieval"
authors: "JooHyun Park, HyeongYeop Kang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14669v1"
tags: ["query:wm"]
score: 9.0
evidence: 具备几何接地的具身推理空间记忆框架
tldr: 将渲染视为空间记忆检索接口，使具身智能体能够直接对 3D 可见性和视线进行推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14669v1/fig-001.webp\", \"caption\": \"Fig. 1: RenderMem retrieves spatial evidence from a stored 3D scene by rendering query-conditioned views, which serve as visual memory readouts for vision–language reasoning about visibility and object state.\", \"page\": 2, \"index\": 1, \"width\": 721, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2603.14669v1/fig-002.webp\", \"caption\": \"Fig. 2: Overview of the RenderMem pipeline. (a) A renderable 3D scene with an object list serves as spatial memory. (b) Given a question, RenderMem decides whether rendering is needed and selects a rendering mode and object anchors. Surround rendering captures multiple views around an object, while directional rendering generates a source-to-target viewpoint for visibility reasoning. The rendered images are used to answer the user question.\", \"page\": 6, \"index\": 2, \"width\": 716, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2603.14669v1/fig-003.webp\", \"caption\": \"Fig. 3: RenderMem’s robustness to imperfect scene representations. (a) Performance under decreasing reconstruction fidelity, simulated with blur-only (bo) and blur+ghosting (bg). (b) Performance under increasing localization perturbation applied to object bounding spheres.\", \"page\": 12, \"index\": 3, \"width\": 710, \"height\": 250}]"
motivation: 具备几何接地的具身推理空间记忆框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
具身推理本质上是视点相关的：什么是可见的、被遮挡的或可达的，关键取决于智能体所处的位置。然而，现有的具身智能体空间记忆系统通常存储多视图观测或以对象为中心的抽象，这使得难以进行具有显式几何基础的推理。我们提出了 RenderMem，这是一个将渲染视为 3D 世界表示与空间推理之间接口的空间记忆框架。RenderMem 不存储固定的观测结果，而是维护一个 3D 场景表示，并通过从查询所隐含的视点渲染场景，生成以查询为条件的视觉证据。这使得具身智能体能够直接从任意视角对视线、可见性和遮挡进行推理。RenderMem 与现有的视觉语言模型完全兼容，且无需对标准架构进行任何修改。在 AI2-THOR 环境中的实验表明，与之前的记忆基准相比，RenderMem 在视点相关的可见性和遮挡查询方面表现出持续的改进。

## 速览
**TLDR**：具身智能推理高度依赖视角，但现有空间记忆系统难以处理复杂的几何遮挡关系。本文提出RenderMem，将渲染视为3D世界表示与空间推理之间的接口。该框架维护3D场景表示，并根据查询需求从特定视角渲染视觉证据，使智能体能直接推理视线、可见性和遮挡。在AI2-THOR环境中的实验证明，该方法显著提升了视角相关任务的性能。 \
**Motivation**：现有的具身智能空间记忆系统缺乏显式的几何基础，难以处理与视角相关的可见性和遮挡推理问题。 \
**Method**：提出RenderMem框架，通过维护3D场景表示并根据查询动态渲染特定视角的图像，作为视觉语言模型的推理依据。 \
**Result**：在AI2-THOR环境下的实验表明，RenderMem在处理视角相关的可见性和遮挡查询方面优于现有的记忆基准模型。 \
**Conclusion**：将渲染作为空间记忆检索的接口，能有效增强具身智能体对复杂3D环境的几何推理能力。

---

## Abstract
Embodied reasoning is inherently viewpoint-dependent: what is visible, occluded, or reachable depends critically on where the agent stands. However, existing spatial memory systems for embodied agents typically store either multi-view observations or object-centric abstractions, making it difficult to perform reasoning with explicit geometric grounding. We introduce RenderMem, a spatial memory framework that treats rendering as the interface between 3D world representations and spatial reasoning. Instead of storing fixed observations, RenderMem maintains a 3D scene representation and generates query-conditioned visual evidence by rendering the scene from viewpoints implied by the query. This enables embodied agents to reason directly about line-of-sight, visibility, and occlusion from arbitrary perspectives. RenderMem is fully compatible with existing vision-language models and requires no modification to standard architectures. Experiments in the AI2-THOR environment show consistent improvements on viewpoint-dependent visibility and occlusion queries over prior memory baselines.

---

## 论文详细总结（自动生成）

### RenderMem: Rendering as Spatial Memory Retrieval 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能体（Embodied Agents）在物理世界中的推理本质上是**视点相关（Viewpoint-dependent）**的。例如，“从走廊看，灭火器是否被柜子遮挡？”这类问题要求智能体具备几何接地（Geometric Grounding）能力。
然而，现有的空间记忆系统存在局限：
*   **视图记忆**：受限于已观察过的离散视角，难以处理新视角或对象中心的查询。
*   **对象中心记忆**：将场景抽象为关系图，缺乏精确的视线（Line-of-sight）几何建模。
*   **3D 场景表示**：虽然包含几何信息，但难以直接与语言模型（LLM/VLM）集成。
**研究动机**：将“渲染”重新定义为空间记忆的“读取”操作，通过动态生成符合查询需求的视觉证据，弥合 3D 几何与语言推理之间的鸿沟。

#### 2. 论文提出的方法论
RenderMem 的核心思想是：**不存储固定观测，而是维护一个可渲染的 3D 场景状态，并根据查询实时合成视觉证据。**

*   **场景表示**：采用统一的可渲染 3D 表示（如 Mesh、NeRF 或 3DGS），并辅以轻量级的对象列表。每个对象被简化为**包围球（Bounding Sphere）**，包含中心点 $c_i$ 和半径 $r_i$。
*   **两阶段查询流水线**：
    1.  **渲染决策（Rendering Decision）**：判断问题是否需要视觉证据。如“房间里有几个椅子”可直接从对象列表回答，而“电视开了吗”则触发渲染请求。
    2.  **渲染规格（Rendering Specification）**：确定渲染模式和对象锚点。
*   **关键渲染算法**：
    *   **环绕渲染（Surround Rendering）**：在目标对象周围均匀采样 $K$ 个相机位姿，确保对象在视场（FOV）内完整可见，用于推理对象属性或状态。
    *   **定向渲染（Directional Rendering）**：根据源对象和目标对象的位置，计算从源指向目标的特定视角，用于推理可见性和遮挡关系。
*   **推理过程**：将渲染出的图像序列输入预训练的视觉语言模型（如 Qwen2.5-VL），结合原始问题生成最终答案。

#### 3. 实验设计
*   **数据集/场景**：基于 **AI2-THOR** 环境，涵盖 iTHOR、RoboTHOR 和 ProcTHOR 场景。
*   **Benchmark 构成**：
    1.  **静态属性与计数**：评估基础对象推理。
    2.  **动态属性与计数**：评估环境交互（如打开电视、切开苹果）后的记忆更新能力。
    3.  **视点相关可见性**：评估从特定空间位置出发的几何推理。
*   **对比方法**：
    *   **Multi-view retrieval**：基于 CLIP 的多视图检索。
    *   **Concept Graphs**：以对象为中心的场景图表示。
    *   **3D-Mem**：结合对象列表和视图快照的混合记忆系统。

#### 4. 资源与算力
*   **模型使用**：实验统一使用 **Qwen2.5-VL-7B** 作为视觉推理后端。
*   **算力说明**：论文**未明确说明**具体的训练时长、GPU 型号或数量。由于该方法主要依赖预训练的 VLM 进行零样本（Zero-shot）或上下文推理，且渲染过程在仿真器中完成，其核心开销在于 3D 重建和 VLM 的推理延迟，而非大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 180 个场景中进行了测试，涵盖了静态、动态及复杂的可见性查询。
*   **消融与鲁棒性实验**：
    *   对比了 1-step、2-step 和 3-step 的查询决策策略。
    *   进行了**鲁棒性分析**：模拟了重建失真（模糊、重影）和定位扰动（包围球中心和半径的噪声）。
*   **评价指标**：采用 Binary LLM Match（由外部 LLM 判断预测答案与真值的语义一致性）。
*   **充分性评价**：实验设计较为全面，特别是针对动态场景和不完美重建的讨论，增强了结果的说服力。对比基准涵盖了当前主流的空间记忆范式，实验过程相对客观公平。

#### 6. 论文的主要结论与发现
*   **可见性推理的突破**：RenderMem 在视点相关可见性任务上显著优于所有基准（平均得分约 0.79，而 3D-Mem 仅为 0.43），证明了动态渲染在几何推理中的必要性。
*   **动态场景支持**：由于渲染直接作用于最新的 3D 状态，RenderMem 无需显式重写记忆即可自然适应环境变化（如物体状态改变）。
*   **鲁棒性**：即使在存在中度重建伪影或定位噪声的情况下，RenderMem 依然能保持稳定的推理性能，表现出较强的实用潜力。

#### 7. 优点（亮点）
*   **架构解耦**：将 3D 几何处理与语言推理分离，无需修改现有的 VLM 架构，具有极强的通用性。
*   **几何接地**：通过定向渲染解决了传统记忆系统难以处理的“视线”问题。
*   **灵活性**：能够根据查询需求动态调整观察视角，比存储固定图像的系统更高效、更精准。

#### 8. 不足与局限
*   **实例消歧困难**：当场景中存在多个同类物体（如“窗户边的椅子”）时，仅靠类别 ID 和包围球可能难以精确锁定目标。
*   **存储开销**：高质量的可渲染 3D 场景（如 Mesh 或 3DGS）占用空间较大（数百 MB 每场景），在大规模环境部署时面临存储瓶颈。
*   **依赖重建质量**：推理精度高度依赖于底层 3D 重建的保真度，在极端低质量的重建下，可见性推理性能下降明显。

（完）
