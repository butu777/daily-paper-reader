---
title: "Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding"
authors: "Haibo Wang, Zihao Lin, Zhiyang Xu, Lifu Huang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00528v2"
tags: ["query:wm"]
score: 9.0
evidence: 利用2D到3D重建范式进行3D视觉定位与空间语义解析
tldr: 提出TAB框架，通过解耦空间语义与几何结构，实现零样本3D视觉定位。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00528v2/fig-001.webp\", \"caption\": \"Figure 1: (1) Top: Previous methods rely on preprocessed 3D point clouds, degrading the task into proposal matching. (2) Bottom: Our TAB operates directly on RGB-D streams. Through an iterative Think-Act-Build process, the agent reconstructs the target object.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00528v2/fig-002.webp\", \"caption\": \"Table 1: 3D Visual Grounding results on ScanRefer. \\\"w/o PC\\\" denotes methods that do not rely on 3D point clouds as input. † means results with two stage training.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.00528v2/fig-003.webp\", \"caption\": \"Table 2: 3D Visual Grounding results on Nr3D (w/o GT object class). \\\"w/o PC\\\" denotes methods that do not rely on 3D point clouds as input.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.00528v2/fig-004.webp\", \"caption\": \"Figure 2: The \\\"Think, Act, Build (TAB)\\\" framework. Guided by an expert 3D-VG Skill, the VLM agent reasons about the task, invokes visual tools, and reconstructs the target.\", \"page\": 4, \"index\": 4, \"width\": 824, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.00528v2/fig-005.webp\", \"caption\": \"Figure 4: Qualitative comparison of different components in TAB.\", \"page\": 9, \"index\": 5, \"width\": 417, \"height\": 352}]"
motivation: 利用2D到3D重建范式进行3D视觉定位与空间语义解析。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
3D 视觉定位 (3D-VG) 旨在通过自然语言描述在 3D 场景中定位物体。虽然利用视觉语言模型 (VLM) 的最新进展已经探索了零样本的可能性，但它们通常受限于依赖预处理 3D 点云的静态工作流，本质上将定位退化为了候选框匹配。为了摆脱这种依赖，我们的核心动机是将任务解耦：利用 2D VLM 解析复杂的空间语义，同时依靠确定性的多视图几何来实例化 3D 结构。基于这一见解，我们提出了“思考、行动、构建 (TAB)”，这是一个动态智能体框架，它将 3D-VG 任务重新表述为直接在原始 RGB-D 流上运行的生成式 2D 到 3D 重建范式。具体而言，在专门的 3D-VG 技能引导下，我们的 VLM 智能体动态调用视觉工具，在 2D 帧中跟踪并重建目标。关键在于，为了克服由严格的 VLM 语义跟踪引起的多视图覆盖不足问题，我们引入了语义锚定几何扩展，该机制首先在参考视频剪辑中锚定目标，然后利用多视图几何将其空间位置传播到未观察到的帧中。这使得智能体能够通过相机参数聚合这些多视图特征，从而“构建”目标的 3D 表示，将 2D 视觉线索直接映射到 3D 坐标。此外，为了确保评估的严谨性，我们识别了现有基准测试中存在的指代歧义和类别错误等缺陷，并手动修正了错误的查询。在 ScanRefer 和 Nr3D 上的大量实验表明，我们的框架完全依赖开源模型，其性能显著优于之前的零样本方法，甚至超过了全监督基准。

## 速览
**TLDR**：本研究针对3D视觉定位（3D-VG）中现有零样本方法过度依赖预处理点云的问题，提出了TAB（Think, Act, Build）智能体框架。该框架将3D-VG重新定义为从原始RGB-D流到3D的生成式重建过程，利用2D VLM处理复杂语义，并通过多视图几何构建3D结构。通过引入语义锚定几何扩展机制，TAB实现了跨帧目标追踪与3D坐标映射，在ScanRefer和Nr3D基准上显著超越了现有零样本及部分全监督方法。 \
**Motivation**：旨在摆脱对预处理3D点云的依赖，通过解耦语义理解与几何重建，利用2D VLM和多视图几何直接从原始视频流中定位3D目标。 \
**Method**：提出了TAB框架，通过VLM智能体动态调用视觉工具进行目标追踪，并利用语义锚定几何扩展机制将2D视觉线索聚合为3D表示。 \
**Result**：在ScanRefer和Nr3D数据集上的实验表明，该框架不仅大幅领先于现有的零样本方法，甚至在性能上超过了部分全监督基线模型。 \
**Conclusion**：TAB框架证明了通过智能体驱动的2D-to-3D重建范式，可以在无需3D标注的情况下实现高效且精准的3D视觉定位。

---

## Abstract
3D Visual Grounding (3D-VG) aims to localize objects in 3D scenes via natural language descriptions. While recent advancements leveraging Vision-Language Models (VLMs) have explored zero-shot possibilities, they typically suffer from a static workflow relying on preprocessed 3D point clouds, essentially degrading grounding into proposal matching. To bypass this reliance, our core motivation is to decouple the task: leveraging 2D VLMs to resolve complex spatial semantics, while relying on deterministic multi-view geometry to instantiate the 3D structure. Driven by this insight, we propose "Think, Act, Build (TAB)", a dynamic agentic framework that reformulates 3D-VG tasks as a generative 2D-to-3D reconstruction paradigm operating directly on raw RGB-D streams. Specifically, guided by a specialized 3D-VG skill, our VLM agent dynamically invokes visual tools to track and reconstruct the target across 2D frames. Crucially, to overcome the multi-view coverage deficit caused by strict VLM semantic tracking, we introduce the Semantic-Anchored Geometric Expansion, a mechanism that first anchors the target in a reference video clip and then leverages multi-view geometry to propagate its spatial location across unobserved frames. This enables the agent to "Build" the target's 3D representation by aggregating these multi-view features via camera parameters, directly mapping 2D visual cues to 3D coordinates. Furthermore, to ensure rigorous assessment, we identify flaws such as reference ambiguity and category errors in existing benchmarks and manually refine the incorrect queries. Extensive experiments on ScanRefer and Nr3D demonstrate that our framework, relying entirely on open-source models, significantly outperforms previous zero-shot methods and even surpasses fully supervised baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：传统的3D视觉定位（3D-VG）高度依赖昂贵的、预先标注的3D点云数据。现有的零样本（Zero-Shot）方法虽然引入了视觉语言模型（VLM），但大多采用“静态工作流”，将任务简化为在预处理的点云提案中进行“匹配”，这限制了模型在缺乏预构建3D地图的真实开放环境中的应用。
*   **研究动机**：作者希望解耦3D-VG任务，利用2D VLM强大的空间语义推理能力处理“语言-视觉”对齐，同时利用确定性的多视图几何（Multi-view Geometry）来构建物体的3D物理结构，从而实现直接基于原始RGB-D视频流的生成式定位。

### 2. 方法论：Think, Act, Build (TAB) 框架
TAB框架将3D-VG重新定义为一个动态的智能体推理与重建过程：
*   **Think（思考）**：基于预设的“3D-VG Skill”专家知识蓝图，VLM智能体（如Qwen3-VL）对查询语句进行解析，并规划后续的视觉工具调用步骤。
*   **Act（行动）**：智能体调用工具库（如Grounding DINO进行检测、SAM3进行分割、Query Analysis进行语义解析）在2D帧中识别和锁定目标。
*   **Build（构建）**：这是该框架的核心，包含两个关键子阶段：
    *   **语义时间扩展（STE）**：在参考帧附近进行双向语义追踪，通过VLM验证一致性，获取初始的2D掩码序列。
    *   **语义锚定几何扩展（MGE）**：利用STE阶段获得的掩码通过逆投影（Inverse Projection）计算物体的3D几何质心（$P_{centroid}$）。随后，利用相机内参（$K$）和外参（$T_{c2w}$）将该质心投影回所有视频帧，通过深度验证和遮挡检查（Z-buffer check）找回因语义失效（如视角剧变）而丢失的目标掩码。
*   **重建逻辑**：将所有获取的多视图掩码像素点云化，经过离群点去除（SOR）和聚类（DBSCAN），最终计算出轴向对齐的3D边界框（BBox）。

### 3. 实验设计
*   **数据集与场景**：主要在 **ScanRefer** 和 **Nr3D** 两个主流3D视觉定位基准上进行测试，这两个数据集均基于ScanNet室内场景。
*   **Benchmark 修正**：作者指出原始基准存在指代歧义、类别错误和空间位置描述错误，并手动修正了这些噪声数据，提出了更严谨的评估版本。
*   **对比方法**：
    *   **全监督方法**：如ScanRefer, 3DVG-T, BUTD-DETR, EDA, G3-LQ等。
    *   **零样本方法**：如LLM-Grounder, ZSVG3D, SeeGround, VLM-Grounder, SeqVLM, SPAZER等。

### 4. 资源与算力
*   **模型配置**：核心智能体采用 **Qwen3-VL-32B**，视觉工具包括 Grounding DINO 和 SAM3。
*   **算力说明**：论文**未明确说明**具体的GPU型号、数量及推理总时长。由于该框架属于零样本推理范式，不涉及大规模训练，但涉及多帧图像的VLM推理和几何计算，其计算开销主要集中在VLM的API调用或本地推理上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集上进行了全面测试，涵盖了“唯一目标”、“多目标”、“易/难”、“视角依赖/独立”等多个子维度。
    *   **消融实验**：针对STE（语义扩展）和MGE（几何扩展）模块分别进行了消融，验证了每个组件对提升定位精度的贡献。
    *   **定性分析**：提供了可视化对比，展示了在语义追踪失效时，几何扩展如何补全物体结构。
*   **充分性评价**：实验设计较为充分且客观。通过修正Benchmark噪声，作者确保了评估的公平性，避免了因原始标注错误导致的性能误判。

### 6. 主要结论与发现
*   **性能领先**：TAB在不依赖预处理点云的情况下，在ScanRefer上的Acc@0.25达到71.2%，显著超过了之前所有零样本方法，甚至在某些指标上优于全监督基准。
*   **几何补偿语义**：实验证明，纯语义追踪在视角变化大时非常脆弱，而引入基于3D质心的几何投影能有效弥补多视图覆盖不足的问题。
*   **3D先验并非必须**：该研究证明了直接从2D视频流生成3D结构是可行的，且精度足以匹配甚至超越依赖预建地图的方法。

### 7. 优点（亮点）
*   **解耦设计**：巧妙地将VLM的语义长处与多视图几何的确定性结合，避开了VLM在精确空间坐标计算上的短板。
*   **动态智能体流**：相比静态Pipeline，TAB具备容错和自适应能力（如阈值动态调整）。
*   **基准贡献**：对ScanRefer和Nr3D的修正为后续研究提供了更高质量的测试环境。
*   **无需3D训练**：完全依赖开源基础模型，具有极强的通用性和可扩展性。

### 8. 不足与局限
*   **深度图依赖**：该方法高度依赖RGB-D流中的深度图质量。在深度传感器噪声大或缺失的场景下，重建精度会受损。
*   **计算延迟**：由于需要处理多帧图像并进行多次VLM推理，实时性可能不如单阶段的全监督模型。
*   **静态场景假设**：目前的几何扩展机制主要针对静态场景，在物体频繁移动的动态场景中，基于固定质心的投影可能会失效。

（完）
