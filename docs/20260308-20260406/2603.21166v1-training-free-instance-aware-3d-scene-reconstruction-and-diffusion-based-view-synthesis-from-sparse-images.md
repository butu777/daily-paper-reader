---
title: Training-Free Instance-Aware 3D Scene Reconstruction and Diffusion-Based View Synthesis from Sparse Images
authors: "Jiatong Xia, Lingqiao Liu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21166v1"
tags: ["query:3d"]
score: 9.0
evidence: 基于稀疏图像的3D场景重建与视图合成
tldr: 一种无需训练的系统，用于从稀疏RGB图像中进行实例感知的3D室内场景重建和渲染。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21166v1/fig-001.webp\", \"caption\": \"Fig. 4. Main results. We present the main experimental results, including instance segmentation, point cloud reconstruction, novel view synthesis, and scene edits through object removal. Each row corresponds to a test scene. The left column shows the instance segmentation results. The center section labeled “Original” displays the reconstructed point cloud (top) and novel view synthesis results (bottom, with ground truth images on the top and rendered results on the bottom) of original input images. The two columns on the right illustrate two different object removal results, with the removed objects are highlighted with black dashed boxes in the point clouds\", \"page\": 7, \"index\": 1, \"width\": 1044, \"height\": 1169}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-002.webp\", \"caption\": \"Fig. 8. Anomaly points elimination. We show the reconstruction, warping, and rendering visualization results before and after anomaly point elimination. On the left, the baseline method without anomaly point removal shows a large number of outlier points in the reconstructed point cloud, which significantly degrades the warping and final rendering results. In contrast, our proposed method effectively eliminates these anomaly points, resulting in cleaner reconstructions and better rendering quality.\", \"page\": 10, \"index\": 2, \"width\": 1064, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-003.webp\", \"caption\": \"Fig. 9. Rendering masks.We show the object removal results with and without applying rendering masks to the source images. Without rendering masks, the rendered outputs tend to preserve the full geometry from the source images, even after the object has been removed from the point cloud. This leads to a failure in reflecting the intended object removal in the final rendered image.\", \"page\": 10, \"index\": 3, \"width\": 1022, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-004.webp\", \"caption\": \"Fig. 2. Framework. We apply a feed-forward a feed-forward model (e.g., MV-DUSt3R) to predict the initial point cloud from the input unposed sparse images, followed by a novel warping-based anomaly points removal strategy to eliminate unreliable points in the initial point cloud, forming a clean and accurate scene point cloud. For 2D-to-3D instance segmentation, we first utilize a foundational segmentation model (e.g., SAM) to generate initial segmentation masks. We then introduce a warping-based instance unification strategy to align instances across frames. By associating the instance masks with the corresponding pointmaps, we obtain an instance-segmented 3D point cloud. The instance-segmented 3D point cloud facilitates downstream tasks such as object-level editing. Finally, the point cloud can be directly rendered into high-quality 2D images through our designed diffusion-based rendering pipeline.\", \"page\": 3, \"index\": 4, \"width\": 1064, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-005.webp\", \"caption\": \"Fig. 5. Novel views rendering. We show the visualization comparisons on novel view synthesis.\", \"page\": 8, \"index\": 5, \"width\": 1064, \"height\": 583}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-006.webp\", \"caption\": \"Table 3. Rendering evaluation on novel views. We show the overall novel views synthesis performance of methods on selected reconstruction scenes. We indicate whether each method requires camera parameter input or per-scene training. We apply our rendering pipeline to the reconstructed point clouds produced by PE3R and MV-DUSt3R for a comparison.\", \"page\": 8, \"index\": 6, \"width\": 756, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-007.webp\", \"caption\": \"Fig. 10. Editing tasks example. We show the original image of the view on the left. We apply a object point-cloud translation (red arrow) in the first row, followed by warping and rendering. We modify the object (red box) color of the input images before reconstruction, and the final rendered results reflect the applied color changes.\", \"page\": 12, \"index\": 7, \"width\": 510, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-008.webp\", \"caption\": \"Fig. 6. Object removal comparison. We present the visualization results of removing the same object under two different views, with the original groundtruth image on the left as reference. For each method, we display the rendered results before and after object removal. The removed object is highlighted with a red box in our results.\", \"page\": 9, \"index\": 8, \"width\": 790, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2603.21166v1/fig-009.webp\", \"caption\": \"Fig. 7. Instance segmentation. We present a visual comparison between our 2D-to-3D instance segmentation results and those of PE3R, with the reference raw 3D point cloud on the left. While PE3R’s reconstructed point cloud appears to be of lower quality than ours, it is worth noting that their segmentation results are produced independently and are not affected by the reconstruction quality.\", \"page\": 9, \"index\": 9, \"width\": 977, \"height\": 262}]"
motivation: 基于稀疏图像的3D场景重建与视图合成。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
我们提出了一种新颖的、无需训练的系统，用于从一组稀疏的、无位姿的 RGB 图像中重建、理解和渲染三维室内场景。与需要密集视图和逐场景优化的传统辐射场方法不同，我们的流水线在无需任何训练或位姿预处理的情况下实现了高保真结果。该系统集成了三项关键创新：(1) 一个鲁棒的点云重建模块，利用基于扭曲（warping）的异常移除策略过滤不可靠的几何结构；(2) 一种扭曲引导的 2D 到 3D 实例提升机制，将 2D 分割掩码传播为一致的、实例感知的 3D 表示；(3) 一种新颖的渲染方法，将点云投影到新视角，并使用 3D 感知的扩散模型优化渲染效果。我们的方法利用扩散模型的生成能力来补偿缺失的几何信息并增强真实感，特别是在稀疏输入条件下。我们进一步证明，通过仅修改点云，我们的流水线可以自然地支持物体级场景编辑（如实例移除），从而在无需重新训练的情况下合成一致的编辑后视图。我们的结果为无需依赖特定场景优化的、高效且可编辑的三维内容生成确立了新方向。项目主页：https://jiatongxia.github.io/TID3R/

## 速览
**TLDR**：本文提出一种无需训练的3D室内场景重建与渲染系统，仅需少量无位姿RGB图像即可实现。该系统通过基于重投影的异常移除策略构建鲁棒点云，利用2D到3D实例提升机制实现一致的实例感知表示，并结合3D感知扩散模型对投影视图进行细化。该方法不仅在稀疏输入下实现了高保真渲染，还支持无需重训练的对象级场景编辑（如实例删除），为高效、可编辑的3D内容生成开辟了新方向。 \
**Motivation**：针对传统辐射场方法依赖密集视图和耗时的逐场景优化，且难以进行灵活的对象级编辑等局限性。 \
**Method**：整合了基于重投影的点云去噪、2D分割掩码的3D实例提升以及基于扩散模型的视图细化技术。 \
**Result**：在无需位姿预处理和训练的情况下，实现了高质量的3D重建、实例理解及一致的视图合成与编辑。 \
**Conclusion**：该系统证明了结合生成式扩散模型与显式点云表示，可以在无优化前提下实现高效且可编辑的3D场景建模。

---

## Abstract
We introduce a novel, training-free system for reconstructing, understanding, and rendering 3D indoor scenes from a sparse set of unposed RGB images. Unlike traditional radiance field approaches that require dense views and per-scene optimization, our pipeline achieves high-fidelity results without any training or pose preprocessing. The system integrates three key innovations: (1) A robust point cloud reconstruction module that filters unreliable geometry using a warping-based anomaly removal strategy; (2) A warping-guided 2D-to-3D instance lifting mechanism that propagates 2D segmentation masks into a consistent, instance-aware 3D representation; and (3) A novel rendering approach that projects the point cloud into new views and refines the renderings with a 3D-aware diffusion model. Our method leverages the generative power of diffusion to compensate for missing geometry and enhances realism, especially under sparse input conditions. We further demonstrate that object-level scene editing such as instance removal can be naturally supported in our pipeline by modifying only the point cloud, enabling the synthesis of consistent, edited views without retraining. Our results establish a new direction for efficient, editable 3D content generation without relying on scene-specific optimization. Project page: https://jiatongxia.github.io/TID3R/

---

## 论文详细总结（自动生成）

这是一份关于论文《Training-Free Instance-Aware 3D Scene Reconstruction and Diffusion-Based View Synthesis from Sparse Images》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机与背景）
该研究针对的是从**极稀疏（10-20张）且无位姿（Unposed）**的RGB图像中重建三维室内场景并进行高质量渲染的难题。
*   **背景**：传统的神经辐射场（NeRF）或3D高斯泼溅（3DGS）方法通常依赖密集的视图输入和耗时的逐场景优化（Per-scene Optimization）。在 casual capture（随手拍摄）场景下，图像往往稀疏且缺乏精确的相机位姿，导致传统方法难以生成高质量结果，且难以进行物体级别的语义编辑。
*   **核心问题**：如何在无需场景特定训练（Training-free）、无需位姿预处理的前提下，实现具备实例感知能力（Instance-aware）的3D重建、高质量新视角合成及灵活的场景编辑。

### 2. 论文提出的方法论
论文提出了一个统一的、无需训练的流水线，包含三个核心模块：
*   **鲁棒的3D几何重建**：
    *   基于前馈模型（如 MV-DUSt3R）预测初始点云、深度图及相机参数。
    *   **关键技术：基于扭曲（Warping）的异常点消除**。通过多视图深度一致性检查，对比“扭曲深度”与“原生深度”，剔除遮挡或估计错误产生的离群点，从而获得干净的几何结构。
*   **实例感知感知与交互环境**：
    *   **2D到3D实例提升**：利用 SAM 生成单帧掩码，通过几何投影和掩码交并比（mIoU）匹配，将不同视角的 2D 掩码统一合并为 3D 实例标签。
    *   这种结构化表示允许直接在点云空间进行物体删除、平移等编辑操作。
*   **基于扩散模型的渲染流水线**：
    *   将点云投影到目标视角生成稀疏 RGB 图和掩码。
    *   **核心思想**：利用 3D 感知的扩散模型（See3D）作为渲染器，利用其强大的图像先验知识对投影产生的空洞进行修复（Inpainting）和细节增强。
    *   **编辑支持**：在物体移除后，通过对参考图像进行掩码处理，确保扩散模型生成的背景与修改后的几何结构保持一致。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet** 和 **ScanNet++** 室内场景数据集上进行评估。
*   **Benchmark 与对比方法**：
    *   **实例分割**：对比了 DEVA 和 PE3R。
    *   **深度估计**：对比了 DepthAnythingV2、PE3R 和 MV-DUSt3R+。
    *   **新视角合成（NVS）**：对比了 3DGS、2DGS、GaussianGrouping、DNGaussian、CoR-GS、SE-GS（这些均为针对稀疏视图优化的 3DGS 变体）以及 LVSM（基于扩散模型的视频生成方法）。
*   **评估指标**：AP（实例分割）；RMSE、$\delta < 1.25$（深度）；PSNR、SSIM、LPIPS（渲染质量）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX 4090 GPU** 上运行。
*   **耗时**：
    *   **无需位姿处理时间**（传统方法需约 2 分钟）。
    *   **重建阶段**：约 1 分钟。
    *   **推理/渲染阶段**：约 2.5 分钟（10个测试视角）。
    *   **总计**：约 3.5 分钟，远快于 NeRF（约 150 分钟）和 3DGS（约 9 分钟）。
*   **训练时长**：该方法本身是 **Training-free** 的，直接使用预训练的基础模型（MV-DUSt3R, SAM, See3D），无需针对特定场景训练。

### 5. 实验数量与充分性
*   **实验规模**：在 ScanNet 和 ScanNet++ 中选择了多个代表性室内场景（如客厅、卧室等）。
*   **消融实验**：详细验证了“基于扭曲的实例统一策略”、“异常点消除模块”以及“渲染掩码策略”的有效性。
*   **充分性评价**：实验设计较为全面，涵盖了从几何精度、语义分割到最终渲染质量的全方位对比。对比对象不仅包括传统的显式/隐式重建方法，也包括了最新的基于扩散模型的生成式方法，体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：在稀疏视图（10-20张图）条件下，该方法在渲染质量（PSNR/LPIPS）上显著优于经过优化的 3DGS 变体。
*   **鲁棒性**：基于扭曲的异常点剔除能显著提升点云质量，进而改善渲染效果。
*   **编辑能力**：通过简单的点云操作即可实现一致的物体移除和移动，且无需重新优化场景。
*   **无需位姿的优势**：证明了在完全不依赖外部相机参数的情况下，依然可以构建出具有高度一致性的 3D 场景。

### 7. 优点（亮点）
*   **完全无需训练**：极大地降低了 3D 重建的门槛和计算成本，实现了“即插即用”。
*   **端到端一致性**：将几何重建、语义理解和生成式渲染有机结合，解决了点云渲染不真实和辐射场编辑困难的双重痛点。
*   **稀疏视图友好**：在极少输入下表现稳健，利用扩散模型弥补了几何缺失。

### 8. 不足与局限
*   **非实时渲染**：由于核心渲染依赖扩散模型的迭代推理，目前无法达到实时渲染速度（渲染 10 张图需 2.5 分钟）。
*   **位姿对齐敏感性**：在测试给定视角时，需要将预测位姿与真实位姿对齐，对齐精度受限于前馈网络的位姿预测准确度。
*   **场景限制**：目前主要针对室内场景，对于大尺度室外场景或极度复杂的遮挡关系，点云的稀疏性可能会挑战扩散模型的补全能力。
*   **显存占用**：扩散模型和大型 Transformer 重建模型对显存有一定要求。

（完）
