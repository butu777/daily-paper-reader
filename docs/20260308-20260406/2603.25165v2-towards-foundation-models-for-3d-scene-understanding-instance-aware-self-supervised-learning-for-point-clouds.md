---
title: "Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds"
authors: "Bin Yang, Mohamed Abdelsamad, Miao Zhang, Alexandru Paul Condurache"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25165v2"
tags: ["query:wm"]
score: 9.0
evidence: 3D 场景理解的基础模型
tldr: PointINS 是一个面向实例的自监督框架，用于构建 3D 场景理解基础模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25165v2/fig-001.webp\", \"caption\": \"Figure 1. We achieve superior results over state-of-the-art selfsupervised approaches (DOS [2] and Sonata [36]) on both indoor instance (left) and outdoor panoptic segmentation (right).\", \"page\": 1, \"index\": 1, \"width\": 485, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-002.webp\", \"caption\": \"Table 1. Comparison of self-supervised methods on various indoor scene datasets: ScanNet [12], ScanNet200 [30] and S3DIS [21]. For fair comparison, all models deploy the same backbone of Point Transformer v3 (PTv3) [35] and no additional data is used during pre-training. The best and second best results in each setting are highlighted in bold and underline, respectively.\", \"page\": 6, \"index\": 2, \"width\": 872, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-003.webp\", \"caption\": \"Table 2. Comparison of self-supervised methods on the outdoor scene datasets: nuScenes [14] and SemanticKITTI [4].\", \"page\": 6, \"index\": 3, \"width\": 489, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-004.webp\", \"caption\": \"Figure 6. Visualization of predicted centroids ci = xi+Oi (no explicit regularization) from models pre-trained under four configurations. (a) Without regularization, the centroids are scattered irregularly across the scenes. (b) With ODR only, the centroids are aligned more closely with the scene structure but they are not grouped (lack of local coherence). (c) With SCR only, the centroids are partially grouped but spatially scattered. (d) With ODR and SCR, the centroids become tightly concentrated around instances.\", \"page\": 7, \"index\": 4, \"width\": 1006, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-005.webp\", \"caption\": \"Figure 8. Qualitative results of unsupervised instance segmentation. Left: ground-truth instance labels. Right: predictions from PointINS obtained directly from offset clustering without any downstream supervision.\", \"page\": 14, \"index\": 5, \"width\": 496, \"height\": 896}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-006.webp\", \"caption\": \"Figure 5. We monitor the linear probing (LP) performance of a model pre-trained with a recent SSL method [36]. Remarkably, the model reaches 85% of its final LP performance within just 10% of the total training.\", \"page\": 4, \"index\": 6, \"width\": 443, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-007.webp\", \"caption\": \"Figure 3. Overview of PointINS: A point cloud is augmented to two independent views and they are randomly masked. The teacher processes the full input and the student receives only visible points. Both networks share the same architecture. In the semantic branch, features are computed similarity with prototypes Q. A KL-divergence loss Lsem is then applied for distillation. In the offset branch, an offset head maps features into 3D offset vectors. Teacher offsets are first regularized by ODR to align with empirically observed geometric priors. Next, segments obtained from K-means-clustering are used to extract pseudo-instance masks. Those masks help to enhance instance awareness by regularizing local coherence of points. Finally, an offset loss Loff is computed as the second distillation.\", \"page\": 4, \"index\": 7, \"width\": 1031, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-008.webp\", \"caption\": \"Table 9. Pretraining settings for indoor and outdoor point clouds.\", \"page\": 12, \"index\": 8, \"width\": 489, \"height\": 375}]"
motivation: 3D 场景理解的基础模型。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
点云自监督学习（SSL）的最新进展在无需人工标注的情况下显著提升了 3D 场景理解能力。现有方法通过在增强视图之间强制执行特征一致性或通过掩码场景建模来强调语义感知。然而，由此产生的表示在实例定位方面的迁移效果较差，且通常需要全量微调才能获得强大的性能。实例感知是 3D 感知的基本组成部分，因此弥合这一差距对于迈向支持 3D 数据上所有下游任务的真正 3D 基础模型至关重要。在这项工作中，我们介绍了 PointINS，这是一个面向实例的自监督框架，通过几何感知学习来丰富点云表示。PointINS 采用正交偏移分支来共同学习高层语义理解和几何推理，从而产生实例感知能力。我们确定了对于鲁棒实例定位至关重要的两个一致属性，并将其表述为互补的正规化策略：偏移分布正规化（ODR），它将预测的偏移量与经验观察到的几何先验对齐；以及空间聚类正规化（SCR），它通过使用伪实例掩码对偏移量进行正规化来强制执行局部连贯性。通过在五个数据集上的广泛实验，PointINS 在室内实例分割方面平均实现了 +3.5% 的 mAP 提升，在室外全景分割方面实现了 +4.1% 的 PQ 增益，为可扩展的 3D 基础模型铺平了道路。

## 速览
**TLDR**：针对现有3D点云自监督学习方法在实例定位能力上的不足，本文提出了PointINS框架。该框架通过引入正交偏移分支，结合几何感知学习，使模型能够同时掌握高层语义和底层几何推理。通过偏移分布正则化（ODR）和空间聚类正则化（SCR）两种策略，PointINS显著提升了模型在室内实例分割和室外全景分割任务中的表现，为构建通用的3D基础模型奠定了基础。 \
**Motivation**：现有的3D自监督学习方法侧重于语义一致性，但在实例定位任务上表现不佳，限制了其作为通用3D基础模型的能力。 \
**Method**：提出了PointINS框架，通过正交偏移分支学习几何推理，并利用偏移分布正则化（ODR）和空间聚类正则化（SCR）来增强实例感知能力。 \
**Result**：在五个数据集上的实验表明，该方法使室内实例分割的mAP平均提升了3.5%，室外全景分割的PQ提升了4.1%。 \
**Conclusion**：PointINS通过增强实例感知表征，有效弥补了3D自监督学习在定位任务上的短板，推动了3D基础模型的发展。

---

## Abstract
Recent advances in self-supervised learning (SSL) for point clouds have substantially improved 3D scene understanding without human annotations. Existing approaches emphasize semantic awareness by enforcing feature consistency across augmented views or by masked scene modeling. However, the resulting representations transfer poorly to instance localization, and often require full finetuning for strong performance. Instance awareness is a fundamental component of 3D perception, thus bridging this gap is crucial for progressing toward true 3D foundation models that support all downstream tasks on 3D data. In this work, we introduce PointINS, an instance-oriented self-supervised framework that enriches point cloud representations through geometry-aware learning. PointINS employs an orthogonal offset branch to jointly learn high-level semantic understanding and geometric reasoning, yielding instance awareness. We identify two consistent properties essential for robust instance localization and formulate them as complementary regularization strategies, Offset Distribution Regularization (ODR), which aligns predicted offsets with empirically observed geometric priors, and Spatial Clustering Regularization (SCR), which enforces local coherence by regularizing offsets with pseudo-instance masks. Through extensive experiments across five datasets, PointINS achieves on average +3.5% mAP improvement for indoor instance segmentation and +4.1% PQ gain for outdoor panoptic segmentation, paving the way for scalable 3D foundation models.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的3D点云自监督学习（SSL）方法（如基于对比学习或掩码建模的方法）主要侧重于**语义一致性**，即学习对增强视图具有不变性的特征。这种方法虽然在语义分割上表现良好，但忽略了**实例感知（Instance Awareness）**，导致模型在实例分割和全景分割等需要精确几何定位的任务中表现较差，且往往需要全量微调才能达到较好效果。
*   **研究动机**：实例感知是3D感知的基本组成部分。为了构建真正的3D基础模型，SSL表示必须同时具备高层语义理解和底层几何推理能力。

### 2. 论文提出的方法论：PointINS
PointINS 是首个显式设计用于同时学习语义一致性和几何推理的自监督框架。
*   **核心架构**：采用教师-学生（Teacher-Student）自蒸馏架构。
    *   **语义分支**：通过原型聚类机制学习类别级理解。
    *   **偏移分支（Offset Branch）**：预测每个点指向其所属实例几何中心的3D向量。
*   **关键技术细节**：
    *   **偏移分布正则化（ODR）**：利用概率积分变换（PIT）将预测的偏移量幅值和方向与经验几何先验（如长尾分布和球体均匀分布）对齐，防止无监督训练下的模型崩溃。
    *   **空间聚类正则化（SCR）**：利用模型自身的语义特征进行K-means聚类，并在聚类块内通过广度优先搜索（BFS）构建局部k近邻图，生成“伪实例掩码”，强制局部点向共同中心收敛。
    *   **偏移自蒸馏**：学生网络通过 $\ell_1$ 损失和余弦相似度损失，学习教师网络经过 ODR 和 SCR 修正后的偏移目标。

### 3. 实验设计
*   **数据集**：
    *   **室内场景**：ScanNet (v2), ScanNet200, S3DIS (Area 5)。
    *   **室外场景**：nuScenes, SemanticKITTI。
*   **Benchmark 与协议**：
    *   **线性探测（Linear Probing）**：冻结主干网络，仅训练线性头。
    *   **解码器探测（Decoder Probing）**：训练标准解码器。
    *   **全量微调（Full Finetuning）**。
*   **对比方法**：对比了 SegContrast, PSA, NOMAE, Sonata, DOS 等最先进的 3D SSL 方法。

### 4. 资源与算力
*   **硬件使用**：
    *   单数据集预训练及下游微调：使用单张 **NVIDIA H200 GPU**。
    *   多数据集大规模预训练：使用两张 **NVIDIA H200 GPU**（分布式数据并行模式）。
*   **训练时长**：引入偏移分支和正则化增加了约 **25%** 的预训练计算开销（例如，ScanNet 预训练从 20 小时增加到 25 小时）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流数据集，进行了语义、实例、全景分割及目标检测等多种任务的评估。
*   **消融实验**：详细验证了 ODR、SCR 的互补性、损失权重 $\lambda_{off}$、K-means 聚类数 $K$、预热比例等超参数的影响。
*   **鲁棒性验证**：测试了 ODR 对不同分布先验（甚至使用无监督聚类 HDBSCAN 估计的先验）的敏感性，证明了方法在无标注情况下的稳健性。
*   **充分性评价**：实验设计非常充分，通过跨数据集探测（Cross-dataset Probing）和极低标签率（0.1%）下的实验，客观地证明了该方法在泛化性和标签效率上的优势。

### 6. 论文的主要结论与发现
*   **性能提升**：PointINS 在室内实例分割上平均提升了 **+3.5% mAP**，在室外全景分割上提升了 **+4.1% PQ**。
*   **语义与几何的互补性**：引入实例感知的几何任务不仅不会损害语义性能，反而能通过增强特征的判别力来协同提升语义分割效果。
*   **基础模型潜力**：通过多数据集联合预训练，PointINS 在线性探测下的表现接近全监督水平，展示了向通用 3D 基础模型迈进的潜力。

### 7. 优点
*   **创新性**：首次将实例偏移预测引入 3D SSL，弥补了现有方法在几何推理上的短板。
*   **正则化策略巧妙**：ODR 和 SCR 分别从全局统计和局部结构两个维度解决了无监督偏移学习的崩溃问题。
*   **兼容性强**：该框架可以无缝集成到现有的教师-学生类 SSL 框架中（如 Sonata 或 DOS）。
*   **标签效率极高**：在仅有 0.1% 标注的情况下，性能远超传统的监督学习和现有的 SSL 方法。

### 8. 不足与局限
*   **性能差距**：尽管提升显著，但在纯线性探测设置下，与全监督性能之间仍存在一定差距。
*   **先验依赖**：ODR 仍需假设偏移量符合某种统计分布（虽然实验证明其对分布形式不敏感）。
*   **维度扩展**：目前仅限于静态 3D 场景，尚未扩展到包含时序信息的 4D 几何推理。
*   **计算开销**：相比基础 SSL 方法，预训练时间增加了约四分之一。

（完）
