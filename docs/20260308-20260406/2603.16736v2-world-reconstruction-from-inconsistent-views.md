---
title: World Reconstruction From Inconsistent Views
authors: "Lukas Höllein, Matthias Nießner"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16736v2"
tags: ["query:wm"]
score: 9.0
evidence: 利用非刚性对齐从不一致的视频扩散帧中重建 3D 世界。
tldr: 提出一种从不一致的视频扩散输出中重建一致性 3D 世界的方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.16736v2/fig-001.webp\", \"caption\": \"Fig. 4: Single video 3D reconstruction. We generate videos with SEVA [91] (top), Gen3C [56] (mid), Wan [76] (bottom) and 3D reconstruct these frames. Inconsistencies in the generations lead to blurry textures for the baselines compared to the corresponding video, and to severe floating artifacts from novel views. In contrast, our method creates 3D consistent worlds with high fidelity beyond the generated perspectives.\", \"page\": 11, \"index\": 1, \"width\": 735, \"height\": 683}, {\"url\": \"assets/figures/arxiv/2603.16736v2/fig-002.webp\", \"caption\": \"Fig. 3: Single video 3D reconstruction. We generate videos with HY-WorldPlay [71] (top), Genie3 [5] (mid), ViewCrafter [88] (bottom) and 3D reconstruct them. Our method optimizes consistent worlds from inconsistent generated frames. Compared to baselines, the renderings are of higher visual fidelity from both input and novel views.\", \"page\": 10, \"index\": 2, \"width\": 735, \"height\": 683}, {\"url\": \"assets/figures/arxiv/2603.16736v2/fig-003.webp\", \"caption\": \"Fig. 7: Large-scale 3D reconstructions. We compare rendering quality on input poses and novel views for entire 360 degree explorable scenes. Our method creates 3D consistent worlds with a high rendering fidelity far beyond the training views. Please see the supplementary material for animated flythroughs.\", \"page\": 14, \"index\": 3, \"width\": 713, \"height\": 472}, {\"url\": \"assets/figures/arxiv/2603.16736v2/fig-004.webp\", \"caption\": \"Fig. 6: Ablation. We compare our full method on rendering quality of input views (top) and novel views (mid), as well as on pointcloud reconstruction quality (bottom). The DA3 [42] prediction contains misalignments that cannot be resolved by only rigid alignment or without correspondences. Removing the color filtering and global optimization steps adversely impact texture quality. Removing our novel inverse deformation loss reduces sharpness. In contrast, our method obtains flat/sharp surfaces which better constrain the 3D reconstruction, yielding high quality rendering results.\", \"page\": 13, \"index\": 4, \"width\": 762, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2603.16736v2/fig-005.webp\", \"caption\": \"Fig. 5: Pointcloud reconstructions. We compare the quality of the reconstructed pointclouds that are used as initialization for Gaussian Splatting [33] optimization in the subsequent stages for each method. Our approach achieves the highest alignment and compelling textures for individual objects with no overlap of multiple surfaces.\", \"page\": 12, \"index\": 5, \"width\": 741, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2603.16736v2/fig-006.webp\", \"caption\": \"Table 2: Ablation study. 3D reconstruction from only rigid alignment, without correspondences, or without backward deformation (“no inv”) negatively impacts 3D consistency and image quality. No point filter or global optimization decrease image fidelity and consistency. Our full method achieves the highest consistency and fidelity.\", \"page\": 12, \"index\": 6, \"width\": 575, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2603.16736v2/fig-007.webp\", \"caption\": \"Table 1: Quantitative comparison. We compare against baselines on the single video 3D reconstruction task and report averaged results across all source video models. Our method achieves the highest 3D and photometric consistency, which underlines its ability to reconstruct worlds from inconsistent frames. We also improve in terms of image fidelity, which signals our images are of higher sharpness and texture detail.\", \"page\": 9, \"index\": 7, \"width\": 650, \"height\": 196}]"
motivation: 利用非刚性对齐从不一致的视频扩散帧中重建 3D 世界。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
视频扩散模型能够生成高质量且多样化的世界；然而，输出序列中的单个帧往往缺乏 3D 一致性，这使得 3D 世界的重建变得困难。为此，我们提出了一种新方法，通过将视频帧非刚性地对齐到一个全局一致的坐标系中来处理这些不一致性，从而产生清晰且细节丰富的点云重建。首先，利用几何基础模型将每一帧提升为像素级的 3D 点云，由于上述不一致性，这些点云包含未对齐的表面。接着，我们提出了一种定制的非刚性迭代帧到模型 ICP 算法，以获得所有帧的初始对齐，随后通过全局优化进一步提升点云的清晰度。最后，我们将该点云作为 3D 重建的初始化，并提出了一种新颖的反向变形渲染损失，从而从不一致的视图中创建高质量且可探索的 3D 环境。我们证明了我们的 3D 场景达到了比基准模型更高的质量，有效地将视频模型转化为具有 3D 一致性的世界生成器。

## 速览
**TLDR**：针对视频扩散模型生成的视频帧之间缺乏3D一致性的问题，本文提出一种新方法，通过非刚性对齐技术将不一致的视频帧整合到全局一致的坐标系中。该方法利用几何基础模型提取点云，结合改进的非刚性ICP算法和全局优化，并引入逆变形渲染损失进行3D重建，成功将视频生成模型转化为高质量、可探索的3D场景生成器。 \
**Motivation**：视频扩散模型生成的画面虽精美但帧间3D一致性差，导致难以直接进行高质量的3D场景重建。 \
**Method**：提出一种非刚性迭代帧到模型ICP算法进行初步对齐，并结合全局优化和新型逆变形渲染损失来构建一致的3D点云与场景。 \
**Result**：实验证明该方法在处理不一致视图时生成的3D场景质量优于现有基准，实现了更清晰、更具细节的重建效果。 \
**Conclusion**：该研究有效解决了视频生成中的不一致性难题，为利用生成式视频模型构建大规模3D一致性世界提供了可行方案。

---

## Abstract
Video diffusion models generate high-quality and diverse worlds; however, individual frames often lack 3D consistency across the output sequence, which makes the reconstruction of 3D worlds difficult. To this end, we propose a new method that handles these inconsistencies by non-rigidly aligning the video frames into a globally-consistent coordinate frame that produces sharp and detailed pointcloud reconstructions. First, a geometric foundation model lifts each frame into a pixel-wise 3D pointcloud, which contains unaligned surfaces due to these inconsistencies. We then propose a tailored non-rigid iterative frame-to-model ICP to obtain an initial alignment across all frames, followed by a global optimization that further sharpens the pointcloud. Finally, we leverage this pointcloud as initialization for 3D reconstruction and propose a novel inverse deformation rendering loss to create high quality and explorable 3D environments from inconsistent views. We demonstrate that our 3D scenes achieve higher quality than baselines, effectively turning video models into 3D-consistent world generators.

---

## 论文详细总结（自动生成）

这篇论文由慕尼黑工业大学（TUM）的研究团队发表，题为《World Reconstruction From Inconsistent Views》。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视频扩散模型（VDM）虽然能生成高质量且多样化的视频，但生成的帧序列在 3D 空间上往往是不一致的（即存在“生成漂移” Generative Drift）。
*   **研究动机**：直接利用这些不一致的视频帧进行 3D 重建（如使用传统的 NeRF 或 Gaussian Splatting）会导致几何对齐错误、渲染模糊和严重的浮空伪影。
*   **目标**：开发一种轻量级的重建方法，将不一致的视频生成转化为全局一致、可实时探索的高质量 3D 世界。

### 2. 核心方法论
论文提出了一种三阶段的重建框架，核心思想是通过**非刚性变形**来抵消生成模型的不一致性：

1.  **场景初始化（Scene Initialization）**：
    *   利用几何基础模型（如 DepthAnything-3）为每一帧预测深度图和相机位姿。
    *   通过反投影将像素提升为 3D 点云。由于帧间不一致，此时的点云包含多个互不重叠的表面。
    *   引入基于权重的体素化置信度过滤，保留可靠的几何点。

2.  **几何对齐（Geometry Alignment）**：
    *   **非刚性迭代帧到模型 ICP**：通过哈希网格 MLP 预测每个点的非刚性变形（扭转坐标 ξ），将各帧点云对齐到参考空间。
    *   **稀疏对应项**：引入图像匹配（RoMa）提供的 2D 对应关系，解决 ICP 难以处理的大范围失配。
    *   **全局非刚性优化**：联合优化所有相机位姿和变形网络，使表面变薄并锐化纹理。

3.  **非刚性高斯泼溅优化（Non-rigid Gaussian Splatting）**：
    *   **逆变形优化**：训练一个逆向变形场，将对齐后的“标准”点云映射回各帧不一致的原始空间。
    *   **非刚性感知渲染损失**：在训练 2D 高斯泼溅（2DGS）时，先将高斯球进行逆向变形再渲染。这样可以在保持 3D 几何一致性的同时，利用不一致的图像观测来学习精细的外观。

### 3. 实验设计
*   **数据集/场景**：使用了多种主流视频扩散模型生成的视频，包括 Wan-2.2、ViewCrafter、Gen3C、Seva、Voyager、Genie3 和 HY-WorldPlay。涵盖室内外多种场景及 360 度环绕视角。
*   **Benchmark**：采用 **WorldScore** 评测基准。
*   **对比方法**：
    *   3DGS-MCMC（从随机初始化开始）。
    *   DA3（基于深度图初始化的 3DGS）。
    *   VGGT-X（基于刚性对齐的最新方法）。
*   **评价指标**：3D 一致性、光度一致性、CLIP-IQA+（图像质量）、CLIP Aesthetic（美学评分）。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA **RTX A6000** GPU。
*   **耗时**：
    *   几何对齐阶段：约 25 分钟（处理 50 帧图像，显存占用约 20GB）。
    *   高斯泼溅优化：约 10 分钟（5000 次迭代，显存占用约 8GB）。
*   **模型规模**：初始点云约 150 万个高斯点。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 种不同的视频生成模型进行了测试，每种模型包含 2-3 个场景，共计 16 个核心测试场景。
*   **消融实验**：非常充分。分别验证了“仅刚性对齐”、“无逆变形损失”、“无点云过滤”、“无稀疏对应项”以及“无全局优化”对最终效果的影响。
*   **客观性**：实验涵盖了从单视频重建到大规模 360 度场景扩展的多种任务，对比了当前最先进的基准，结果显示在一致性和保真度上均有显著提升。

### 6. 主要结论与发现
*   **非刚性是关键**：视频生成模型的不一致性本质上是几何上的非刚性扭曲，仅靠刚性变换（平移/旋转）无法消除伪影。
*   **性能提升**：该方法在 3D 一致性指标上显著优于基准方法（如在 WorldScore 3D 指标上从 60+ 提升至 79+）。
*   **普适性**：该方法可以作为任何视频扩散模型的“后处理插件”，将其转化为一致的 3D 生成器。

### 7. 优点与亮点
*   **轻量且通用**：不需要对庞大的视频扩散模型进行昂贵的微调，仅在重建侧进行优化。
*   **高质量渲染**：通过逆变形渲染损失，成功在不一致的输入下保留了生成视频原有的纹理细节，且消除了浮空伪影。
*   **端到端流程**：从深度估计、非刚性 ICP 到高斯优化，形成了一套完整的从视频到 3D 世界的解决方案。

### 8. 不足与局限
*   **幻觉处理能力有限**：虽然能处理几何漂移，但如果视频模型产生了严重的“幻觉”（如物体突然凭空出现或消失），该方法仍会将其视为静态几何的一部分，导致渲染异常。
*   **计算开销**：虽然比微调模型快，但每场景 35 分钟左右的重建时间对于实时交互应用仍有优化空间。
*   **依赖基础模型**：重建质量高度依赖于第一步 DepthAnything-3 等几何基础模型的初始预测精度。

（完）
