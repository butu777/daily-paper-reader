---
title: "Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding"
authors: "Haibo Wang, Zihao Lin, Zhiyang Xu, Lifu Huang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00528v1"
tags: ["query:wm"]
score: 9.0
evidence: 3D环境中的3D视觉定位与空间语义解析
tldr: TAB是一个利用视觉语言模型和多视图几何进行零样本3D视觉定位的智能体框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00528v1/fig-001.webp\", \"caption\": \"Figure 1: (1) Top: Previous methods rely on preprocessed 3D point clouds, degrading the task into proposal matching. (2) Bottom: Our TAB operates directly on RGB-D streams. Through an iterative Think-Act-Build process, the agent reconstructs the target object.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-002.webp\", \"caption\": \"Table 1: 3D Visual Grounding results on ScanRefer. \\\"w/o PC\\\" denotes methods that do not rely on 3D point clouds as input. † means results with two stage training.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-003.webp\", \"caption\": \"Table 2: 3D Visual Grounding results on Nr3D (w/o GT object class). \\\"w/o PC\\\" denotes methods that do not rely on 3D point clouds as input.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-004.webp\", \"caption\": \"Figure 2: The \\\"Think, Act, Build (TAB)\\\" framework. Guided by an expert 3D-VG Skill, the VLM agent reasons about the task, invokes visual tools, and reconstructs the target.\", \"page\": 4, \"index\": 4, \"width\": 824, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-005.webp\", \"caption\": \"Figure 4: Qualitative comparison of different components in TAB.\", \"page\": 9, \"index\": 5, \"width\": 417, \"height\": 352}]"
motivation: 3D环境中的3D视觉定位与空间语义解析。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
3D 视觉定位 (3D-VG) 旨在通过自然语言描述在 3D 场景中定位物体。虽然利用视觉语言模型 (VLM) 的最新进展已经探索了零样本的可能性，但它们通常受限于依赖预处理 3D 点云的静态工作流，本质上将定位退化为了候选框匹配。为了摆脱这种依赖，我们的核心动机是将任务解耦：利用 2D VLM 解析复杂的空间语义，同时依靠确定性的多视图几何来实例化 3D 结构。基于这一见解，我们提出了“思考、行动、构建 (TAB)”，这是一个动态智能体框架，它将 3D-VG 任务重新表述为直接在原始 RGB-D 流上运行的生成式 2D 到 3D 重建范式。具体而言，在专门的 3D-VG 技能引导下，我们的 VLM 智能体动态调用视觉工具，在 2D 帧中跟踪并重建目标。至关重要的是，为了克服由严格的 VLM 语义跟踪引起的多视图覆盖不足问题，我们引入了语义锚定几何扩展，该机制首先在参考视频剪辑中锚定目标，然后利用多视图几何将其空间位置传播到未观察到的帧中。这使得智能体能够通过相机参数聚合这些多视图特征，从而“构建”目标的 3D 表示，将 2D 视觉线索直接映射到 3D 坐标。此外，为了确保评估的严谨性，我们识别了现有基准测试中存在的指代歧义和类别错误等缺陷，并手动修正了错误的查询。在 ScanRefer 和 Nr3D 上的大量实验表明，我们的框架完全依赖开源模型，其性能显著优于之前的零样本方法，甚至超过了全监督基线。

## 速览
**TLDR**：针对3D视觉定位（3D-VG）中过度依赖预处理点云的问题，本文提出TAB框架。该框架将3D-VG重构为生成式2D到3D重建过程，利用VLM智能体动态调用工具处理原始RGB-D流。通过语义锚定几何扩展机制，将2D语义与多视图几何结合，实现了无需训练的零样本3D定位，在多个基准测试上显著超越了现有方法。 \
**Motivation**：旨在摆脱对预处理3D点云的依赖，通过解耦空间语义理解与几何结构重建，利用2D VLM处理复杂的3D定位任务。 \
**Method**：提出“思考-行动-构建”框架，利用VLM智能体动态追踪目标，并通过语义锚定几何扩展机制将多视图2D特征聚合为3D坐标。 \
**Result**：在ScanRefer和Nr3D数据集上，该框架显著优于之前的零样本方法，甚至超过了部分全监督基线模型。 \
**Conclusion**：本研究证明了通过智能体化框架结合2D VLM与多视图几何，可以在无需3D预训练的情况下高效实现高精度的3D视觉定位。

---

## Abstract
3D Visual Grounding (3D-VG) aims to localize objects in 3D scenes via natural language descriptions. While recent advancements leveraging Vision-Language Models (VLMs) have explored zero-shot possibilities, they typically suffer from a static workflow relying on preprocessed 3D point clouds, essentially degrading grounding into proposal matching. To bypass this reliance, our core motivation is to decouple the task: leveraging 2D VLMs to resolve complex spatial semantics, while relying on deterministic multi-view geometry to instantiate the 3D structure. Driven by this insight, we propose "Think, Act, Build (TAB)", a dynamic agentic framework that reformulates 3D-VG tasks as a generative 2D-to-3D reconstruction paradigm operating directly on raw RGB-D streams. Specifically, guided by a specialized 3D-VG skill, our VLM agent dynamically invokes visual tools to track and reconstruct the target across 2D frames. Crucially, to overcome the multi-view coverage deficit caused by strict VLM semantic tracking, we introduce the Semantic-Anchored Geometric Expansion, a mechanism that first anchors the target in a reference video clip and then leverages multi-view geometry to propagate its spatial location across unobserved frames. This enables the agent to "Build" the target's 3D representation by aggregating these multi-view features via camera parameters, directly mapping 2D visual cues to 3D coordinates. Furthermore, to ensure rigorous assessment, we identify flaws such as reference ambiguity and category errors in existing benchmarks and manually refine the incorrect queries. Extensive experiments on ScanRefer and Nr3D demonstrate that our framework, relying entirely on open-source models, significantly outperforms previous zero-shot methods and even surpasses fully supervised baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Think, Act, Build (TAB) 框架

#### 1. 核心问题与整体含义（研究动机和背景）
3D 视觉定位（3D Visual Grounding, 3D-VG）的任务是根据自然语言描述在 3D 空间中精准定位物体。
*   **现有瓶颈**：目前的零样本（Zero-shot）方法大多依赖预先处理好的 3D 点云数据，将定位任务简化为“提案匹配”（Proposal Matching），即从预设的 3D 候选框中选一个。这限制了模型在没有预构建 3D 地图的实时环境中的应用。
*   **核心动机**：作者认为 2D 视觉语言模型（VLM）擅长处理复杂的空间语义，而 3D 结构的实例化应交给确定性的多视图几何。因此，论文提出了 **TAB** 框架，旨在直接从原始 RGB-D 视频流中，通过“生成式”的 2D 到 3D 重建来实现定位，彻底摆脱对预处理点云的依赖。

#### 2. 方法论：核心思想与技术细节
TAB 采用了一种基于智能体（Agentic）的动态循环，分为 **Think（思考）**、**Act（行动）**、**Build（构建）** 三个阶段：

*   **3D-VG 专家技能（Expert Skill）**：为 VLM 智能体提供一套标准操作程序（SOP），包括查询解析、粗到细过滤、目标隔离等步骤。
*   **参考目标定位（Reference Target Localization）**：
    *   **查询解析**：将复杂指令拆解为目标类别、属性、空间条件和场景特征。
    *   **粗细过滤**：先用检测器筛选含目标的帧，再由 VLM 验证是否符合场景约束，最后在选定的“参考帧”中通过视觉提示（Visual Prompting）锁定唯一的实例 ID。
*   **语义锚定几何扩展（Semantic-Anchored Geometric Expansion, SAGE）**：这是本文的核心技术，用于解决 VLM 在极端视角下语义跟踪失效的问题。
    *   **语义时间扩展（STE）**：利用 VLM 在时间轴上双向跟踪目标，构建一个初步的 3D 几何质心。
    *   **多视图几何扩展（MGE）**：将 3D 质心反向投影到视频的所有帧中。通过相机内参、外参及 Z-buffer 遮挡检查，确定目标在未观察帧中的 2D 位置，从而获取更全面的多视图掩码。
*   **2D 到 3D 重建**：将所有获取的 2D 掩码像素通过深度图逆投影回 3D 空间，利用 DBSCAN 聚类去噪，最终计算出轴向对齐的 3D 边界框（BBox）。

#### 3. 实验设计
*   **数据集与场景**：在 **ScanRefer** 和 **Nr3D** 两个主流 3D 场景理解基准上进行评估，均基于 ScanNet 室内场景。
*   **Benchmark 优化**：作者指出原始数据集中存在大量标注噪声（如指代歧义、类别错误、空间方位描述错误），并手动修正了这些错误以确保评估的严谨性。
*   **对比方法**：
    *   **全监督方法**：如 ScanRefer, 3DVG-T, EDA, G3-LQ, 以及基于 LLM 的 LLaVA-3D, GPT4Scene 等。
    *   **零样本方法**：如 LLM-Grounder, ZSVG3D, SeeGround, SeqVLM, SPAZER 等。

#### 4. 资源与算力
*   **模型组成**：核心智能体使用 **Qwen3-VL-32B**（开源 VLM），视觉工具包括 Grounding DINO（检测）和 SAM3（分割）。
*   **算力说明**：文中有提到视频采样率为 300 帧，扩展过程上限为 32 帧。**但文中未明确说明具体的 GPU 型号、数量以及推理/实验的总时长。** 考虑到 32B 参数量的 VLM 频繁调用，该框架对显存和推理延迟有一定要求。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 ScanRefer 的“唯一（Unique）”和“多目标（Multiple）”子集，以及 Nr3D 的“简单/困难”、“视角依赖/独立”子集。
*   **消融实验**：作者设计了多组消融实验，分别验证了“单帧重建”、“仅语义扩展”和“仅几何扩展”的效果，证明了 STE 和 MGE 协同工作的必要性。
*   **客观性与公平性**：通过引入“3D 辅助（w/o PC 标记为 ✗）”设置，将 TAB 生成的 BBox 与 Mask3D 提案进行匹配，以便与那些基于提案匹配的方法进行公平对比。实验设计较为全面，涵盖了定性与定量分析。

#### 6. 主要结论与发现
*   **性能领先**：TAB 在完全不依赖预处理点云的情况下，在 ScanRefer 上的 Acc@0.25 达到 71.2%，在 Nr3D 上达到 68.0%，显著优于现有的零样本方法。
*   **超越监督模型**：在某些指标上，TAB 的零样本表现甚至超过了经过全量数据训练的监督模型（如 SceneVerse）。
*   **几何的重要性**：实验发现，单纯依靠 VLM 的语义跟踪在视角变化剧烈时极易失败，而引入确定性的几何投影能显著提升多视图覆盖率和重建精度。

#### 7. 优点（亮点）
*   **范式创新**：将 3D-VG 从“静态匹配”转变为“动态推理与生成式重建”，更符合具身智能的实际应用场景。
*   **解耦设计**：巧妙地将 VLM 的语义理解能力与多视图几何的结构化能力结合，解决了 2D VLM 处理 3D 空间时的“空间感”缺失问题。
*   **鲁棒性**：具备故障恢复机制（Dynamic Adjustment），当某个工具调用失败时，智能体能自动调整阈值或跳过非关键步骤。
*   **数据贡献**：对 ScanRefer 和 Nr3D 标注错误的修正为后续研究提供了更干净的测试环境。

#### 8. 不足与局限
*   **依赖深度质量**：由于是基于 RGB-D 的逆投影，如果深度图存在严重噪声或缺失，重建的 3D 几何会受到直接影响。
*   **推理延迟**：Agent 模式涉及多次 VLM 推理和工具调用，其实时性可能不如单次前向传播的模型，难以直接应用于高频机器人控制。
*   **帧数限制**：目前实验中扩展帧数上限设为 32 帧，对于超大规模场景或极长视频流的泛化能力尚待验证。
*   **算力成本**：调用 32B 规模的 VLM 作为 Agent 核心，在边缘设备上的部署存在挑战。

（完）
