---
title: "SpatialStack: Layered Geometry-Language Fusion for 3D VLM Spatial Reasoning"
title_zh: SpatialStack：用于 3D VLM 空间推理的分层几何-语言融合
authors: "Jiang Zhang, Shijie Zhou, Bangya Liu, Achuta Kadambi, Zhiwen Fan"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27437v1"
tags: ["query:wm"]
score: 10.0
evidence: 具身与物理人工智能的三维空间推理
tldr: 针对大型视觉语言模型在3D空间推理能力的不足，本文提出了SpatialStack框架。该框架打破了仅在深层进行特征融合的传统限制，通过在模型各层级同步堆叠多级几何特征与语言表示，实现了视觉、几何与语言的层次化对齐。实验表明，VLM-SpatialStack在多个3D推理任务中取得SOTA性能，为下一代物理AI系统的空间理解提供了高效且可扩展的集成方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27437v1/fig-001.webp\", \"caption\": \"Table 3. Evaluation on VSI-Bench. Dark orange cells denote the best open-source result in each column, while light orange cells denote the second-best open-source result. Group-wise ranks within proprietary and open-source model blocks are highlighted in purple,\", \"page\": 7, \"index\": 1, \"width\": 990, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2603.27437v1/fig-002.webp\", \"caption\": \"Figure 2. Architecture of SpatialStack. A standard VLM backbone is coupled with a multi-view geometry encoder whose layer-wise features are processed by layer-specific projectors and sequentially injected into the LLM decoder, progressively integrating geometric cues. Explanation of the similarity heatmaps on the left is provided in Sec. 3. This multi-level injection preserves both fine-grained geometric structure and high-level spatial context, supporting more reliable low-level understanding and high-level reasoning.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2603.27437v1/fig-003.webp\", \"caption\": \"Table 4. Comparison on CV-Bench. Built on Qwen2.5, SpatialStack-4B outperforms its base model alongside VG-LLM and Cambrian-S. Scaling to Qwen3.5, SpatialStack-5B further improves upon its baseline to set a new state-of-the-art.\", \"page\": 8, \"index\": 3, \"width\": 487, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2603.27437v1/fig-004.webp\", \"caption\": \"Figure 4. Effect of Geometry Injection Layers on Spatial Tasks. Deeper layers improve high-level tasks, while low-level tasks peak at layer 11 and decline at deeper layers, suggesting a trade-off between fine-grained perception and higher-level reasoning.\", \"page\": 5, \"index\": 4, \"width\": 486, \"height\": 289}]"
motivation: 现有VLM在3D空间推理上存在瓶颈，主要是因为仅在深层融合视觉与几何特征，导致丢失了细粒度的层次化几何信号。
method: 提出一种通用的层次化融合框架，通过在模型骨干网络中逐层堆叠和同步多级几何特征，增强对局部几何精度和全局上下文的捕捉。
result: 基于该框架开发的VLM-SpatialStack在多个3D空间推理基准测试中刷新了纪录，显著提升了模型的空间理解与泛化能力。
conclusion: SpatialStack确立了多层次视觉-语言-几何融合的新范式，是构建具备稳健空间推理能力的物理AI系统的有效途径。
---

## 摘要
大型视觉语言模型（VLM）在可靠的 3D 空间推理方面仍面临挑战，而这是具身智能和物理 AI 系统的核心能力。这一局限性源于它们无法捕捉细粒度的 3D 几何结构和空间关系。尽管最近的研究尝试将多视图几何 Transformer 引入 VLM，但它们通常仅融合来自视觉和几何编码器的深层特征，丢弃了丰富的层次化信号，从而为空间理解造成了根本性的瓶颈。为了克服这一问题，我们提出了 SpatialStack，这是一个通用的层次化融合框架，能够在模型层级结构中逐步对齐视觉、几何和语言表示。SpatialStack 超越了传统的后期视觉-几何融合，将多级几何特征与语言主干网络进行堆叠和同步，使模型能够同时捕捉局部几何精度和全局上下文语义。基于该框架，我们开发了 VLM-SpatialStack 模型，该模型在多个 3D 空间推理基准测试中达到了最先进的性能。大量的实验和消融研究表明，我们的多级融合策略持续增强了 3D 理解能力，并在各种空间推理任务中表现出稳健的泛化能力，使 SpatialStack 成为下一代多模态物理 AI 系统中视觉-语言-几何集成的有效且可扩展的设计范式。

## 速览
**TLDR**：针对大语言模型在3D空间推理能力的不足，本文提出SpatialStack框架。该框架通过在模型层级中逐步融合视觉、几何与语言表示，克服了传统深层融合导致的信息丢失问题。通过多级几何特征与语言骨干网的同步，模型能同时捕捉局部几何精度和全局语义，在多个3D空间推理基准测试中达到SOTA水平，为具身智能提供了有效的几何集成方案。 \
**Motivation**：现有的视觉语言模型在3D空间推理方面表现不佳，主要原因是其仅在深层融合视觉与几何特征，忽略了丰富的层级信号。 \
**Method**：提出一种通用的层级融合框架，将多级几何特征与语言骨干网络进行堆叠和同步，实现视觉、几何与语言表示的渐进式对齐。 \
**Result**：基于该框架开发的VLM-SpatialStack在多个3D空间推理基准测试中取得了当前最先进的性能，并展现出强大的泛化能力。 \
**Conclusion**：SpatialStack证明了多级融合策略在增强3D理解方面的有效性，为下一代多模态物理AI系统的视觉-语言-几何集成提供了可扩展的设计范式。

---

## Abstract
Large vision-language models (VLMs) still struggle with reliable 3D spatial reasoning, a core capability for embodied and physical AI systems. This limitation arises from their inability to capture fine-grained 3D geometry and spatial relationships. While recent efforts have introduced multi-view geometry transformers into VLMs, they typically fuse only the deep-layer features from vision and geometry encoders, discarding rich hierarchical signals and creating a fundamental bottleneck for spatial understanding. To overcome this, we propose SpatialStack, a general hierarchical fusion framework that progressively aligns vision, geometry, and language representations across the model hierarchy. Moving beyond conventional late-stage vision-geometry fusion, SpatialStack stacks and synchronizes multi-level geometric features with the language backbone, enabling the model to capture both local geometric precision and global contextual semantics. Building upon this framework, we develop VLM-SpatialStack, a model that achieves state-of-the-art performance on multiple 3D spatial reasoning benchmarks. Extensive experiments and ablations demonstrate that our multi-level fusion strategy consistently enhances 3D understanding and generalizes robustly across diverse spatial reasoning tasks, establishing SpatialStack as an effective and extensible design paradigm for vision-language-geometry integration in next-generation multimodal physical AI systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《SpatialStack: Layered Geometry-Language Fusion for 3D VLM Spatial Reasoning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大模型（VLM）在 3D 空间推理（如距离估计、相对位置判断、导航规划）方面表现薄弱。
*   **研究动机**：作者发现，现有的视觉-几何融合方案通常只提取几何编码器（如 VGGT）的**最后一层（深层）特征**与视觉特征融合。这种“单层融合”丢弃了浅层网络中丰富的细粒度几何信号（如边缘、局部结构），导致模型在处理需要精确感知的低级任务和需要全局语义的高级推理任务时存在瓶颈。

### 2. 方法论：SpatialStack 框架
*   **核心思想**：提出一种**分层几何-语言融合（Layered Geometry-Language Fusion）**机制。不再将几何特征注入视觉编码器，而是将其直接、分层地注入到 LLM 解码器的不同层中。
*   **关键技术细节**：
    *   **多级特征提取**：从几何编码器（VGGT）中提取不同深度（如第 11、17、23 层）的特征。
    *   **空间对齐与重排**：为了匹配 LLM 中视觉 Token 的顺序，对几何 Token 进行窗口化重排（Window-based Reordering），确保几何信息与视觉区域在空间上对齐。
    *   **分层注入（Layered Injection）**：将提取的浅层、中层、深层几何特征，分别作为残差项（Additive Residuals）注入到 LLM 解码器的前几层（如第 0、1、2 层）。
    *   **掩码加法融合**：通过视觉 Token 掩码，确保几何特征仅耦合到序列中的视觉 Token 上，而不干扰文本 Token。
*   **算法流程**：输入多视图图像 -> 视觉编码器提取语义 Token -> 几何编码器提取多级几何 Token -> 几何 Token 投影至语言空间 -> 分层注入 LLM 解码器 -> LLM 输出推理结果。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：混合了 SPAR-234k、LLaVA-Hound、VLM-3R (ScanNet 分支) 和 VSI-590k 的子集，总计约 21.2 万个样本。
    *   **测试基准（Benchmarks）**：VSI-Bench（具身空间推理）、SPAR-Bench（3D 感知与推理）、BLINK-Spatial（细粒度视觉感知）、CV-Bench（2D/3D 空间任务）以及 Video-MME（通用视频理解）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-2.5 Pro。
    *   **开源模型**：Qwen2.5-VL, LLaVA-Video, VILA, LongVA 等。
    *   **几何增强模型**：Spatial-MLLM, VG-LLM, Cambrian-S。

### 4. 资源与算力
*   **硬件环境**：使用了 **32 台 NVIDIA A100 GPU (80GB)**。
*   **训练配置**：采用 DeepSpeed ZeRO-2 优化器，bfloat16 精度。
*   **训练时长**：文中未明确给出总训练小时数，但提到训练了 1 个 Epoch，学习率为 $1 \times 10^{-5}$，有效 Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流空间推理基准测试，并进行了详细的消融实验。
*   **消融实验**：包括几何特征注入位置（视觉端 vs 语言端）、注入层级的选择（单层 vs 多层）、融合顺序（正向层级对齐 vs 反向对齐）等。
*   **充分性与公平性**：实验设计较为严谨。作者在相同的基座模型（如 Qwen2.5-VL-3B）上对比了不同的融合策略，证明了 SpatialStack 架构本身的优越性，而非单纯依靠模型规模或数据量。

### 6. 主要结论与发现
*   **层级对应关系**：浅层几何特征有助于细粒度的空间感知（低级任务），深层几何特征支持高层级的上下文推理（高级任务）。
*   **架构优势**：将几何特征直接注入 LLM 解码器比注入视觉编码器更有效，能避免特征干扰并提升泛化能力。
*   **性能表现**：VLM-SpatialStack 在 VSI-Bench 等多个榜单上刷新了开源模型的 SOTA 纪录，甚至在某些任务（如路径规划）上展现出强大的零样本泛化能力。

### 7. 优点与亮点
*   **创新架构**：首次系统研究并实现了几何与语言表示的“层级对齐”，打破了传统的末端融合范式。
*   **模型无关性**：SpatialStack 是一个通用框架，可以适配不同的 VLM 基座（如 Qwen, LLaVA）。
*   **可视化分析**：通过 ROI 相似度热力图直观展示了几何特征在保持空间结构方面的优势，增强了论文的说服力。

### 8. 不足与局限
*   **计算开销**：虽然几何编码器是冻结的，但引入多级特征投影和分层注入会增加推理时的显存占用和计算延迟。
*   **数据依赖**：模型性能高度依赖于高质量的 3D 标注数据（如 SPAR），在缺乏此类数据的长尾场景下表现尚待验证。
*   **动态场景限制**：虽然在视频基准上表现良好，但对于极高动态或复杂遮挡环境下的实时 4D 推理，论文未做深入探讨。

（完）
