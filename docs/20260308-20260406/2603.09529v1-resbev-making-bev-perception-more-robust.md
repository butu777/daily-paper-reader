---
title: "RESBev: Making BEV Perception More Robust"
title_zh: RESBev：提升 BEV 感知的鲁棒性
authors: "Lifeng Zhuo, Kefan Jin, Zhe Liu, Hesheng Wang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09529v1"
tags: ["query:wm"]
score: 9.0
evidence: 用于自动驾驶鲁棒BEV感知的潜空间世界模型
tldr: 针对自动驾驶中BEV感知易受传感器退化和对抗攻击影响的问题，本文提出RESBev，一种即插即用的鲁棒性增强方法。该方法将鲁棒性建模为潜在语义预测问题，通过构建潜在世界模型提取时空相关性，学习BEV状态转移以预测并修复受损特征。实验证明，RESBev在nuScenes数据集上能显著提升现有模型应对多种干扰和攻击的能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09529v1/fig-001.webp\", \"caption\": \"Fig. 1: (a) By leveraging a latent world model, our method generates a predictive prior from past clean frames and fuses it with current corrupted observations to produce the final reconstructed BEV representation. (b) Performance comparison demonstrates that RESBev significantly enhances the baseline LSS model’s resilience against diverse natural disturbances and adversarial attacks.\", \"page\": 2, \"index\": 1, \"width\": 725, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2603.09529v1/fig-002.webp\", \"caption\": \"Fig. 3: The overall architecture of our proposed temporal fusion model. The model consists of two core components: Semantic Prior Predictor that predicts the current BEV state from the past, and Anomaly Reconstructor that fuses this prediction with current fused BEV features.\", \"page\": 7, \"index\": 2, \"width\": 581, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2603.09529v1/fig-003.webp\", \"caption\": \"Table 3: Average performance under seen corruptions across three severity levels. We compare four LSS-based models with and without RESBev, also including GraphBEV as an additional baseline. The ↑ symbol indicates the relative performance improvement brought by RESBev over the vanilla models\", \"page\": 12, \"index\": 3, \"width\": 725, \"height\": 181}]"
motivation: 现有的BEV感知系统在面对传感器故障或恶意对抗攻击时表现脆弱，严重威胁自动驾驶的安全。
method: 提出一种基于潜在世界模型的语义预测框架，利用时空相关性预测并重建受损的BEV特征，且无需修改原有骨干网络。
result: 在nuScenes数据集上的实验表明，仅需少量样本微调，RESBev即可显著增强现有BEV模型对自然干扰和对抗攻击的防御能力。
conclusion: RESBev通过在语义层面修复特征，为提升自动驾驶BEV感知的鲁棒性提供了一种通用且高效的即插即用方案。
---

## 摘要
鸟瞰图（BEV）感知已成为自动驾驶系统的基石，提供了一种对下游规划和控制至关重要的结构化、以自我为中心的表示。然而，实际部署面临着传感器退化和对抗性攻击的挑战，这些挑战可能导致严重的感知异常，并最终损害自动驾驶系统的安全性。为了解决这一问题，我们提出了一种具有韧性且即插即用的 BEV 感知方法 RESBev，该方法可以轻松应用于现有的 BEV 感知方法，以增强其对各种干扰的鲁棒性。具体而言，我们将感知鲁棒性重新定义为一个潜在语义预测问题。我们构建了一个潜在世界模型，用于提取连续 BEV 观测中的时空相关性，从而学习底层的 BEV 状态转移，以预测干净的 BEV 特征，用于重建受损的观测结果。所提出的框架在 Lift-Splat-Shoot 流线的语义特征层级运行，能够在不修改底层骨干网络的情况下，实现对自然干扰和对抗性攻击的泛化恢复。在 nuScenes 数据集上的大量实验表明，通过少样本微调，RESBev 显著提高了现有 BEV 感知模型对抗各种外部干扰和对抗性攻击的鲁棒性。

## 速览
**TLDR**：针对自动驾驶中BEV感知易受传感器退化和对抗攻击影响的问题，本文提出RESBev，一种即插即用的鲁棒性增强方法。该方法将鲁棒性建模为潜在语义预测问题，通过构建潜在世界模型提取序列BEV观测中的时空相关性，从而预测并重建受损的BEV特征。实验证明，RESBev在仅需少量样本微调的情况下，能显著提升现有模型在多种自然干扰和对抗攻击下的感知稳定性。 \
**Motivation**：现有的BEV感知系统在面对传感器故障或恶意对抗攻击时表现脆弱，严重威胁自动驾驶系统的安全性。 \
**Method**：提出一种基于潜在世界模型的框架，通过学习BEV状态的时空演变规律，在语义特征层面预测并修复受损的观测数据。 \
**Result**：在nuScenes数据集上的实验表明，该方法显著增强了现有BEV模型对各类外部干扰和对抗攻击的防御能力。 \
**Conclusion**：RESBev通过引入时空预测机制，为提升BEV感知系统的鲁棒性提供了一种通用、高效且无需修改主干网络的解决方案。

---

## Abstract
Bird's-eye-view (BEV) perception has emerged as a cornerstone of autonomous driving systems, providing a structured, ego-centric representation critical for downstream planning and control. However, real-world deployment faces challenges from sensor degradation and adversarial attacks, which can cause severe perceptual anomalies and ultimately compromise the safety of autonomous driving systems. To address this, we propose a resilient and plug-and-play BEV perception method, RESBev, which can be easily applied to existing BEV perception methods to enhance their robustness to diverse disturbances. Specifically, we reframe perception robustness as a latent semantic prediction problem. A latent world model is constructed to extract spatiotemporal correlations across sequential BEV observations, thereby learning the underlying BEV state transitions to predict clean BEV features for reconstructing corrupted observations. The proposed framework operates at the semantic feature level of the Lift-Splat-Shoot pipeline, enabling recovery that generalizes across both natural disturbances and adversarial attacks without modifying the underlying backbone. Extensive experiments on the nuScenes dataset demonstrate that, with few-shot fine-tuning, RESBev significantly improves the robustness of existing BEV perception models against various external disturbances and adversarial attacks.

---

## 论文详细总结（自动生成）

以下是对论文《RESBev: Making BEV Perception More Robust》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管鸟瞰图（BEV）感知已成为自动驾驶的核心，但在现实部署中，传感器退化（如恶劣天气、硬件故障）和恶意对抗攻击（如 FGSM、PGD 攻击）会导致感知系统出现严重异常，威胁行车安全。
*   **研究动机**：现有的鲁棒性提升方法存在局限性。例如，多模态融合依赖昂贵的 LiDAR；对抗训练难以泛化到未知的干扰类型；简单的时序聚合在面对细微但致命的对抗扰动时效果有限。因此，行业急需一种轻量级、通用且能应对多种干扰的即插即用方案。

### 2. 方法论：RESBev 框架
RESBev 的核心思想是将鲁棒感知重构为一个**潜在语义预测与重建问题**，利用“潜在世界模型”学习场景的演化规律。
*   **核心思想**：不直接依赖可能受损的当前帧观测，而是通过历史干净帧预测当前帧的“语义先验”，并以此为基准修复受损特征。
*   **关键技术组件**：
    1.  **语义先验预测器 (Semantic Prior Predictor)**：
        *   利用前一时刻的重建特征和自车运动向量（平移与旋转）。
        *   通过视觉和动作编码器将信息映射到紧凑的潜空间。
        *   使用基于 Transformer 的潜在动力学世界模型 (LDWM) 预测当前时刻的干净 BEV 潜状态，再通过解码器还原为高维 BEV 特征先验。
    2.  **异常重建器 (Anomaly Reconstructor)**：
        *   采用查询驱动（Query-driven）的交叉注意力机制。
        *   将预测的“干净先验”作为 Query，去探测当前受损的观测特征（Key/Value）。
        *   引入**可学习的动态门控因子**，自动平衡历史先验与当前观测的权重，从而在抑制噪声的同时捕捉场景中的随机变化（如突然出现的车辆）。
*   **算法流程与训练**：基于概率图模型 (PGM) 推导出的变分推断框架，通过最大化证据下界 (ELBO) 来联合训练预测器和重建器。

### 3. 实验设计
*   **数据集与场景**：使用主流的 **nuScenes** 自动驾驶数据集。
*   **任务与 Benchmark**：聚焦于 **BEV 语义分割**任务。
*   **基准模型 (Baselines)**：集成了四种经典的基于 LSS 的模型：LSS、SimpleBEV、GaussianLSS 和 FIERY。
*   **对比方法**：除了 vanilla 模型外，还对比了先进的鲁棒感知框架 **GraphBEV**。
*   **干扰设置**：
    *   **自然干扰**：雾、雪、强光、暗光、相机崩溃、丢帧、随机噪声。
    *   **对抗攻击**：FGSM、PGD、C&W 攻击。
    *   **泛化性测试**：在 5 种已知干扰上训练，在另外 5 种未见过的干扰上评估。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A100-SXM4-80GB GPU** 上完成。
*   **训练细节**：Batch size 设置为 16。论文提到该方法仅需“少样本微调 (few-shot fine-tuning)”，暗示其对算力的需求相对较低，具有较好的部署经济性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种不同架构的模型进行了广泛测试。
    *   涵盖了 10 种不同类型的干扰，每种干扰设有 3 个严重等级。
    *   进行了**消融实验**，验证了预测器和重建器各自的贡献。
    *   进行了**长时稳定性实验**（10 步连续受损重建），验证模型不会随时间产生严重的误差累积。
*   **充分性评价**：实验设计非常充分且客观。通过对比“已知干扰”与“未知干扰”，验证了模型的泛化能力；通过 t-SNE 特征可视化，从底层原理解释了干扰对语义空间的影响，增强了说服力。

### 6. 主要结论与发现
*   **显著提升鲁棒性**：RESBev 在各种干扰下均显著优于原始模型和 GraphBEV。例如，在 LSS 模型上，平均 IoU 提升了约 19%。
*   **泛化能力强**：即使面对训练期间未见过的干扰（如 C&W 攻击或特定天气），RESBev 依然能保持稳定的修复效果。
*   **生成式优于聚合式**：实验证明，简单的时序特征聚合难以过滤对抗扰动，而基于世界模型的生成式预测能有效绕过受损观测。
*   **介入位置至关重要**：在 BEV 语义特征层（Splat 阶段）进行干预，比在图像空间或任务输出空间更有效。

### 7. 优点与亮点
*   **即插即用**：无需修改原有的 Backbone 结构，可轻松适配各种基于 LSS 的 BEV 模型。
*   **架构洞察**：论文深入分析了为何在 BEV 空间而非图像空间进行修复，以及为何选择高维语义特征而非低维任务输出，具有很强的理论指导意义。
*   **应对对抗攻击**：在不进行专门对抗训练的情况下，通过时空一致性约束实现了对对抗攻击的天然防御。

### 8. 不足与局限
*   **架构限制**：目前主要针对基于 LSS（Lift-Splat-Shoot）路径的模型，对于基于 Transformer 全局注意力（如纯 BEVFormer 变体）的适配性有待进一步验证。
*   **任务覆盖**：实验主要集中在语义分割，虽然这是基础任务，但在 3D 目标检测、路径规划等更复杂的下游任务上的表现尚未完全展示。
*   **极端场景风险**：如果初始帧即受损，或者自车运动估计出现巨大偏差，世界模型的预测先验可能会失效，导致重建错误。

（完）
