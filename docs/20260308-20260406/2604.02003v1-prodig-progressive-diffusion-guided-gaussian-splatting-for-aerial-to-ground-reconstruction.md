---
title: "ProDiG: Progressive Diffusion-Guided Gaussian Splatting for Aerial to Ground Reconstruction"
authors: "Sirshapan Mitra, Yogesh S. Rawat"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02003v1"
tags: ["query:3d"]
score: 9.0
evidence: 基于扩散引导的高斯泼溅技术，用于航拍图像的3D场景重建
tldr: 提出一种渐进式扩散引导框架，用于实现从航拍到地面视角的高保真3D重建。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02003v1/fig-001.webp\", \"caption\": \"Figure 4. Qualitative analysis of ProDiG(ours): Comparison of our method with existing baselines on aerial-to-ground reconstruction. Gaussian Splatting [16] struggles to render complete scenes due to the absence of ground-level viewpoints, while Diffix+[38] exhibits noisy artifacts and hallucinated structures. In contrast, ProDiG (ours) produces geometrically consistent and visually coherent reconstructions with fewer hallucinations. Notably, in the second row, the aerial inputs and reconstructed model include cars visible from above, whereas the ground-truth image - captured at a different time - does not.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.02003v1/fig-002.webp\", \"caption\": \"Figure 3. Effectiveness of aeroFix: Comparison of aerial image refinement between Difix[38] and our aeroFix model. The noisy novel views are outlined in orange, the reference images in green, and the refined (fixed) novel images in pink. Difix tends to copy content from the reference view when the viewpoint difference is large, leading to inconsistencies and artifacts. In contrast, aeroFix effectively preserves structural fidelity and produces geometrically consistent, artifact-free refinements.\", \"page\": 6, \"index\": 2, \"width\": 1026, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.02003v1/fig-003.webp\", \"caption\": \"Figure 1. Overview of ProDiG. (a) Our framework reconstructs a complete 3D scene using only aerial images. A large distribution shift exists between the aerial training images and the ground-level query images. During evaluation, we render novel views at ground-level camera poses and compare them against ground-truth images. (b) In our Distance-Adaptive Gaussian Splatting module, each Gaussian is dynamically scaled and reweighted using a lightweight encoder that predicts adjustment factors from its learned scaling feature and its distance to the active camera. (c) We progressively render noisy novel views at successively lower altitudes, fix these views using our diffusion model, and iteratively retrain the Gaussian Splatting model using the fixed novel view.\", \"page\": 2, \"index\": 3, \"width\": 1019, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.02003v1/fig-004.webp\", \"caption\": \"Table 2. Comparison of our method with existing methods on the WRIVA dataset across two sites. Best values are in bold.(D: Dreamsim, P: PSNR, S: SSIM, L: LPIPS)\", \"page\": 7, \"index\": 4, \"width\": 487, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.02003v1/fig-005.webp\", \"caption\": \"Figure 5. Generalization across Varying Altitudes. We evaluate our method on the Aerial MegaDepth[36] dataset, which contains sites captured at diverse altitude ranges.\", \"page\": 8, \"index\": 5, \"width\": 496, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.02003v1/fig-006.webp\", \"caption\": \"Figure 6. Ablations. (top) Comparison of different progressive methods. (bottom) Effectiveness of Distance Adaptive Gaussian Module. 7k represents, evalution at 7k iteration, after initial training.\", \"page\": 8, \"index\": 6, \"width\": 496, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.02003v1/fig-007.webp\", \"caption\": \"Figure 2. Overview of aeroFix: (left) Our diffusion model is fine-tuned on aerial imagery using LoRA. The noisy novel view is fixed using the reference view to fixed novel image. In the diffusion block, the relative camera pose difference is injected into the timestep embedding of the noisy image to encode geometric variation across viewpoints. We additionally include Plücker ray embeddings before the attention mixing layer to provide geometric cues. In the Causal Attention Mixing module, we enforce an epipolar constraint by masking the novel query - reference key attention map such that only tokens aligned with the corresponding epipolar lines retain attention (value 1), while all others are suppressed (value 0). The reference query - novel key block is fully masked, and the remaining attention blocks operate under standard full attention. (right) The figure illustrates epipolar correspondences for multiple query points on the noisy image and their corresponding lines on the reference view.\", \"page\": 4, \"index\": 7, \"width\": 1027, \"height\": 408}]"
motivation: 基于扩散引导的高斯泼溅技术，用于航拍图像的3D场景重建。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
仅凭航拍图像生成地面视角和连贯的 3D 场景模型具有挑战性，原因在于极端的视角变化、缺失的中间观测以及巨大的尺度差异。现有方法要么在渲染后进行后期优化，这通常会导致几何不一致的结果；要么依赖于多高度的真值数据，而这在现实中很难获得。高斯泼溅（Gaussian Splatting）和基于扩散的优化在小范围变化下能提高保真度，但在巨大的航拍到地面跨度下则会失效。为了解决这些局限性，我们提出了 ProDiG（渐进式高度高斯泼溅），这是一个扩散引导的框架，能够将航拍 3D 表示逐步转化为具有地面级保真度的表示。ProDiG 合成中间高度的视图，并利用几何感知的因果注意力模块在每个阶段优化高斯表示，该模块将极线结构注入到参考视图扩散中。距离自适应高斯模块根据相机距离动态调整高斯的尺度和不透明度，确保在巨大视角跨度下的稳定重建。这些组件共同实现了渐进式的、具有几何依据的优化，且无需额外的地面真值视角。在合成和真实世界数据集上的广泛实验表明，ProDiG 能够生成视觉逼真的地面渲染图和连贯的 3D 几何结构，在视觉质量、几何一致性以及对极端视角变化的鲁棒性方面显著优于现有方法。

## 速览
**TLDR**：针对仅有航拍图像时难以重建高质量地面视图的问题，本文提出ProDiG框架。该方法结合扩散模型与3D高斯泼溅，通过逐步降低高度合成中间视图，并利用几何感知因果注意力模块注入极线结构，配合距离自适应高斯模块动态调整尺度。ProDiG在无需地面真值的情况下，实现了从航拍到地面的一致性3D重建，显著提升了视觉质量和几何稳定性。 \
**Motivation**：航拍与地面视图之间存在巨大的视角差异和尺度变化，导致现有3D重建方法在缺乏中间观测时难以生成几何一致的地面细节。 \
**Method**：提出一种渐进式高度引导框架，利用几何感知因果注意力注入极线约束，并结合距离自适应高斯模块动态优化表示。 \
**Result**：在合成和真实数据集上的实验表明，该方法在视觉真实感、几何一致性以及应对极端视角变化方面均优于现有技术。 \
**Conclusion**：ProDiG通过扩散引导的渐进式优化，有效解决了大跨度视角下的3D场景重建难题，无需额外地面数据即可生成高质量模型。

---

## Abstract
Generating ground-level views and coherent 3D site models from aerial-only imagery is challenging due to extreme viewpoint changes, missing intermediate observations, and large scale variations. Existing methods either refine renderings post-hoc, often producing geometrically inconsistent results, or rely on multi-altitude ground-truth, which is rarely available. Gaussian Splatting and diffusion-based refinements improve fidelity under small variations but fail under wide aerial-to-ground gaps. To address these limitations, we introduce ProDiG (Progressive Altitude Gaussian Splatting), a diffusion-guided framework that progressively transforms aerial 3D representations toward ground-level fidelity. ProDiG synthesizes intermediate-altitude views and refines the Gaussian representation at each stage using a geometry-aware causal attention module that injects epipolar structure into reference-view diffusion. A distance-adaptive Gaussian module dynamically adjusts Gaussian scale and opacity based on camera distance, ensuring stable reconstruction across large viewpoint gaps. Together, these components enable progressive, geometrically grounded refinement without requiring additional ground-truth viewpoints. Extensive experiments on synthetic and real-world datasets demonstrate that ProDiG produces visually realistic ground-level renderings and coherent 3D geometry, significantly outperforming existing approaches in terms of visual quality, geometric consistency, and robustness to extreme viewpoint changes.

---

## 论文详细总结（自动生成）

以下是对论文《ProDiG: Progressive Diffusion-Guided Gaussian Splatting for Aerial to Ground Reconstruction》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何仅利用**航拍图像**（Aerial-only）重建出高质量、几何一致的**地面视角**（Ground-level）3D模型。
*   **研究背景**：
    *   **视角跨度极大**：航拍与地面视角之间存在极端的角度变化和尺度差异，且缺乏中间过渡观测。
    *   **现有技术局限**：传统高斯泼溅（3DGS）在仅有航拍数据时会过度拟合航拍分布，导致地面视角出现严重伪影；现有的扩散模型优化方法在视角差异过大时容易产生幻觉或几何不一致。
    *   **数据稀缺**：在实际应用中，往往难以同时获得同一场景的多高度、多视角真值数据。

### 2. 论文提出的方法论
ProDiG 框架通过“渐进式下降”的思想，将巨大的视角鸿沟分解为多个可控的中间阶段。
*   **渐进式高度优化（Progressive Altitude Refinement）**：
    *   不直接从航拍跳跃到地面，而是合成一系列中间高度的虚拟视图。
    *   流程：训练初始GS -> 合成中间高度视图 -> 使用扩散模型修复（aeroFix） -> 将修复后的视图加入训练集重新训练GS -> 降低高度重复此过程（共5个阶段）。
*   **aeroFix 扩散修复模块**：
    *   **因果注意力混合（Causal Attention Mixing）**：引入极线几何（Epipolar Geometry）约束。在扩散过程中，新视角的 Token 只能关注参考视图中对应极线上的 Token，防止产生违背几何逻辑的幻觉。
    *   **几何嵌入**：将相机位姿差异和 Plücker 射线嵌入注入扩散模型，增强跨视角的一致性。
    *   **结构引导损失**：结合 DSSIM 损失和多尺度 Sobel 边缘损失，确保在去噪的同时保留建筑边缘等精细结构。
*   **距离自适应高斯模块（Distance-Adaptive Gaussian Module）**：
    *   针对相机距离变化剧烈的问题，引入轻量级 MLP，根据高斯点到相机的距离 $d_{gc}$ 动态调整其缩放（Scale）和不透明度（Opacity），提高大基线场景下的渲染稳定性。

### 3. 实验设计
*   **数据集**：
    *   **WRIVA**：真实世界、野外采集的多高度图像数据集（使用 COLMAP 估计位姿，排除地面图像参与点云生成以防泄露）。
    *   **MatrixCity**：大型合成城市数据集，包含航拍和街道视角。
    *   **Aerial MegaDepth**：用于验证模型的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了 3DGS、2DGS、Scaffold-GS、GS-MCMC 以及 Difix3D+（最先进的扩散优化方法）。
    *   **评价指标**：PSNR、SSIM（结构指标），LPIPS、**DreamSim**（感知指标，后者更符合人类视觉偏好）。

### 4. 资源与算力
*   **硬件平台**：使用单张 **NVIDIA RTX A6000 GPU** 进行实验。
*   **训练细节**：
    *   初始 GS 训练 7,000 次迭代。
    *   渐进式优化分为 5 个阶段（高度分别为原始高度的 90%, 70%, 50%, 30%, 10%）。
    *   文中未明确给出总训练时长（小时数），但提到了迭代次数和阶段数，属于中等算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：在多个真实和合成站点上进行了详尽测试，并提供了跨数据集的泛化性实验。
*   **消融实验**：
    *   验证了不同的渐进式轨迹策略（如随机前向移动、缩放轨迹等）。
    *   验证了距离自适应模块对 PSNR/SSIM 的提升作用。
    *   验证了 aeroFix 中极线约束和位姿嵌入的有效性。
*   **公平性**：对比方法均采用了相同的输入数据限制，且对基线方法（如 Difix3D+）进行了渐进式训练的适配，以确保对比的客观性。

### 6. 论文的主要结论与发现
*   **渐进式策略至关重要**：直接进行大跨度视角合成会导致扩散模型失效，而逐步降低高度能有效引导 3D 表示向地面保真度演进。
*   **几何约束不可或缺**：引入极线约束的因果注意力能显著减少扩散模型产生的“内容拷贝”错误和几何幻觉。
*   **性能领先**：ProDiG 在 DreamSim 等感知指标上显著优于现有方法，生成的地面视图更加清晰且几何结构更连贯。

### 7. 优点（亮点）
*   **无需地面真值**：实现了“仅凭航拍重建地面”的突破，具有极高的实用价值（如无人机测绘）。
*   **几何与生成结合**：巧妙地将极线几何约束嵌入到生成式扩散模型中，解决了扩散模型在 3D 任务中常见的“不合逻辑”问题。
*   **动态调整机制**：距离自适应模块解决了 3DGS 在处理大尺度变化场景时的不稳定性。

### 8. 不足与局限
*   **计算开销**：由于需要进行 5 个阶段的迭代训练，且每个阶段涉及扩散模型推理和 GS 重新训练，其总耗时必然高于单次训练的方法。
*   **误差累积风险**：渐进式过程中，如果早期阶段修复的视图存在偏差，可能会在后续阶段被放大。
*   **动态物体处理**：论文主要针对静态场景，对于包含大量行人、车辆等动态物体的复杂城市环境，其重建效果可能受限（虽然实验中提到了对车辆的处理，但并非核心优化点）。

（完）
